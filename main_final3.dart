// ============================================================
// PUBSPEC.YAML — replace dependencies section with:
//
//   dependencies:
//     flutter:
//       sdk: flutter
//     firebase_core: ^3.1.0
//     cloud_firestore: ^5.1.0
//     firebase_auth: ^5.1.0
//     intl: ^0.19.0
//     font_awesome_flutter: ^10.7.0
//     shared_preferences: ^2.2.2
//     qr_flutter: ^4.1.0
//     mobile_scanner: ^5.1.0
//     image_picker: ^1.1.2
//
// Run: flutter pub get
// Then: flutterfire configure  (generates firebase_options.dart)
//
// FIRESTORE INDEXES REQUIRED (create in Firebase Console):
// 1. Collection: toys | Fields: ownerId ASC, createdAt DESC
// 2. Collection: toys | Fields: isAvailable ASC, createdAt DESC
// 3. Collection: requests | Fields: ownerId ASC, status ASC, createdAt DESC
// 4. Collection: exchanges | Fields: ownerId ASC, createdAt DESC
// 5. Collection: exchanges | Fields: holderId ASC, createdAt DESC
// 6. Collection: notifications | Fields: userId ASC, createdAt DESC
// 7. Collection: messages | Fields: exchangeId ASC, createdAt DESC
// 8. Collection: messages | Fields: receiverId ASC, isRead ASC (NEW — for unread counts)
// 9. Collection: disputes | Fields: exchangeId ASC
// 10. Collection: disputes | Fields: reporterId ASC, createdAt DESC
//
// ── FIRESTORE SECURITY RULES (paste into Firebase Console → Firestore → Rules) ──
//
// rules_version = '2';
// service cloud.firestore {
//   match /databases/{database}/documents {
//
//     // Users must be authenticated for all reads/writes
//     function isAuth() { return request.auth != null; }
//     function isOwner(uid) { return request.auth.uid == uid; }
//
//     match /toys/{toyId} {
//       allow read: if isAuth();
//       allow create: if isAuth() && request.resource.data.ownerId == request.auth.uid;
//       allow update, delete: if isAuth() && isOwner(resource.data.ownerId);
//     }
//
//     match /requests/{reqId} {
//       allow read: if isAuth() &&
//         (resource.data.ownerId == request.auth.uid ||
//          resource.data.requesterId == request.auth.uid);
//       allow create: if isAuth() && request.resource.data.requesterId == request.auth.uid;
//       allow update: if isAuth() && resource.data.ownerId == request.auth.uid;
//     }
//
//     match /exchanges/{exId} {
//       allow read: if isAuth() &&
//         (resource.data.ownerId == request.auth.uid ||
//          resource.data.holderId == request.auth.uid);
//       allow create: if isAuth();
//       allow update: if isAuth() &&
//         (resource.data.ownerId == request.auth.uid ||
//          resource.data.holderId == request.auth.uid);
//     }
//
//     match /messages/{msgId} {
//       allow read: if isAuth() &&
//         (resource.data.senderId == request.auth.uid ||
//          resource.data.receiverId == request.auth.uid);
//       allow create: if isAuth() && request.resource.data.senderId == request.auth.uid;
//       allow update: if isAuth() && resource.data.receiverId == request.auth.uid;
//     }
//
//     match /notifications/{notifId} {
//       allow read, update: if isAuth() && resource.data.userId == request.auth.uid;
//       allow create: if isAuth();
//     }
//
//     match /feedback/{fbId} {
//       allow read: if isAuth();
//       allow create: if isAuth() && request.resource.data.fromUserId == request.auth.uid;
//     }
//
//     match /blacklist/{blId} {
//       allow read, write: if isAuth() && resource.data.userId == request.auth.uid;
//       allow create: if isAuth() && request.resource.data.userId == request.auth.uid;
//     }
//
//     match /disputes/{disId} {
//       allow read: if isAuth() &&
//         (resource.data.reporterId == request.auth.uid ||
//          resource.data.otherPartyId == request.auth.uid);
//       allow create: if isAuth() && request.resource.data.reporterId == request.auth.uid;
//       allow update: if isAuth();
//     }
//   }
// }
// ============================================================

import 'dart:async';
import 'dart:io';
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
import 'package:firebase_core/firebase_core.dart';
import 'package:firebase_auth/firebase_auth.dart' as fb_auth;
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:intl/intl.dart';
import 'package:font_awesome_flutter/font_awesome_flutter.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:qr_flutter/qr_flutter.dart';
import 'package:mobile_scanner/mobile_scanner.dart';
import 'package:image_picker/image_picker.dart';

// ─── Generated by: flutterfire configure ───────────────────────────────────
import 'firebase_options.dart';

// ==================== SPLASH SCREEN ====================
class SplashScreen extends StatefulWidget {
  const SplashScreen({super.key});
  @override
  State<SplashScreen> createState() => _SplashScreenState();
}

class _SplashScreenState extends State<SplashScreen> with TickerProviderStateMixin {
  late AnimationController _logoCtrl;
  late AnimationController _textCtrl;
  late Animation<double> _logoScale;
  late Animation<double> _logoOpacity;
  late Animation<double> _textOpacity;
  late Animation<Offset> _textSlide;
  late String _tagline;

  static const _taglines = [
    'Share joy, one toy at a time',
    'Every toy deserves a second adventure',
    'Giving toys a new home, one child at a time',
    'Where toys find new friends',
    'Sharing smiles, one toy at a time',
    'Less clutter, more laughter',
    'Your unused toy is someone\'s treasure',
    'Building community through play',
  ];

  @override
  void initState() {
    super.initState();
    _tagline = _taglines[DateTime.now().millisecondsSinceEpoch % _taglines.length];
    _logoCtrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 900));
    _textCtrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 600));

    _logoScale = CurvedAnimation(parent: _logoCtrl, curve: Curves.elasticOut)
        .drive(Tween(begin: 0.0, end: 1.0));
    _logoOpacity = CurvedAnimation(parent: _logoCtrl, curve: Curves.easeIn)
        .drive(Tween(begin: 0.0, end: 1.0));
    _textOpacity = CurvedAnimation(parent: _textCtrl, curve: Curves.easeIn)
        .drive(Tween(begin: 0.0, end: 1.0));
    _textSlide = CurvedAnimation(parent: _textCtrl, curve: Curves.easeOut)
        .drive(Tween(begin: const Offset(0, 0.4), end: Offset.zero));

    _logoCtrl.forward().then((_) => _textCtrl.forward());

    Future.delayed(const Duration(milliseconds: 2200), () {
      if (mounted) Navigator.of(context).pushReplacementNamed('/auth');
    });
  }

  @override
  void dispose() {
    _logoCtrl.dispose();
    _textCtrl.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        decoration: const BoxDecoration(
          gradient: LinearGradient(
            colors: [Color(0xFF3D32A8), Color(0xFF5B4FCF), Color(0xFF7B6FE8)],
            begin: Alignment.topLeft,
            end: Alignment.bottomRight,
          ),
        ),
        child: Stack(children: [
          // Background circles
          Positioned(top: -60, right: -60, child: _circle(200, Colors.white.withOpacity(0.05))),
          Positioned(bottom: -80, left: -80, child: _circle(280, Colors.white.withOpacity(0.05))),
          Positioned(top: 120, left: -40, child: _circle(100, Colors.white.withOpacity(0.04))),
          Center(
            child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [
              ScaleTransition(
                scale: _logoScale,
                child: FadeTransition(
                  opacity: _logoOpacity,
                  child: Container(
                    width: 110,
                    height: 110,
                    decoration: BoxDecoration(
                      color: Colors.white.withOpacity(0.15),
                      borderRadius: BorderRadius.circular(32),
                      border: Border.all(color: Colors.white.withOpacity(0.3), width: 1.5),
                    ),
                    child: const Center(child: ToyShareLogo(size: 72, animate: false, white: true)),
                  ),
                ),
              ),
              const SizedBox(height: 28),
              SlideTransition(
                position: _textSlide,
                child: FadeTransition(
                  opacity: _textOpacity,
                  child: Column(children: [
                    const Text('ToyShare',
                        style: TextStyle(color: Colors.white, fontSize: 34, fontWeight: FontWeight.w900, letterSpacing: -1)),
                    const SizedBox(height: 6),
                    Text(_tagline,
                        style: TextStyle(color: Colors.white.withOpacity(0.75), fontSize: 15)),
                  ]),
                ),
              ),
            ]),
          ),
          Positioned(
            bottom: 48,
            left: 0, right: 0,
            child: FadeTransition(
              opacity: _textOpacity,
              child: const Center(
                child: SizedBox(
                  width: 24, height: 24,
                  child: CircularProgressIndicator(color: Colors.white54, strokeWidth: 2),
                ),
              ),
            ),
          ),
        ]),
      ),
    );
  }

  Widget _circle(double size, Color color) => Container(
    width: size, height: size,
    decoration: BoxDecoration(color: color, shape: BoxShape.circle),
  );
}


// ==================== MAIN SHELL WITH BOTTOM NAV ====================
class MainShell extends StatefulWidget {
  const MainShell({super.key});
  @override
  State<MainShell> createState() => _MainShellState();
}

class _MainShellState extends State<MainShell> with TickerProviderStateMixin {
  int _currentIndex = 0;
  late PageController _pageCtrl;
  final GlobalKey<ScaffoldState> _scaffoldKey = GlobalKey<ScaffoldState>();

  // Pages: Home, Browse, Exchanges, Activities
  // (Notifications & Profile moved to drawer/appbar)
  static const _pages = [
    HomeScreen(),
    AvailableToysScreen(),
    MyExchangesScreen(),
    UserDashboardScreen(),
  ];

  @override
  void initState() {
    super.initState();
    _pageCtrl = PageController();
  }

  @override
  void dispose() {
    _pageCtrl.dispose();
    super.dispose();
  }

  void _onTap(int index) {
    setState(() => _currentIndex = index);
    _pageCtrl.jumpToPage(index);
  }

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final uid = _auth.currentUser?.uid ?? '';
    final user = _auth.currentUser;
    final username = user?.displayName ?? user?.email?.split('@').first ?? 'User';

    // ── Mobile layout for all platforms ──
    return Scaffold(
      key: _scaffoldKey,
      // ── Side Drawer ──
      drawer: Drawer(
        backgroundColor: isDark ? AppColors.darkCardBg : Colors.white,
        shape: const RoundedRectangleBorder(
          borderRadius: BorderRadius.only(topRight: Radius.circular(28), bottomRight: Radius.circular(28)),
        ),
        child: SafeArea(
          child: Column(children: [
            // Drawer header
            Container(
              width: double.infinity,
              padding: const EdgeInsets.fromLTRB(20, 24, 20, 24),
              decoration: const BoxDecoration(
                gradient: LinearGradient(
                  colors: [Color(0xFF3D32A8), Color(0xFF7B6FE8)],
                  begin: Alignment.topLeft,
                  end: Alignment.bottomRight,
                ),
                borderRadius: BorderRadius.only(topRight: Radius.circular(28)),
              ),
              child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                UserAvatar(name: username, size: 64),
                const SizedBox(height: 12),
                Text(username,
                  style: const TextStyle(color: Colors.white, fontSize: 20, fontWeight: FontWeight.w900)),
                const SizedBox(height: 2),
                Text(user?.email ?? '',
                  style: TextStyle(color: Colors.white.withOpacity(0.7), fontSize: 12.5)),
              ]),
            ),
            const SizedBox(height: 8),
            Expanded(
              child: ListView(padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8), children: [
                _drawerItem(Icons.person_rounded, 'My Profile', AppColors.primary, () {
                  Navigator.pop(context);
                  navigateTo(context, const ProfileScreen());
                }),
                _drawerItem(Icons.notifications_rounded, 'Notifications', AppColors.accent, () {
                  Navigator.pop(context);
                  navigateTo(context, const NotificationsScreen());
                }),
                _drawerItem(Icons.star_rounded, 'My Reviews', AppColors.yellow, () {
                  Navigator.pop(context);
                  navigateTo(context, const FeedbackScreen());
                }),
                _drawerItem(Icons.inbox_rounded, 'Pending Requests', AppColors.coral, () {
                  Navigator.pop(context);
                  navigateTo(context, const RequestsScreen());
                }),
                _drawerItem(Icons.block_rounded, 'Blocked Users', Colors.grey.shade600, () {
                  Navigator.pop(context);
                  navigateTo(context, const BlacklistScreen());
                }),
                const Divider(height: 24),
                // Dark/Light mode toggle
                Container(
                  margin: const EdgeInsets.only(bottom: 8),
                  decoration: BoxDecoration(
                    color: isDark ? Colors.white.withOpacity(0.05) : Colors.grey.shade50,
                    borderRadius: BorderRadius.circular(14),
                    border: Border.all(color: isDark ? AppColors.darkBorder : AppColors.divider),
                  ),
                  child: SwitchListTile(
                    value: isDark,
                    onChanged: (_) => ThemeManager().toggleTheme(),
                    activeColor: AppColors.primary,
                    secondary: Container(
                      width: 36, height: 36,
                      decoration: BoxDecoration(
                        color: isDark ? AppColors.yellow.withOpacity(0.15) : AppColors.primary.withOpacity(0.1),
                        borderRadius: BorderRadius.circular(10),
                      ),
                      child: Icon(
                        isDark ? Icons.light_mode_rounded : Icons.dark_mode_rounded,
                        color: isDark ? AppColors.yellow : AppColors.primary,
                        size: 18,
                      ),
                    ),
                    title: Text(
                      isDark ? 'Light Mode' : 'Dark Mode',
                      style: TextStyle(
                        fontSize: 14, fontWeight: FontWeight.w700,
                        color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
                      ),
                    ),
                    subtitle: Text(
                      isDark ? 'Switch to light theme' : 'Switch to dark theme',
                      style: TextStyle(fontSize: 11.5, color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary),
                    ),
                    contentPadding: const EdgeInsets.symmetric(horizontal: 12, vertical: 4),
                    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
                  ),
                ),
              ]),
            ),
            // Sign out at bottom
            Padding(
              padding: const EdgeInsets.fromLTRB(12, 0, 12, 12),
              child: _drawerItem(Icons.logout_rounded, 'Sign Out', AppColors.error, () async {
                Navigator.pop(context);
                final confirm = await showDialog<bool>(
                  context: context,
                  builder: (ctx) => AlertDialog(
                    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
                    title: const Text('Sign Out?', style: TextStyle(fontWeight: FontWeight.w800)),
                    content: const Text('Are you sure you want to sign out?'),
                    actions: [
                      TextButton(onPressed: () => Navigator.pop(ctx, false), child: const Text('Cancel')),
                      ElevatedButton(
                        onPressed: () => Navigator.pop(ctx, true),
                        style: ElevatedButton.styleFrom(backgroundColor: AppColors.error, foregroundColor: Colors.white, elevation: 0,
                          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
                        child: const Text('Sign Out', style: TextStyle(fontWeight: FontWeight.w700)),
                      ),
                    ],
                  ),
                );
                if (confirm == true) await _auth.signOut();
              }, isDestructive: true),
            ),
          ]),
        ),
      ),
      // ── App Bar ──
      appBar: AppBar(
        leading: IconButton(
          icon: Container(
            padding: const EdgeInsets.all(6),
            decoration: BoxDecoration(
              color: isDark ? Colors.white.withOpacity(0.08) : AppColors.primary.withOpacity(0.08),
              borderRadius: BorderRadius.circular(10),
            ),
            child: Icon(Icons.menu_rounded,
              color: isDark ? AppColors.darkTextPrimary : AppColors.primary, size: 20),
          ),
          onPressed: () => _scaffoldKey.currentState?.openDrawer(),
        ),
        title: Row(mainAxisSize: MainAxisSize.min, children: [
          const ToyShareLogo(size: 28, animate: false),
          const SizedBox(width: 8),
          RichText(text: const TextSpan(
            style: TextStyle(fontSize: 18, fontWeight: FontWeight.w900, letterSpacing: -0.5),
            children: [
              TextSpan(text: 'Toy', style: TextStyle(color: AppColors.primary)),
              TextSpan(text: 'Share', style: TextStyle(color: AppColors.accent)),
            ],
          )),
        ]),
        centerTitle: true,
        actions: [
          // Notification bell with badge
          StreamBuilder<QuerySnapshot>(
            stream: uid.isEmpty ? const Stream.empty() : _db
                .collection('notifications')
                .where('userId', isEqualTo: uid)
                .where('isRead', isEqualTo: false)
                .snapshots(),
            builder: (ctx, snap) {
              final count = snap.data?.docs.length ?? 0;
              return Stack(clipBehavior: Clip.none, children: [
                IconButton(
                  icon: Icon(
                    count > 0 ? Icons.notifications_active_rounded : Icons.notifications_rounded,
                    color: count > 0 ? AppColors.warning : (isDark ? AppColors.darkTextPrimary : AppColors.primary),
                  ),
                  onPressed: () => navigateTo(context, const NotificationsScreen()),
                  tooltip: 'Notifications',
                ),
                if (count > 0)
                  Positioned(
                    right: 6, top: 6,
                    child: IgnorePointer(
                      child: Container(
                        width: 16, height: 16,
                        decoration: BoxDecoration(
                          color: AppColors.error,
                          shape: BoxShape.circle,
                          border: Border.all(color: isDark ? AppColors.darkCardBg : Colors.white, width: 1.5),
                        ),
                        child: Center(child: Text(
                          count > 9 ? '9+' : '$count',
                          style: const TextStyle(color: Colors.white, fontSize: 8, fontWeight: FontWeight.w900),
                        )),
                      ),
                    ),
                  ),
              ]);
            },
          ),
          const SizedBox(width: 4),
        ],
      ),
      // ── Body ──
      body: PageView(
        controller: _pageCtrl,
        physics: const NeverScrollableScrollPhysics(),
        children: _pages,
      ),
      // ── Bottom Nav ──
      bottomNavigationBar: Container(
        decoration: BoxDecoration(
          color: isDark ? AppColors.darkCardBg : Colors.white,
          boxShadow: [
            BoxShadow(
              color: isDark ? Colors.black.withOpacity(0.4) : Colors.black.withOpacity(0.08),
              blurRadius: 24,
              offset: const Offset(0, -4),
            ),
          ],
        ),
        child: SafeArea(
          child: SizedBox(
            height: 62,
            child: Row(
              children: [
                Expanded(child: _navItem(0, Icons.home_rounded, Icons.home_outlined, 'Home')),
                Expanded(child: _navItem(1, Icons.search_rounded, Icons.search_outlined, 'Browse')),
                Expanded(
                  child: Center(
                    child: GestureDetector(
                      onTap: () => navigateTo(context, const AddToyScreen()),
                      child: Container(
                        width: 46, height: 46,
                        decoration: BoxDecoration(
                          gradient: const LinearGradient(
                            colors: [Color(0xFF5B4FCF), Color(0xFF7B6FE8)],
                            begin: Alignment.topLeft, end: Alignment.bottomRight,
                          ),
                          borderRadius: BorderRadius.circular(14),
                          boxShadow: [BoxShadow(
                            color: AppColors.primary.withOpacity(0.4),
                            blurRadius: 12, offset: const Offset(0, 3),
                          )],
                        ),
                        child: const Icon(Icons.add_rounded, color: Colors.white, size: 24),
                      ),
                    ),
                  ),
                ),
                Expanded(child: _navItemWithBadge(2, Icons.swap_horiz_rounded, Icons.swap_horiz_outlined, 'Exchanges', uid,
                    stream: uid.isEmpty ? const Stream.empty() : _db
                        .collection('exchanges')
                        .where('ownerId', isEqualTo: uid)
                        .where('status', isEqualTo: 'pending_receipt')
                        .snapshots())),
                Expanded(child: _navItem(3, Icons.history_rounded, Icons.history_outlined, 'Activities')),
              ],
            ),
          ),
        ),
      ),
    );
  }

  Widget _buildWebLayout(BuildContext context, bool isDark, String uid, String username) {
    final isDesktop = Responsive.isDesktop(context);
    final sidebarWidth = isDesktop ? 240.0 : 72.0;

    final navItems = [
      {'index': 0, 'icon': Icons.home_rounded, 'inactive': Icons.home_outlined, 'label': 'Home'},
      {'index': 1, 'icon': Icons.search_rounded, 'inactive': Icons.search_outlined, 'label': 'Browse'},
      {'index': 2, 'icon': Icons.swap_horiz_rounded, 'inactive': Icons.swap_horiz_outlined, 'label': 'Exchanges'},
      {'index': 3, 'icon': Icons.history_rounded, 'inactive': Icons.history_outlined, 'label': 'Activities'},
    ];

    Widget buildNavTile(Map<String, Object> item) {
      final index = item['index'] as int;
      final isActive = _currentIndex == index;
      final label = item['label'] as String;
      final icon = isActive ? item['icon'] as IconData : item['inactive'] as IconData;
      Widget tile = GestureDetector(
        onTap: () => _onTap(index),
        child: AnimatedContainer(
          duration: const Duration(milliseconds: 180),
          margin: const EdgeInsets.symmetric(horizontal: 10, vertical: 2),
          padding: EdgeInsets.symmetric(horizontal: isDesktop ? 14 : 0, vertical: 11),
          decoration: BoxDecoration(
            color: isActive ? AppColors.primary.withOpacity(0.1) : Colors.transparent,
            borderRadius: BorderRadius.circular(12),
            border: isActive ? Border.all(color: AppColors.primary.withOpacity(0.18)) : null,
          ),
          child: isDesktop
              ? Row(children: [
                  Icon(icon, color: isActive ? AppColors.primary : (isDark ? AppColors.darkTextSecondary : Colors.grey.shade500), size: 21),
                  const SizedBox(width: 12),
                  Text(label, style: TextStyle(fontSize: 14, fontWeight: isActive ? FontWeight.w800 : FontWeight.w600,
                    color: isActive ? AppColors.primary : (isDark ? AppColors.darkTextPrimary : AppColors.textPrimary))),
                ])
              : Center(child: Icon(icon,
                  color: isActive ? AppColors.primary : (isDark ? AppColors.darkTextSecondary : Colors.grey.shade500), size: 23)),
        ),
      );
      if (index == 2) {
        tile = StreamBuilder<QuerySnapshot>(
          stream: uid.isEmpty ? const Stream.empty() : _db
              .collection('exchanges')
              .where('ownerId', isEqualTo: uid)
              .where('status', isEqualTo: 'pending_receipt')
              .snapshots(),
          builder: (ctx, snap) {
            final count = snap.data?.docs.length ?? 0;
            return Stack(clipBehavior: Clip.none, children: [
              tile,
              if (count > 0) Positioned(right: isDesktop ? 12 : 8, top: 8,
                child: Container(width: 14, height: 14,
                  decoration: BoxDecoration(color: AppColors.error, shape: BoxShape.circle,
                    border: Border.all(color: isDark ? AppColors.darkCardBg : Colors.white, width: 1.5)),
                  child: Center(child: Text('$count', style: const TextStyle(color: Colors.white, fontSize: 7, fontWeight: FontWeight.w900))))),
            ]);
          },
        );
      }
      return tile;
    }

    // ── Sidebar widget ──
    Widget sidebar = Container(
      color: isDark ? AppColors.darkCardBg : Colors.white,
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.stretch,
        children: [
          // Logo header
          Container(
            padding: EdgeInsets.symmetric(horizontal: isDesktop ? 18 : 12, vertical: 16),
            child: isDesktop
                ? Row(children: [
                    const ToyShareLogo(size: 32, animate: false),
                    const SizedBox(width: 10),
                    RichText(text: const TextSpan(
                      style: TextStyle(fontSize: 17, fontWeight: FontWeight.w900, letterSpacing: -0.5),
                      children: [
                        TextSpan(text: 'Toy', style: TextStyle(color: AppColors.primary)),
                        TextSpan(text: 'Share', style: TextStyle(color: AppColors.accent)),
                      ],
                    )),
                  ])
                : const Center(child: ToyShareLogo(size: 32, animate: false)),
          ),
          Divider(height: 1, color: isDark ? AppColors.darkDivider : AppColors.divider),
          const SizedBox(height: 6),
          // ── Scrollable middle section ──
          Expanded(
            child: SingleChildScrollView(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.stretch,
                children: [
                  // Nav items
                  ...navItems.map((item) => buildNavTile(item as Map<String, Object>)),
                  const SizedBox(height: 12),
                  // Add toy button
                  Padding(
                    padding: EdgeInsets.symmetric(horizontal: isDesktop ? 14 : 10),
                    child: GestureDetector(
                      onTap: () => navigateTo(context, const AddToyScreen()),
                      child: Container(
                        padding: EdgeInsets.symmetric(vertical: 11, horizontal: isDesktop ? 12 : 0),
                        decoration: BoxDecoration(
                          gradient: const LinearGradient(colors: [Color(0xFF5B4FCF), Color(0xFF7B6FE8)],
                            begin: Alignment.topLeft, end: Alignment.bottomRight),
                          borderRadius: BorderRadius.circular(14),
                          boxShadow: [BoxShadow(color: AppColors.primary.withOpacity(0.3), blurRadius: 10, offset: const Offset(0, 4))],
                        ),
                        child: isDesktop
                            ? const Row(mainAxisAlignment: MainAxisAlignment.center, children: [
                                Icon(Icons.add_rounded, color: Colors.white, size: 18),
                                SizedBox(width: 6),
                                Text('List a Toy', style: TextStyle(color: Colors.white, fontWeight: FontWeight.w800, fontSize: 13.5)),
                              ])
                            : const Icon(Icons.add_rounded, color: Colors.white, size: 22),
                      ),
                    ),
                  ),
                  const SizedBox(height: 16),
                ],
              ),
            ),
          ),
          // ── Pinned bottom section ──
          Divider(height: 1, color: isDark ? AppColors.darkDivider : AppColors.divider),
          // Notifications
          StreamBuilder<QuerySnapshot>(
            stream: uid.isEmpty ? const Stream.empty() : _db
                .collection('notifications')
                .where('userId', isEqualTo: uid)
                .where('isRead', isEqualTo: false)
                .snapshots(),
            builder: (ctx, snap) {
              final count = snap.data?.docs.length ?? 0;
              return GestureDetector(
                onTap: () => navigateTo(context, const NotificationsScreen()),
                child: Container(
                  margin: const EdgeInsets.symmetric(horizontal: 10, vertical: 4),
                  padding: EdgeInsets.symmetric(horizontal: isDesktop ? 14 : 0, vertical: 10),
                  child: isDesktop
                      ? Row(children: [
                          Stack(clipBehavior: Clip.none, children: [
                            Icon(count > 0 ? Icons.notifications_active_rounded : Icons.notifications_rounded,
                              color: count > 0 ? AppColors.warning : (isDark ? AppColors.darkTextSecondary : Colors.grey.shade500), size: 21),
                            if (count > 0) Positioned(right: -4, top: -4,
                              child: Container(width: 14, height: 14,
                                decoration: BoxDecoration(color: AppColors.error, shape: BoxShape.circle,
                                  border: Border.all(color: isDark ? AppColors.darkCardBg : Colors.white, width: 1.5)),
                                child: Center(child: Text('$count', style: const TextStyle(color: Colors.white, fontSize: 7, fontWeight: FontWeight.w900))))),
                          ]),
                          const SizedBox(width: 12),
                          Expanded(child: Text('Notifications', style: TextStyle(fontSize: 13.5, fontWeight: FontWeight.w600,
                            color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary))),
                          if (count > 0)
                            Container(
                              padding: const EdgeInsets.symmetric(horizontal: 7, vertical: 2),
                              decoration: BoxDecoration(color: AppColors.error, borderRadius: BorderRadius.circular(20)),
                              child: Text('$count', style: const TextStyle(color: Colors.white, fontSize: 11, fontWeight: FontWeight.w800)),
                            ),
                        ])
                      : Center(child: Stack(clipBehavior: Clip.none, children: [
                          Icon(count > 0 ? Icons.notifications_active_rounded : Icons.notifications_rounded,
                            color: count > 0 ? AppColors.warning : (isDark ? AppColors.darkTextSecondary : Colors.grey.shade500), size: 22),
                          if (count > 0) Positioned(right: -4, top: -4,
                            child: Container(width: 12, height: 12,
                              decoration: BoxDecoration(color: AppColors.error, shape: BoxShape.circle),
                              child: Center(child: Text('$count', style: const TextStyle(color: Colors.white, fontSize: 7, fontWeight: FontWeight.w900))))),
                        ])),
                ),
              );
            },
          ),
          // Profile
          GestureDetector(
            onTap: () => navigateTo(context, const ProfileScreen()),
            child: Container(
              margin: const EdgeInsets.symmetric(horizontal: 10, vertical: 2),
              padding: EdgeInsets.symmetric(horizontal: isDesktop ? 14 : 0, vertical: 8),
              child: isDesktop
                  ? Row(children: [
                      UserAvatar(name: username, size: 30),
                      const SizedBox(width: 10),
                      Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                        Text(username, style: TextStyle(fontSize: 13, fontWeight: FontWeight.w700,
                          color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary), overflow: TextOverflow.ellipsis),
                        Text('View profile', style: TextStyle(fontSize: 11, color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary)),
                      ])),
                    ])
                  : Center(child: UserAvatar(name: username, size: 30)),
            ),
          ),
          // Theme toggle
          GestureDetector(
            onTap: () => ThemeManager().toggleTheme(),
            child: Container(
              margin: const EdgeInsets.symmetric(horizontal: 10, vertical: 2),
              padding: EdgeInsets.symmetric(horizontal: isDesktop ? 14 : 0, vertical: 10),
              child: isDesktop
                  ? Row(children: [
                      Icon(isDark ? Icons.light_mode_rounded : Icons.dark_mode_rounded,
                        color: isDark ? AppColors.yellow : AppColors.primary, size: 21),
                      const SizedBox(width: 12),
                      Text(isDark ? 'Light Mode' : 'Dark Mode',
                        style: TextStyle(fontSize: 13.5, fontWeight: FontWeight.w600,
                          color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                    ])
                  : Center(child: Icon(isDark ? Icons.light_mode_rounded : Icons.dark_mode_rounded,
                      color: isDark ? AppColors.yellow : AppColors.primary, size: 21)),
            ),
          ),
          const SizedBox(height: 8),
        ],
      ),
    );

    return Scaffold(
      backgroundColor: isDark ? const Color(0xFF12121C) : const Color(0xFFF0F0F8),
      body: SizedBox.expand(
        child: Row(
          crossAxisAlignment: CrossAxisAlignment.stretch,
          children: [
            // Sidebar with explicit bounded width — height comes from Row stretch
            RepaintBoundary(
              child: SizedBox(
                width: sidebarWidth,
                child: sidebar,
              ),
            ),
            // Divider line
            VerticalDivider(width: 1, color: isDark ? AppColors.darkDivider : AppColors.divider),
          // ── Main content ──
          Expanded(
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.stretch,
              children: [
                // Top bar
                Container(
                  color: isDark ? AppColors.darkCardBg : Colors.white,
                  padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 12),
                  child: Row(children: [
                    Expanded(
                      child: Text(_webPageTitle(_currentIndex), style: TextStyle(
                        fontSize: 20, fontWeight: FontWeight.w900, letterSpacing: -0.5,
                        color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
                      )),
                    ),
                    if (isDesktop)
                      Container(
                        width: 260, height: 38,
                        margin: const EdgeInsets.only(right: 12),
                        decoration: BoxDecoration(
                          color: isDark ? AppColors.darkInputBg : const Color(0xFFF3F4F8),
                          borderRadius: BorderRadius.circular(11),
                          border: Border.all(color: isDark ? AppColors.darkBorder : AppColors.divider),
                        ),
                        child: Row(children: [
                          const SizedBox(width: 10),
                          Icon(Icons.search_rounded, size: 17, color: Colors.grey.shade400),
                          const SizedBox(width: 8),
                          Expanded(child: TextField(
                            decoration: InputDecoration.collapsed(
                              hintText: 'Search toys...',
                              hintStyle: TextStyle(fontSize: 13, color: Colors.grey.shade400),
                            ),
                            style: TextStyle(fontSize: 13, color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
                          )),
                        ]),
                      ),
                    GestureDetector(
                      onTap: () => navigateTo(context, const ProfileScreen()),
                      child: UserAvatar(name: username, size: 34),
                    ),
                  ]),
                ),
                Divider(height: 1, color: isDark ? AppColors.darkDivider : AppColors.divider),
                Expanded(
                  child: PageView(
                    controller: _pageCtrl,
                    physics: const NeverScrollableScrollPhysics(),
                    children: _pages,
                  ),
                ),
              ],
            ),
          ),
        ],
      ),
      ),
    );
  }

  String _webPageTitle(int index) {
    const titles = ['Home', 'Browse Toys', 'My Exchanges', 'My Activities'];
    return index < titles.length ? titles[index] : 'ToyShare';
  }

  Widget _drawerItem(IconData icon, String label, Color color, VoidCallback onTap, {bool isDestructive = false}) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return ListTile(
      onTap: onTap,
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
      leading: Container(
        width: 36, height: 36,
        decoration: BoxDecoration(
          color: color.withOpacity(0.1),
          borderRadius: BorderRadius.circular(10),
        ),
        child: Icon(icon, color: color, size: 18),
      ),
      title: Text(label, style: TextStyle(
        fontSize: 14, fontWeight: FontWeight.w700,
        color: isDestructive ? AppColors.error : (isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
      )),
      trailing: Icon(Icons.chevron_right_rounded, size: 18, color: Colors.grey.shade400),
      contentPadding: const EdgeInsets.symmetric(horizontal: 8, vertical: 2),
      minLeadingWidth: 36,
    );
  }

  Widget _navItem(int index, IconData activeIcon, IconData inactiveIcon, String label) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final isActive = _currentIndex == index;
    return GestureDetector(
      onTap: () => _onTap(index),
      behavior: HitTestBehavior.opaque,
      child: AnimatedContainer(
        duration: const Duration(milliseconds: 200),
        padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
        decoration: BoxDecoration(
          color: isActive ? AppColors.primary.withOpacity(0.12) : Colors.transparent,
          borderRadius: BorderRadius.circular(12),
        ),
        child: Column(mainAxisSize: MainAxisSize.min, children: [
          AnimatedSwitcher(
            duration: const Duration(milliseconds: 200),
            child: Icon(
              isActive ? activeIcon : inactiveIcon,
              key: ValueKey(isActive),
              color: isActive ? AppColors.primary : (isDark ? AppColors.darkTextSecondary : Colors.grey.shade500),
              size: 22,
            ),
          ),
          const SizedBox(height: 2),
          Text(label, style: TextStyle(
            fontSize: 10,
            fontWeight: isActive ? FontWeight.w800 : FontWeight.w500,
            color: isActive ? AppColors.primary : (isDark ? AppColors.darkTextSecondary : Colors.grey.shade500),
          )),
        ]),
      ),
    );
  }

  Widget _navItemWithBadge(int index, IconData activeIcon, IconData inactiveIcon, String label, String uid, {required Stream<QuerySnapshot> stream}) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final isActive = _currentIndex == index;
    return StreamBuilder<QuerySnapshot>(
      stream: stream,
      builder: (ctx, snap) {
        final count = snap.data?.docs.length ?? 0;
        return GestureDetector(
          onTap: () => _onTap(index),
          behavior: HitTestBehavior.opaque,
          child: AnimatedContainer(
            duration: const Duration(milliseconds: 200),
            padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
            decoration: BoxDecoration(
              color: isActive ? AppColors.primary.withOpacity(0.12) : Colors.transparent,
              borderRadius: BorderRadius.circular(12),
            ),
            child: Column(mainAxisSize: MainAxisSize.min, children: [
              Stack(clipBehavior: Clip.none, children: [
                Icon(
                  isActive ? activeIcon : inactiveIcon,
                  color: isActive ? AppColors.primary : (isDark ? AppColors.darkTextSecondary : Colors.grey.shade500),
                  size: 22,
                ),
                if (count > 0)
                  Positioned(
                    right: -5, top: -4,
                    child: Container(
                      width: 14, height: 14,
                      decoration: BoxDecoration(
                        color: AppColors.error,
                        shape: BoxShape.circle,
                        border: Border.all(color: isDark ? AppColors.darkCardBg : Colors.white, width: 1.5),
                      ),
                      child: Center(child: Text(count > 9 ? '9+' : '$count',
                        style: const TextStyle(color: Colors.white, fontSize: 7, fontWeight: FontWeight.w900))),
                    ),
                  ),
              ]),
              const SizedBox(height: 2),
              Text(label, style: TextStyle(
                fontSize: 10,
                fontWeight: isActive ? FontWeight.w800 : FontWeight.w500,
                color: isActive ? AppColors.primary : (isDark ? AppColors.darkTextSecondary : Colors.grey.shade500),
              )),
            ]),
          ),
        );
      },
    );
  }
}

// ==================== THEME MANAGER (DARK MODE) ====================
class ThemeManager extends ChangeNotifier {
  static final ThemeManager _instance = ThemeManager._internal();
  factory ThemeManager() => _instance;
  ThemeManager._internal();

  ThemeMode _themeMode = ThemeMode.system;
  ThemeMode get themeMode => _themeMode;

  bool get isDarkMode {
    if (_themeMode == ThemeMode.dark) return true;
    if (_themeMode == ThemeMode.light) return false;
    return WidgetsBinding.instance.platformDispatcher.platformBrightness == Brightness.dark;
  }

  Future<void> loadTheme() async {
    final prefs = await SharedPreferences.getInstance();
    final saved = prefs.getString('theme_mode');
    if (saved == 'dark') _themeMode = ThemeMode.dark;
    else if (saved == 'light') _themeMode = ThemeMode.light;
    else _themeMode = ThemeMode.system;
    notifyListeners();
  }

  Future<void> setThemeMode(ThemeMode mode) async {
    _themeMode = mode;
    final prefs = await SharedPreferences.getInstance();
    String value;
    switch (mode) {
      case ThemeMode.dark: value = 'dark'; break;
      case ThemeMode.light: value = 'light'; break;
      default: value = 'system';
    }
    await prefs.setString('theme_mode', value);
    notifyListeners();
  }

  Future<void> toggleTheme() async {
    final newMode = isDarkMode ? ThemeMode.light : ThemeMode.dark;
    await setThemeMode(newMode);
  }
}

// ==================== DARK MODE COLORS ====================
class AppColors {
  static const primary = Color(0xFF5B4FCF);
  static const primaryLight = Color(0xFF7B6FE8);
  static const primaryDark = Color(0xFF3D32A8);
  static const accent = Color(0xFF4ECDC4);
  static const coral = Color(0xFFFF9A3C);
  static const yellow = Color(0xFFFFE066);
  static const success = Color(0xFF06D6A0);
  static const warning = Color(0xFFFFBE0B);
  static const error = Color(0xFFEF476F);
  static const surface = Color(0xFFF7F6FF);
  static const cardBg = Colors.white;
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B6B8A);
  static const divider = Color(0xFFEEEEF5);

  static const darkSurface = Color(0xFF12121A);
  static const darkCardBg = Color(0xFF1E1E2E);
  static const darkTextPrimary = Color(0xFFE8E8F0);
  static const darkTextSecondary = Color(0xFF9B9BB8);
  static const darkDivider = Color(0xFF2A2A3E);
  static const darkInputBg = Color(0xFF252538);
  static const darkBorder = Color(0xFF3A3A52);

  static const List<Color> heroGradient = [Color(0xFF5B4FCF), Color(0xFF7B6FE8)];
  static const List<Color> heroGradientWarm = [Color(0xFF5B4FCF), Color(0xFF8B5CF6)];
  static const List<Color> cardGradientTeal = [Color(0xFF4ECDC4), Color(0xFF38B2AC)];
  static const List<Color> cardGradientCoral = [Color(0xFFFF9A3C), Color(0xFFFF6B35)];
}

class AppThemeColors {
  final bool isDark;
  AppThemeColors(this.isDark);

  Color get surface => isDark ? AppColors.darkSurface : AppColors.surface;
  Color get cardBg => isDark ? AppColors.darkCardBg : AppColors.cardBg;
  Color get textPrimary => isDark ? AppColors.darkTextPrimary : AppColors.textPrimary;
  Color get textSecondary => isDark ? AppColors.darkTextSecondary : AppColors.textSecondary;
  Color get divider => isDark ? AppColors.darkDivider : AppColors.divider;
  Color get inputBg => isDark ? AppColors.darkInputBg : const Color(0xFFF8F8FC);
  Color get border => isDark ? AppColors.darkBorder : const Color(0xFFE0E0F0);
  Color get shadowColor => isDark ? Colors.black.withOpacity(0.3) : const Color(0x145B4FCF);
  Color get appBarBg => isDark ? AppColors.darkCardBg : Colors.white;
  Color get scaffoldBg => isDark ? AppColors.darkSurface : AppColors.surface;
  Color get bottomSheetBg => isDark ? AppColors.darkCardBg : Colors.white;
}

class AppIcons {
  static const IconData listToy = FontAwesomeIcons.list;
  static const IconData myToys = FontAwesomeIcons.puzzlePiece;
  static const IconData browse = FontAwesomeIcons.magnifyingGlass;
  static const IconData requests = FontAwesomeIcons.inbox;
  static const IconData exchanges = FontAwesomeIcons.arrowsRotate;
  static const IconData blacklist = FontAwesomeIcons.userSlash;
  static const IconData toys = FontAwesomeIcons.cube;
  static const IconData person = FontAwesomeIcons.user;
  static const IconData email = FontAwesomeIcons.envelope;
  static const IconData password = FontAwesomeIcons.lock;
  static const IconData logout = FontAwesomeIcons.rightFromBracket;
  static const IconData trash = FontAwesomeIcons.trashCan;
  static const IconData addToy = FontAwesomeIcons.circlePlus;
  static const IconData donation = FontAwesomeIcons.heart;
  static const IconData refresh = FontAwesomeIcons.arrowsRotate;
  static const IconData chat = FontAwesomeIcons.message;
  static const IconData dispute = FontAwesomeIcons.triangleExclamation;
}

final _auth = fb_auth.FirebaseAuth.instance;
final _db = FirebaseFirestore.instance;

// ==================== MAIN ====================
Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);

  _db.settings = const Settings(
    persistenceEnabled: true,
    cacheSizeBytes: Settings.CACHE_SIZE_UNLIMITED,
  );

  await ThemeManager().loadTheme();

  SystemChrome.setSystemUIOverlayStyle(
    const SystemUiOverlayStyle(
      statusBarColor: Colors.transparent,
      statusBarIconBrightness: Brightness.dark,
      systemNavigationBarColor: Colors.white,
    ),
  );

  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return AnimatedBuilder(
      animation: ThemeManager(),
      builder: (context, child) {
        final isDark = ThemeManager().isDarkMode;
        return MaterialApp(
          title: 'ToyShare',
          debugShowCheckedModeBanner: false,
          themeMode: ThemeManager().themeMode,
          theme: _buildLightTheme(),
          darkTheme: _buildDarkTheme(),
          home: const SplashScreen(),
          routes: {
            '/auth': (_) => StreamBuilder<fb_auth.User?>(
              stream: fb_auth.FirebaseAuth.instance.authStateChanges(),
              builder: (ctx, snapshot) {
                if (snapshot.connectionState == ConnectionState.waiting) {
                  return const Scaffold(body: Center(child: CircularProgressIndicator(color: AppColors.primary)));
                }
                return snapshot.data == null ? const LoginScreen() : const MainShell();
              },
            ),
            '/login': (_) => const LoginScreen(),
            '/home': (_) => const HomeScreen(),
          },
        );
      },
    );
  }

  ThemeData _buildLightTheme() {
    return ThemeData(
      useMaterial3: true,
      brightness: Brightness.light,
      colorSchemeSeed: AppColors.primary,
      fontFamily: 'Roboto',
      scaffoldBackgroundColor: AppColors.surface,
      appBarTheme: const AppBarTheme(
        centerTitle: true,
        elevation: 0,
        scrolledUnderElevation: 1,
        shadowColor: Color(0x145B4FCF),
        backgroundColor: Colors.white,
        foregroundColor: AppColors.textPrimary,
        surfaceTintColor: Colors.transparent,
        titleTextStyle: TextStyle(
          color: AppColors.textPrimary,
          fontSize: 17,
          fontWeight: FontWeight.w800,
          letterSpacing: -0.4,
        ),
        iconTheme: IconThemeData(color: AppColors.textPrimary, size: 22),
      ),
      cardTheme: CardThemeData(
        elevation: 0,
        color: Colors.white,
        surfaceTintColor: Colors.transparent,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(18),
          side: const BorderSide(color: Color(0xFFEEEEF5), width: 1),
        ),
      ),
      inputDecorationTheme: InputDecorationTheme(
        border: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: Color(0xFFE0E0F0)),
        ),
        enabledBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: Color(0xFFE0E0F0)),
        ),
        focusedBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: AppColors.primary, width: 2),
        ),
        errorBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: AppColors.error, width: 1.5),
        ),
        filled: true,
        fillColor: const Color(0xFFF8F8FC),
        contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 15),
        labelStyle: const TextStyle(color: AppColors.textSecondary, fontSize: 14),
        hintStyle: TextStyle(color: Colors.grey.shade400, fontSize: 14),
      ),
      dividerTheme: const DividerThemeData(color: AppColors.divider, space: 1),
      snackBarTheme: SnackBarThemeData(
        behavior: SnackBarBehavior.floating,
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
      ),
      dialogTheme: DialogThemeData(
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(24)),
        elevation: 0,
        shadowColor: Colors.transparent,
        backgroundColor: Colors.white,
      ),
      bottomSheetTheme: const BottomSheetThemeData(
        backgroundColor: Colors.white,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.vertical(top: Radius.circular(28)),
        ),
      ),
      floatingActionButtonTheme: const FloatingActionButtonThemeData(
        backgroundColor: AppColors.primary,
        foregroundColor: Colors.white,
        elevation: 4,
        shape: StadiumBorder(),
      ),
    );
  }

  ThemeData _buildDarkTheme() {
    return ThemeData(
      useMaterial3: true,
      brightness: Brightness.dark,
      colorSchemeSeed: AppColors.primary,
      fontFamily: 'Roboto',
      scaffoldBackgroundColor: AppColors.darkSurface,
      appBarTheme: AppBarTheme(
        centerTitle: true,
        elevation: 0,
        scrolledUnderElevation: 1,
        shadowColor: Colors.black.withOpacity(0.3),
        backgroundColor: AppColors.darkCardBg,
        foregroundColor: AppColors.darkTextPrimary,
        surfaceTintColor: Colors.transparent,
        titleTextStyle: const TextStyle(
          color: AppColors.darkTextPrimary,
          fontSize: 17,
          fontWeight: FontWeight.w800,
          letterSpacing: -0.4,
        ),
        iconTheme: const IconThemeData(color: AppColors.darkTextPrimary, size: 22),
      ),
      cardTheme: CardThemeData(
        elevation: 0,
        color: AppColors.darkCardBg,
        surfaceTintColor: Colors.transparent,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(18),
          side: const BorderSide(color: AppColors.darkBorder, width: 1),
        ),
      ),
      inputDecorationTheme: InputDecorationTheme(
        border: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: AppColors.darkBorder),
        ),
        enabledBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: AppColors.darkBorder),
        ),
        focusedBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: AppColors.primaryLight, width: 2),
        ),
        errorBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: AppColors.error, width: 1.5),
        ),
        filled: true,
        fillColor: AppColors.darkInputBg,
        contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 15),
        labelStyle: const TextStyle(color: AppColors.darkTextSecondary, fontSize: 14),
        hintStyle: TextStyle(color: Colors.grey.shade600, fontSize: 14),
      ),
      dividerTheme: const DividerThemeData(color: AppColors.darkDivider, space: 1),
      snackBarTheme: SnackBarThemeData(
        behavior: SnackBarBehavior.floating,
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
        backgroundColor: AppColors.darkCardBg,
        contentTextStyle: const TextStyle(color: AppColors.darkTextPrimary),
      ),
      dialogTheme: DialogThemeData(
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(24)),
        elevation: 0,
        shadowColor: Colors.transparent,
        backgroundColor: AppColors.darkCardBg,
      ),
      bottomSheetTheme: const BottomSheetThemeData(
        backgroundColor: AppColors.darkCardBg,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.vertical(top: Radius.circular(28)),
        ),
      ),
      floatingActionButtonTheme: const FloatingActionButtonThemeData(
        backgroundColor: AppColors.primaryLight,
        foregroundColor: Colors.white,
        elevation: 4,
        shape: StadiumBorder(),
      ),
    );
  }
}

class AppShadows {
  static List<BoxShadow> card(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return [
      BoxShadow(
        color: isDark
            ? Colors.black.withOpacity(0.3)
            : const Color(0xFF5B4FCF).withOpacity(0.08),
        blurRadius: 20,
        offset: const Offset(0, 4),
      ),
      BoxShadow(
        color: isDark
            ? Colors.black.withOpacity(0.2)
            : Colors.black.withOpacity(0.04),
        blurRadius: 8,
        offset: const Offset(0, 2),
      ),
    ];
  }
  static List<BoxShadow> button = [
    BoxShadow(
      color: const Color(0xFF5B4FCF).withOpacity(0.4),
      blurRadius: 16,
      offset: const Offset(0, 6),
      spreadRadius: -4,
    ),
  ];
  static List<BoxShadow> hero(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return [
      BoxShadow(
        color: isDark
            ? Colors.black.withOpacity(0.5)
            : const Color(0xFF5B4FCF).withOpacity(0.3),
        blurRadius: 32,
        offset: const Offset(0, 12),
        spreadRadius: -6,
      ),
    ];
  }
}

class Responsive {
  static double width(BuildContext context) => MediaQuery.of(context).size.width;
  static bool isMobile(BuildContext context) => width(context) < 650;
  static bool isTablet(BuildContext context) => width(context) >= 650 && width(context) < 1100;
  static bool isDesktop(BuildContext context) => width(context) >= 1100;
  static bool isWeb(BuildContext context) => !isMobile(context);

  static int gridCrossAxisCount(BuildContext context) {
    if (isMobile(context)) return 2;
    if (isTablet(context)) return 3;
    return 4;
  }
  static double formMaxWidth(BuildContext context) {
    if (isMobile(context)) return double.infinity;
    if (isTablet(context)) return 560;
    return 640;
  }
  static double contentMaxWidth(BuildContext context) {
    if (isMobile(context)) return double.infinity;
    if (isTablet(context)) return 760;
    return 1080;
  }
  static EdgeInsets pagePadding(BuildContext context) {
    if (isMobile(context)) return const EdgeInsets.symmetric(horizontal: 16, vertical: 16);
    if (isTablet(context)) return const EdgeInsets.symmetric(horizontal: 32, vertical: 20);
    return const EdgeInsets.symmetric(horizontal: 48, vertical: 24);
  }
}

Future<T?> navigateTo<T>(BuildContext context, Widget page) =>
    Navigator.push<T>(context, _SlideUpRoute(page: page));

Future<T?> navigateReplace<T>(BuildContext context, Widget page) =>
    Navigator.pushReplacement(context, _FadeRoute(page: page));

class _SlideUpRoute<T> extends PageRouteBuilder<T> {
  final Widget page;
  _SlideUpRoute({required this.page})
      : super(
    pageBuilder: (_, __, ___) => page,
    transitionsBuilder: (_, animation, __, child) {
      final tween = Tween(begin: const Offset(0.0, 0.04), end: Offset.zero)
          .chain(CurveTween(curve: Curves.easeOutCubic));
      final fadeTween = Tween<double>(begin: 0.0, end: 1.0)
          .chain(CurveTween(curve: Curves.easeOut));
      return SlideTransition(
        position: animation.drive(tween),
        child: FadeTransition(opacity: animation.drive(fadeTween), child: child),
      );
    },
    transitionDuration: const Duration(milliseconds: 320),
    reverseTransitionDuration: const Duration(milliseconds: 260),
  );
}

class _FadeRoute<T> extends PageRouteBuilder<T> {
  final Widget page;
  _FadeRoute({required this.page})
      : super(
    pageBuilder: (_, __, ___) => page,
    transitionsBuilder: (_, animation, __, child) => FadeTransition(
      opacity: CurvedAnimation(parent: animation, curve: Curves.easeInOut),
      child: child,
    ),
    transitionDuration: const Duration(milliseconds: 280),
  );
}

class InAppNotificationService {
  static StreamSubscription<QuerySnapshot>? _sub;
  static DateTime? _startTime;

  static void init(BuildContext context) {
    final uid = _auth.currentUser?.uid;
    if (uid == null) return;
    _startTime = DateTime.now();

    _sub = _db
        .collection('notifications')
        .where('userId', isEqualTo: uid)
        .where('isRead', isEqualTo: false)
        .snapshots()
        .listen((snap) {
      for (final change in snap.docChanges) {
        if (change.type != DocumentChangeType.added) continue;
        final data = change.doc.data();
        if (data == null) continue;

        final ts = data['createdAt'] as Timestamp?;
        final startTime = _startTime;
        if (startTime != null && ts != null && ts.toDate().isBefore(startTime)) continue;

        if (!context.mounted) return;
        _showBanner(context, data);
      }
    }, onError: (e) => debugPrint('InAppNotificationService error: $e'));
  }

  static void dispose() {
    _sub?.cancel();
    _sub = null;
    _startTime = null;
  }

  static void _showBanner(BuildContext context, Map<String, dynamic> data) {
    final title = (data['title'] as String?)?.trim() ?? 'ToyShare';
    final body = (data['body'] as String?)?.trim() ?? '';
    final type = (data['type'] as String?) ?? '';

    Color bannerColor;
    IconData bannerIcon;
    switch (type) {
      case 'request_received':
        bannerColor = AppColors.primary;
        bannerIcon = Icons.inbox_rounded;
        break;
      case 'request_accepted':
        bannerColor = AppColors.success;
        bannerIcon = Icons.check_circle_rounded;
        break;
      case 'request_rejected':
        bannerColor = AppColors.error;
        bannerIcon = Icons.cancel_rounded;
        break;
      case 'toy_returned':
        bannerColor = AppColors.coral;
        bannerIcon = Icons.assignment_return_rounded;
        break;
      case 'receipt_confirmed':
        bannerColor = AppColors.success;
        bannerIcon = Icons.verified_rounded;
        break;
      case 'new_message':
        bannerColor = AppColors.accent;
        bannerIcon = Icons.message_rounded;
        break;
      case 'dispute_opened':
        bannerColor = AppColors.error;
        bannerIcon = Icons.warning_amber_rounded;
        break;
      default:
        bannerColor = AppColors.primary;
        bannerIcon = Icons.notifications_active_rounded;
    }

    final overlay = Overlay.of(context);
    late OverlayEntry entry;

    entry = OverlayEntry(
      builder: (_) => _BannerWidget(
        title: title,
        body: body,
        color: bannerColor,
        icon: bannerIcon,
        onDismiss: () { if (entry.mounted) entry.remove(); },
      ),
    );

    overlay.insert(entry);
    Future.delayed(const Duration(seconds: 4), () {
      if (entry.mounted) entry.remove();
    });
  }
}

class _BannerWidget extends StatefulWidget {
  final String title, body;
  final Color color;
  final IconData icon;
  final VoidCallback onDismiss;
  const _BannerWidget({
    required this.title,
    required this.body,
    required this.color,
    required this.icon,
    required this.onDismiss,
  });
  @override
  State<_BannerWidget> createState() => _BannerWidgetState();
}

class _BannerWidgetState extends State<_BannerWidget>
    with SingleTickerProviderStateMixin {
  late AnimationController _ctrl;
  late Animation<Offset> _slide;
  late Animation<double> _fade;

  @override
  void initState() {
    super.initState();
    _ctrl = AnimationController(
        vsync: this, duration: const Duration(milliseconds: 380));
    _slide = Tween<Offset>(begin: const Offset(0, -1.2), end: Offset.zero)
        .animate(CurvedAnimation(parent: _ctrl, curve: Curves.easeOutBack));
    _fade = Tween<double>(begin: 0, end: 1)
        .animate(CurvedAnimation(parent: _ctrl, curve: Curves.easeOut));
    _ctrl.forward();
  }

  @override
  void dispose() {
    _ctrl.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Positioned(
      top: MediaQuery.of(context).padding.top + 8,
      left: 16,
      right: 16,
      child: SlideTransition(
        position: _slide,
        child: FadeTransition(
          opacity: _fade,
          child: Material(
            elevation: 12,
            borderRadius: BorderRadius.circular(16),
            color: isDark ? AppColors.darkCardBg : Colors.white,
            child: Container(
              decoration: BoxDecoration(
                borderRadius: BorderRadius.circular(16),
                border: Border.all(color: widget.color.withOpacity(0.25), width: 1.5),
                boxShadow: [
                  BoxShadow(
                    color: widget.color.withOpacity(0.25),
                    blurRadius: 20,
                    offset: const Offset(0, 6),
                  ),
                ],
              ),
              child: Padding(
                padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 12),
                child: Row(children: [
                  Container(
                    width: 40,
                    height: 40,
                    decoration: BoxDecoration(
                      color: widget.color.withOpacity(0.12),
                      borderRadius: BorderRadius.circular(12),
                    ),
                    child: Icon(widget.icon, color: widget.color, size: 20),
                  ),
                  const SizedBox(width: 12),
                  Expanded(
                    child: Column(
                      crossAxisAlignment: CrossAxisAlignment.start,
                      mainAxisSize: MainAxisSize.min,
                      children: [
                        Text(
                          widget.title,
                          style: TextStyle(
                            color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
                            fontSize: 13.5,
                            fontWeight: FontWeight.w800,
                          ),
                        ),
                        if (widget.body.isNotEmpty) ...[
                          const SizedBox(height: 2),
                          Text(
                            widget.body,
                            style: TextStyle(
                              color: isDark ? AppColors.darkTextSecondary : Colors.grey.shade600,
                              fontSize: 12.5,
                            ),
                            maxLines: 2,
                            overflow: TextOverflow.ellipsis,
                          ),
                        ],
                      ],
                    ),
                  ),
                  const SizedBox(width: 6),
                  GestureDetector(
                    onTap: widget.onDismiss,
                    child: Container(
                      padding: const EdgeInsets.all(4),
                      decoration: BoxDecoration(
                        color: isDark ? AppColors.darkInputBg : Colors.grey.shade100,
                        shape: BoxShape.circle,
                      ),
                      child: Icon(Icons.close_rounded,
                          color: isDark ? AppColors.darkTextSecondary : Colors.grey.shade500, size: 14),
                    ),
                  ),
                ]),
              ),
            ),
          ),
        ),
      ),
    );
  }
}

// ==================== FIRESTORE SERVICE ====================
class FirestoreService {
  static fb_auth.User? get _user => _auth.currentUser;
  static String get _uid => _user?.uid ?? '';
  static String get _username {
    final displayName = _user?.displayName;
    if (displayName != null && displayName.isNotEmpty) return displayName;
    final email = _user?.email;
    if (email != null && email.isNotEmpty) return email.split('@').first;
    return 'User';
  }

  static const int maxActiveBorrows = 5;

  static Future<int> getActiveBorrowCount() async {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return 0;
    try {
      final snap = await _db
          .collection('exchanges')
          .where('holderId', isEqualTo: uid)
          .where('status', whereIn: ['active', 'pending_receipt'])
          .count()
          .get();
      return snap.count ?? 0;
    } catch (e) {
      debugPrint('getActiveBorrowCount error: $e');
      return 0;
    }
  }

  static Future<bool> canBorrowMore() async {
    final count = await getActiveBorrowCount();
    return count < maxActiveBorrows;
  }

  // ── In-memory rating cache to avoid repeated Firestore reads ──
  static final Map<String, Map<String, dynamic>> _ratingCache = {};
  static final Map<String, DateTime> _ratingCacheTime = {};
  static const _ratingCacheDuration = Duration(minutes: 5);

  static Future<Map<String, dynamic>> getUserRating(String userId) async {
    if (userId.isEmpty) return {'positive': 0, 'total': 0, 'percentage': 0, 'average': 0.0};

    // Return cached value if fresh
    final cacheTime = _ratingCacheTime[userId];
    if (cacheTime != null && DateTime.now().difference(cacheTime) < _ratingCacheDuration) {
      return _ratingCache[userId]!;
    }

    try {
      final results = await Future.wait([
        _db.collection('feedback').where('toUserId', isEqualTo: userId).where('isPositive', isEqualTo: true).count().get(),
        _db.collection('feedback').where('toUserId', isEqualTo: userId).count().get(),
      ]);
      final positive = results[0].count ?? 0;
      final total = results[1].count ?? 0;
      final percentage = total > 0 ? (positive / total * 100).round() : 0;
      final average = total > 0 ? (positive / total) : 0.0;
      final result = {'positive': positive, 'total': total, 'percentage': percentage, 'average': average};

      // Store in cache
      _ratingCache[userId] = result;
      _ratingCacheTime[userId] = DateTime.now();

      return result;
    } catch (e) {
      debugPrint('getUserRating error: $e');
      return {'positive': 0, 'total': 0, 'percentage': 0, 'average': 0.0};
    }
  }

  // Public stream of a user's feedback — for public profile view
  static Stream<QuerySnapshot> streamUserFeedback(String userId) {
    if (userId.isEmpty) return const Stream.empty();
    return _db
        .collection('feedback')
        .where('toUserId', isEqualTo: userId)
        .orderBy('createdAt', descending: true)
        .snapshots()
        .handleError((e) => debugPrint('streamUserFeedback error: $e'));
  }

  static Stream<QuerySnapshot> streamMyToys() {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return const Stream.empty();
    return _db
        .collection('toys')
        .where('ownerId', isEqualTo: uid)
        .orderBy('createdAt', descending: true)
        .snapshots()
        .handleError((e) => debugPrint('streamMyToys error: $e'));
  }

  static Stream<QuerySnapshot> streamAvailableToys() {
    return _db
        .collection('toys')
        .where('isAvailable', isEqualTo: true)
        .orderBy('createdAt', descending: true)
        .limit(50) // Load max 50 at a time for speed
        .snapshots()
        .handleError((e) => debugPrint('streamAvailableToys error: $e'));
  }

  static Future<void> addToy(Map<String, dynamic> data) async {
    await _db.collection('toys').add({
      ...data,
      'ownerId': _uid,
      'ownerName': _username,
      'isAvailable': true,
      'createdAt': FieldValue.serverTimestamp(),
    });
  }

  static Future<void> deleteToy(String docId) async {
    await _db.collection('toys').doc(docId).delete();
  }

  static Stream<QuerySnapshot> streamRequestsReceived() {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return const Stream.empty();
    return _db
        .collection('requests')
        .where('ownerId', isEqualTo: uid)
        .where('status', isEqualTo: 'pending')
        .orderBy('createdAt', descending: true)
        .snapshots()
        .handleError((e) => debugPrint('streamRequestsReceived error: $e'));
  }

  static Stream<bool> streamHasRequested(String toyDocId) {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return Stream.value(false);
    return _db
        .collection('requests')
        .where('toyId', isEqualTo: toyDocId)
        .where('requesterId', isEqualTo: uid)
        .where('status', isEqualTo: 'pending')
        .snapshots()
        .map((s) => s.docs.isNotEmpty)
        .handleError((_) => false);
  }

  static Future<void> requestToy({
    required String toyDocId,
    required String toyName,
    required String ownerUid,
    required String message,
  }) async {
    if (ownerUid == _uid) throw Exception('Cannot request your own toy');

    // ── Prevent duplicate pending requests ──
    final existing = await _db
        .collection('requests')
        .where('toyId', isEqualTo: toyDocId)
        .where('requesterId', isEqualTo: _uid)
        .where('status', isEqualTo: 'pending')
        .limit(1)
        .get();
    if (existing.docs.isNotEmpty) {
      throw Exception('already_requested');
    }

    final canBorrow = await canBorrowMore();
    if (!canBorrow) {
      throw Exception('You have reached the maximum limit of $maxActiveBorrows active borrows. Please return some toys first.');
    }

    await _db.collection('requests').add({
      'toyId': toyDocId,
      'toyName': toyName,
      'ownerId': ownerUid,
      'requesterId': _uid,
      'requesterName': _username,
      'status': 'pending',
      'message': message.trim(),
      'createdAt': FieldValue.serverTimestamp(),
    });

    await _createNotification(
      userId: ownerUid,
      title: '📦 New Request',
      body: '$_username wants to borrow your toy "$toyName".',
      type: 'request_received',
    );
  }

  static Future<void> respondToRequest({
    required String requestDocId,
    required String toyDocId,
    required bool accept,
  }) async {
    final reqSnap = await _db.collection('requests').doc(requestDocId).get();
    if (!reqSnap.exists) throw Exception('Request not found');
    final req = reqSnap.data()!;

    if (accept) {
      final toySnap = await _db.collection('toys').doc(toyDocId).get();
      if (!toySnap.exists) throw Exception('Toy not found');
      final toy = toySnap.data()!;

      final batch = _db.batch();

      batch.update(_db.collection('requests').doc(requestDocId), {'status': 'accepted'});
      batch.update(_db.collection('toys').doc(toyDocId), {
        'isAvailable': false,
        'toyStatus': 'exchanged',
        'currentHolderId': req['requesterId'],
        'currentHolderName': req['requesterName'],
      });

      final exchangeRef = _db.collection('exchanges').doc();
      batch.set(exchangeRef, {
        'toyId': toyDocId,
        'toyName': req['toyName'] ?? '',
        'ownerId': _uid,
        'ownerName': _username,
        'holderId': req['requesterId'],
        'holderName': req['requesterName'],
        'type': toy['type'] ?? 'exchange',
        'status': 'active',
        'receiveDate': toy['receiveDate'],
        'returnDate': toy['returnDate'],
        'startedAt': FieldValue.serverTimestamp(),
        'createdAt': FieldValue.serverTimestamp(),
        'disputeId': null,
        'disputeReason': null,
      });

      await batch.commit();

      await _createNotification(
        userId: req['requesterId'],
        title: '🎉 Request Accepted!',
        body: '$_username accepted your request for "${req['toyName']}".',
        type: 'request_accepted',
      );

      final others = await _db
          .collection('requests')
          .where('toyId', isEqualTo: toyDocId)
          .where('status', isEqualTo: 'pending')
          .get();

      for (final doc in others.docs) {
        if (doc.id == requestDocId) continue;
        await doc.reference.update({'status': 'rejected'});
        final requesterId = doc.data()['requesterId'];
        if (requesterId != null) {
          await _createNotification(
            userId: requesterId,
            title: '❌ Request Declined',
            body: 'Your request for "${req['toyName']}" was not accepted.',
            type: 'request_rejected',
          );
        }
      }
    } else {
      await _db
          .collection('requests')
          .doc(requestDocId)
          .update({'status': 'rejected'});
      final requesterId = req['requesterId'];
      if (requesterId != null) {
        await _createNotification(
          userId: requesterId,
          title: '❌ Request Declined',
          body: '$_username declined your request for "${req['toyName']}".',
          type: 'request_rejected',
        );
      }
    }
  }

  static Stream<QuerySnapshot> streamExchangesOwned() {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return const Stream.empty();
    return _db
        .collection('exchanges')
        .where('ownerId', isEqualTo: uid)
        .orderBy('createdAt', descending: true)
        .snapshots()
        .handleError((e) => debugPrint('streamExchangesOwned error: $e'));
  }

  static Stream<QuerySnapshot> streamExchangesHeld() {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return const Stream.empty();
    return _db
        .collection('exchanges')
        .where('holderId', isEqualTo: uid)
        .orderBy('createdAt', descending: true)
        .snapshots()
        .handleError((e) => debugPrint('streamExchangesHeld error: $e'));
  }

  // ── Return date reminder — called on app open ──
  static Future<void> checkReturnDateReminders() async {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return;

    final today = DateTime.now();
    final todayStart = DateTime(today.year, today.month, today.day);

    try {
      // Check exchanges where user is the borrower (holder)
      final holderSnap = await _db
          .collection('exchanges')
          .where('holderId', isEqualTo: uid)
          .where('status', isEqualTo: 'active')
          .get();

      for (final doc in holderSnap.docs) {
        final data = doc.data();
        final returnDateStr = data['returnDate'] as String?;
        if (returnDateStr == null) continue;
        final returnDate = DateTime.tryParse(returnDateStr);
        if (returnDate == null) continue;
        final now2 = DateTime.now();
        final diff = returnDate.difference(now2);
        final daysLeft = returnDate.difference(todayStart).inDays;
        final toyName = data['toyName'] ?? 'A toy';
        final ownerName = data['ownerName'] ?? 'the owner';
        final durationMode = data['durationMode'] ?? 'days';

        // Avoid duplicate notifications — check if one was already sent today
        final existingToday = await _db
            .collection('notifications')
            .where('userId', isEqualTo: uid)
            .where('relatedId', isEqualTo: doc.id)
            .where('type', whereIn: ['return_reminder', 'return_overdue'])
            .orderBy('createdAt', descending: true)
            .limit(1)
            .get();

        if (existingToday.docs.isNotEmpty) {
          final lastTs = existingToday.docs.first.data()['createdAt'] as Timestamp?;
          if (lastTs != null) {
            final lastDate = lastTs.toDate();
            final lastDay = DateTime(lastDate.year, lastDate.month, lastDate.day);
            if (lastDay == todayStart) continue; // already notified today
          }
        }

        if (daysLeft < 0) {
          await _createNotification(
            userId: uid,
            title: '⚠️ Overdue Return',
            body: durationMode == 'hours'
                ? '"$toyName" return time has passed. Please return it to $ownerName.'
                : '"$toyName" was due ${daysLeft.abs()} day(s) ago. Please return it to $ownerName.',
            type: 'return_overdue',
            relatedId: doc.id,
          );
          final ownerUid = data['ownerId'] as String?;
          if (ownerUid != null && ownerUid.isNotEmpty) {
            final holderName = data['holderName'] ?? 'The borrower';
            await _createNotification(
              userId: ownerUid,
              title: '⚠️ Return Overdue',
              body: durationMode == 'hours'
                  ? '"$toyName" lent to $holderName is overdue.'
                  : '"$toyName" lent to $holderName is ${daysLeft.abs()} day(s) overdue.',
              type: 'return_overdue',
              relatedId: doc.id,
            );
          }
        } else if (durationMode == 'hours' && diff.inMinutes <= 30 && diff.inMinutes > 0) {
          await _createNotification(
            userId: uid,
            title: '⏰ Return Due Soon',
            body: '"$toyName" is due back to $ownerName in ${diff.inMinutes} minutes.',
            type: 'return_reminder',
            relatedId: doc.id,
          );
        } else if (daysLeft == 0) {
          await _createNotification(
            userId: uid,
            title: '📅 Return Due Today',
            body: '"$toyName" is due back to $ownerName today. Please initiate the return.',
            type: 'return_reminder',
            relatedId: doc.id,
          );
        } else if (daysLeft == 1) {
          await _createNotification(
            userId: uid,
            title: '⏰ Return Due Tomorrow',
            body: '"$toyName" is due back to $ownerName tomorrow.',
            type: 'return_reminder',
            relatedId: doc.id,
          );
        } else if (daysLeft <= 3) {
          await _createNotification(
            userId: uid,
            title: '📅 Return Reminder',
            body: '"$toyName" is due back in $daysLeft days.',
            type: 'return_reminder',
            relatedId: doc.id,
          );
        }
      }
    } catch (e) {
      debugPrint('checkReturnDateReminders error: $e');
    }
  }

  static Future<void> returnToy({    required String exchangeDocId,
    required String toyDocId,
    required String ownerUid,
    required String toyName,
  }) async {
    final batch = _db.batch();
    batch.update(_db.collection('exchanges').doc(exchangeDocId), {
      'status': 'pending_receipt',
      'returnInitiatedAt': FieldValue.serverTimestamp(),
    });
    batch.update(_db.collection('toys').doc(toyDocId), {'toyStatus': 'pending_receipt'});
    await batch.commit();

    await _createNotification(
      userId: ownerUid,
      title: '🔄 Toy Being Returned',
      body: '$_username has initiated the return of "$toyName". Please confirm receipt.',
      type: 'toy_returned',
      relatedId: exchangeDocId,
    );
  }

  static Future<void> confirmToyReceipt({
    required String exchangeDocId,
    required String toyDocId,
    required String holderUid,
    required String toyName,
  }) async {
    final batch = _db.batch();
    batch.update(_db.collection('exchanges').doc(exchangeDocId), {
      'status': 'completed',
      'completedAt': FieldValue.serverTimestamp()
    });
    batch.update(_db.collection('toys').doc(toyDocId), {
      'isAvailable': true,
      'toyStatus': 'available',
      'currentHolderId': FieldValue.delete(),
      'currentHolderName': FieldValue.delete(),
    });
    await batch.commit();

    await _createNotification(
      userId: holderUid,
      title: '✅ Return Confirmed',
      body: 'The owner has confirmed receipt of "$toyName". Exchange complete!',
      type: 'request_accepted',
      relatedId: exchangeDocId,
    );
  }

  static Future<void> resolveDispute({
    required String disputeDocId,
    required String exchangeDocId,
    required String toyDocId,
    required String resolution,
  }) async {
    final batch = _db.batch();
    batch.update(_db.collection('disputes').doc(disputeDocId), {
      'status': 'resolved',
      'resolution': resolution,
      'resolvedAt': FieldValue.serverTimestamp(),
    });
    batch.update(_db.collection('exchanges').doc(exchangeDocId), {
      'status': 'completed',
    });
    batch.update(_db.collection('toys').doc(toyDocId), {
      'isAvailable': true,
      'toyStatus': 'available',
      'currentHolderId': FieldValue.delete(),
      'currentHolderName': FieldValue.delete(),
    });
    await batch.commit();
  }

  static Stream<QuerySnapshot> streamToyHistory(String toyId) {
    return _db
        .collection('exchanges')
        .where('toyId', isEqualTo: toyId)
        .orderBy('createdAt', descending: true)
        .snapshots()
        .handleError((e) => debugPrint('streamToyHistory error: $e'));
  }

  static Future<void> reportDispute({
    required String exchangeDocId,
    required String toyDocId,
    required String otherPartyId,
    required String otherPartyName,
    required String reason,
  }) async {
    if (exchangeDocId.isEmpty) throw Exception('Invalid exchange ID');
    final batch = _db.batch();
    final disputeRef = _db.collection('disputes').doc();
    batch.set(disputeRef, {
      'exchangeId': exchangeDocId,
      'reporterId': _uid,
      'reporterName': _username,
      'otherPartyId': otherPartyId,
      'otherPartyName': otherPartyName,
      'reason': reason.trim(),
      'status': 'open',
      'createdAt': FieldValue.serverTimestamp(),
      'resolvedAt': null,
    });
    batch.update(_db.collection('exchanges').doc(exchangeDocId), {
      'status': 'disputed',
      'disputeId': disputeRef.id,
      'disputeReason': reason.trim(),
    });
    // Only update toy doc if a valid toyDocId was provided
    if (toyDocId.isNotEmpty) {
      batch.update(_db.collection('toys').doc(toyDocId), {
        'toyStatus': 'disputed',
      });
    }
    await batch.commit();

    // Only notify if we have a valid recipient
    if (otherPartyId.isNotEmpty) {
      await _createNotification(
        userId: otherPartyId,
        title: '⚠️ Dispute Reported',
        body: '$_username has reported an issue with the exchange. Please check your exchanges.',
        type: 'dispute_opened',
        relatedId: exchangeDocId,
      );
    }
  }

  static Stream<QuerySnapshot> streamDisputes() {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return const Stream.empty();
    return _db
        .collection('disputes')
        .where('reporterId', isEqualTo: uid)
        .orderBy('createdAt', descending: true)
        .snapshots()
        .handleError((e) => debugPrint('streamDisputes error: $e'));
  }

  static Stream<QuerySnapshot> streamDisputesAgainstMe() {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return const Stream.empty();
    return _db
        .collection('disputes')
        .where('otherPartyId', isEqualTo: uid)
        .orderBy('createdAt', descending: true)
        .snapshots()
        .handleError((e) => debugPrint('streamDisputesAgainstMe error: $e'));
  }

  static Stream<int> streamUnreadMessageCount(String exchangeId) {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return Stream.value(0);
    return _db
        .collection('messages')
        .where('exchangeId', isEqualTo: exchangeId)
        .where('receiverId', isEqualTo: uid)
        .where('isRead', isEqualTo: false)
        .snapshots()
        .map((s) => s.docs.length)
        .handleError((e) {
          debugPrint('streamUnreadMessageCount error: $e');
          return 0;
        });
  }

  static Future<void> markMessagesRead(String exchangeId) async {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return;
    try {
      final unread = await _db
          .collection('messages')
          .where('exchangeId', isEqualTo: exchangeId)
          .where('receiverId', isEqualTo: uid)
          .where('isRead', isEqualTo: false)
          .get();
      if (unread.docs.isEmpty) return;
      final batch = _db.batch();
      for (final doc in unread.docs) {
        batch.update(doc.reference, {'isRead': true});
      }
      await batch.commit();
    } catch (e) {
      debugPrint('markMessagesRead error: $e');
    }
  }

  static Stream<QuerySnapshot> streamMessages(String exchangeId) {
    if (exchangeId.isEmpty) return const Stream.empty();
    return _db
        .collection('messages')
        .where('exchangeId', isEqualTo: exchangeId)
        .orderBy('createdAt', descending: false)
        .limit(100)
        .snapshots()
        .handleError((e) => debugPrint('streamMessages error: $e'));
  }

  static Future<void> sendMessage({
    required String exchangeId,
    required String text,
    String receiverId = '',
    String receiverName = '',
  }) async {
    if (text.trim().isEmpty) return;
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) throw Exception('Not logged in');
    await _db.collection('messages').add({
      'exchangeId': exchangeId,
      'senderId': uid,
      'senderName': _username,
      'receiverId': receiverId,
      'text': text.trim(),
      'isRead': false,
      'createdAt': FieldValue.serverTimestamp(),
    });

    // Send in-app notification to receiver
    if (receiverId.isNotEmpty) {
      await _createNotification(
        userId: receiverId,
        title: '💬 New Message from $_username',
        body: text.trim().length > 60 ? '${text.trim().substring(0, 60)}…' : text.trim(),
        type: 'new_message',
        relatedId: exchangeId,
      );
    }
  }

  static Stream<QuerySnapshot> streamNotifications() {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return const Stream.empty();
    return _db
        .collection('notifications')
        .where('userId', isEqualTo: uid)
        .orderBy('createdAt', descending: true)
        .limit(50)
        .snapshots()
        .handleError((e) => debugPrint('streamNotifications error: $e'));
  }

  static Future<void> _createNotification({
    required String userId,
    required String title,
    required String body,
    required String type,
    String? relatedId,
  }) async {
    if (userId.isEmpty) return;
    try {
      await _db.collection('notifications').add({
        'userId': userId,
        'title': title,
        'body': body,
        'type': type,
        'relatedId': relatedId ?? '',
        'isRead': false,
        'createdAt': FieldValue.serverTimestamp(),
      });
    } catch (e) {
      debugPrint('createNotification error: $e');
    }
  }

  static Future<void> markAllRead() async {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return;

    final unread = await _db
        .collection('notifications')
        .where('userId', isEqualTo: uid)
        .where('isRead', isEqualTo: false)
        .get();

    if (unread.docs.isEmpty) return;

    const chunkSize = 500;
    final totalDocs = unread.docs.length;
    for (var i = 0; i < totalDocs; i += chunkSize) {
      final end = (i + chunkSize < totalDocs) ? i + chunkSize : totalDocs;
      final chunk = unread.docs.sublist(i, end);
      final batch = _db.batch();
      for (final doc in chunk) {
        batch.update(doc.reference, {'isRead': true});
      }
      await batch.commit();
    }
  }

  static Future<void> markRead(String docId) async {
    await _db.collection('notifications').doc(docId).update({'isRead': true});
  }

  static Future<Map<String, int>> getDashboardStats() async {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return {'toys': 0, 'exchanges': 0, 'requests': 0};
    try {
      final results = await Future.wait([
        _db.collection('toys').where('ownerId', isEqualTo: uid).count().get(),
        _db.collection('exchanges')
            .where('ownerId', isEqualTo: uid)
            .where('status', whereIn: ['active', 'pending_receipt'])
            .count().get(),
        _db.collection('requests')
            .where('ownerId', isEqualTo: uid)
            .where('status', isEqualTo: 'pending')
            .count().get(),
      ]);
      return {
        'toys': results[0].count ?? 0,
        'exchanges': results[1].count ?? 0,
        'requests': results[2].count ?? 0,
      };
    } catch (e) {
      debugPrint('getDashboardStats error: $e');
      return {'toys': 0, 'exchanges': 0, 'requests': 0};
    }
  }

  static Future<void> blacklistUser(String targetUid, String targetName) async {
    if (targetUid.isEmpty) return;
    await _db
        .collection('blacklist')
        .doc('${_uid}_$targetUid')
        .set({
      'userId': _uid,
      'blockedUserId': targetUid,
      'blockedUsername': targetName,
      'createdAt': FieldValue.serverTimestamp(),
    });
  }

  static Future<void> removeFromBlacklist(String targetUid) async {
    await _db.collection('blacklist').doc('${_uid}_$targetUid').delete();
  }

  static Future<List<DocumentSnapshot>> getBlacklistedUsers() async {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return [];
    try {
      final snap = await _db
          .collection('blacklist')
          .where('userId', isEqualTo: uid)
          .orderBy('createdAt', descending: true)
          .get();
      return snap.docs;
    } catch (e) {
      debugPrint('getBlacklistedUsers error: $e');
      return [];
    }
  }

  static Future<void> giveFeedback({
    required String targetUserId,
    required bool isPositive,
    required String comment,
    required String exchangeId,
    String toyName = '',
    int rating = 5,
  }) async {
    if (targetUserId.isEmpty) return;
    // Invalidate cache for this user so badge refreshes
    _ratingCache.remove(targetUserId);
    _ratingCacheTime.remove(targetUserId);
    await _db.collection('feedback').add({
      'fromUserId': _uid,
      'fromUserName': _username,
      'toUserId': targetUserId,
      'exchangeId': exchangeId,
      'toyName': toyName,
      'isPositive': isPositive,
      'rating': rating,
      'comment': comment.trim(),
      'createdAt': FieldValue.serverTimestamp(),
    });
  }

  static Future<Map<String, int>> getProfileStats() async {
    final uid = _auth.currentUser?.uid;
    if (uid == null || uid.isEmpty) return {'toys': 0, 'exchanges': 0, 'feedback': 0};

    try {
      final results = await Future.wait([
        _db.collection('toys').where('ownerId', isEqualTo: uid).count().get(),
        _db.collection('exchanges').where('ownerId', isEqualTo: uid).where('status', isEqualTo: 'completed').count().get(),
        _db
            .collection('feedback')
            .where('toUserId', isEqualTo: uid)
            .where('isPositive', isEqualTo: true)
            .count()
            .get(),
      ]);

      return {
        'toys': results[0].count ?? 0,
        'exchanges': results[1].count ?? 0,
        'feedback': results[2].count ?? 0,
      };
    } catch (e) {
      debugPrint('getProfileStats error: $e');
      return {'toys': 0, 'exchanges': 0, 'feedback': 0};
    }
  }
}

// ==================== SHARED WIDGETS ====================
class AppTextField extends StatelessWidget {
  final TextEditingController controller;
  final String label;
  final IconData? prefixIcon;
  final IconData? faIcon;
  final bool obscureText;
  final String? Function(String?)? validator;
  final TextInputType? keyboardType;
  final int maxLines;
  final String? hintText;
  final Widget? suffixIcon;
  final List<TextInputFormatter>? inputFormatters;

  const AppTextField({
    super.key,
    required this.controller,
    required this.label,
    this.prefixIcon,
    this.faIcon,
    this.obscureText = false,
    this.validator,
    this.keyboardType,
    this.maxLines = 1,
    this.hintText,
    this.suffixIcon,
    this.inputFormatters,
  });

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final iconColor = isDark ? AppColors.darkTextSecondary : AppColors.textSecondary;
    final labelColor = isDark ? AppColors.darkTextSecondary : AppColors.textSecondary;
    final fillColor = isDark ? AppColors.darkInputBg : const Color(0xFFF8F8FC);
    final borderColor = isDark ? AppColors.darkBorder : const Color(0xFFE2E8F0);
    return TextFormField(
      controller: controller,
      obscureText: obscureText,
      validator: validator,
      keyboardType: keyboardType,
      maxLines: maxLines,
      inputFormatters: inputFormatters,
      style: TextStyle(
        fontSize: 14.5,
        color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
      ),
      decoration: InputDecoration(
        labelText: label,
        hintText: hintText,
        labelStyle: TextStyle(color: labelColor, fontSize: 14),
        hintStyle: TextStyle(color: labelColor.withOpacity(0.6), fontSize: 14),
        filled: true,
        fillColor: fillColor,
        prefixIcon: faIcon != null
            ? Padding(
                padding: const EdgeInsets.all(12),
                child: FaIcon(faIcon!, size: 20, color: iconColor),
              )
            : (prefixIcon != null
                ? Icon(prefixIcon, size: 20, color: iconColor)
                : null),
        suffixIcon: suffixIcon,
        border: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: BorderSide(color: borderColor),
        ),
        enabledBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: BorderSide(color: borderColor),
        ),
        focusedBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: AppColors.primary, width: 2),
        ),
        errorBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: AppColors.error),
        ),
        focusedErrorBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: const BorderSide(color: AppColors.error, width: 2),
        ),
        contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 16),
      ),
    );
  }
}

class ToyChip extends StatelessWidget {
  final String label;
  final Color color;
  final Color textColor;
  final IconData? icon;
  const ToyChip({
    super.key,
    required this.label,
    required this.color,
    required this.textColor,
    this.icon,
  });

  @override
  Widget build(BuildContext context) {
    return ConstrainedBox(
      constraints: const BoxConstraints(maxWidth: 160),
      child: Container(
        padding: EdgeInsets.symmetric(horizontal: icon != null ? 8 : 10, vertical: 5),
        decoration: BoxDecoration(color: color, borderRadius: BorderRadius.circular(24)),
        child: Row(mainAxisSize: MainAxisSize.min, children: [
          if (icon != null) ...[
            Icon(icon, size: 11, color: textColor),
            const SizedBox(width: 4),
          ],
          Flexible(
            child: Text(
              label,
              maxLines: 1,
              overflow: TextOverflow.ellipsis,
              style: TextStyle(
                fontSize: 11,
                fontWeight: FontWeight.w700,
                color: textColor,
                letterSpacing: 0.3,
              ),
            ),
          ),
        ]),
      ),
    );
  }
}

class UserAvatar extends StatelessWidget {
  final String name;
  final double size;
  final List<Color>? gradient;
  const UserAvatar({super.key, required this.name, this.size = 44, this.gradient});

  @override
  Widget build(BuildContext context) {
    final colors = gradient ?? AppColors.heroGradient;
    return Container(
      width: size,
      height: size,
      decoration: BoxDecoration(
        gradient: LinearGradient(
          colors: colors,
          begin: Alignment.topLeft,
          end: Alignment.bottomRight,
        ),
        shape: BoxShape.circle,
        boxShadow: [
          BoxShadow(
            color: colors.first.withOpacity(0.3),
            blurRadius: 8,
            offset: const Offset(0, 2),
          ),
        ],
      ),
      child: Center(
        child: Text(
          name.isNotEmpty ? name[0].toUpperCase() : '?',
          style: TextStyle(
            color: Colors.white,
            fontWeight: FontWeight.w800,
            fontSize: size * 0.4,
            letterSpacing: -0.5,
          ),
        ),
      ),
    );
  }
}

class UserRatingBadge extends StatelessWidget {
  final String userId;
  final double size;
  final bool showStars;
  final bool lightMode;

  const UserRatingBadge({
    super.key,
    required this.userId,
    this.size = 14,
    this.showStars = true,
    this.lightMode = false,
  });

  @override
  Widget build(BuildContext context) {
    if (userId.isEmpty) return const SizedBox.shrink();
    return FutureBuilder<Map<String, dynamic>>(
      future: FirestoreService.getUserRating(userId),
      builder: (context, snap) {
        if (!snap.hasData) return const SizedBox.shrink();
        final data = snap.data!;
        final total = data['total'] as int;
        if (total == 0) return const SizedBox.shrink();
        final percentage = data['percentage'] as int;
        final positive = data['positive'] as int;
        final average = (data['average'] as double? ?? 0.0) * 5;
        final color = _getRatingColor(percentage);
        return Container(
          padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 3),
          decoration: BoxDecoration(
            color: lightMode ? Colors.white.withOpacity(0.2) : color.withOpacity(0.12),
            borderRadius: BorderRadius.circular(12),
            border: Border.all(color: lightMode ? Colors.white.withOpacity(0.3) : color.withOpacity(0.2)),
          ),
          child: Row(mainAxisSize: MainAxisSize.min, children: [
            if (showStars) ...[
              Icon(_getStarIcon(average), size: size, color: lightMode ? Colors.white : color),
              const SizedBox(width: 4),
            ],
            Text(
              '$positive/$total ($percentage%)',
              style: TextStyle(
                fontSize: size - 1,
                fontWeight: FontWeight.w800,
                color: lightMode ? Colors.white : color,
              ),
            ),
          ]),
        );
      },
    );
  }

  Color _getRatingColor(int percentage) {
    if (percentage >= 90) return AppColors.success;
    if (percentage >= 70) return AppColors.accent;
    if (percentage >= 50) return AppColors.warning;
    return AppColors.error;
  }

  IconData _getStarIcon(double average) {
    if (average >= 4.5) return Icons.star_rounded;
    if (average >= 3.5) return Icons.star_half_rounded;
    return Icons.star_border_rounded;
  }
}

Widget sectionLabel(String label, {Color? color}) => Padding(
  padding: const EdgeInsets.only(bottom: 12),
  child: Row(children: [
    Container(
      width: 3.5,
      height: 16,
      decoration: BoxDecoration(
        color: color ?? AppColors.primary,
        borderRadius: BorderRadius.circular(2),
      ),
    ),
    const SizedBox(width: 8),
    Text(
      label,
      style: TextStyle(
        fontSize: 13,
        fontWeight: FontWeight.w800,
        color: color ?? AppColors.primary,
        letterSpacing: 0.5,
      ),
    ),
  ]),
);

Widget emptyState({
  required IconData icon,
  required String title,
  String? subtitle,
  Color? color,
}) {
  final c = color ?? Colors.grey.shade300;
  return Center(
    child: Padding(
      padding: const EdgeInsets.all(40),
      child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [
        Container(
          width: 80,
          height: 80,
          decoration: BoxDecoration(
            color: c.withOpacity(0.15),
            shape: BoxShape.circle,
          ),
          child: Icon(icon, size: 36, color: c),
        ),
        const SizedBox(height: 20),
        Text(
          title,
          style: TextStyle(
            fontSize: 17,
            fontWeight: FontWeight.w700,
            color: Colors.grey.shade500,
          ),
        ),
        if (subtitle != null) ...[
          const SizedBox(height: 6),
          Text(
            subtitle,
            style: TextStyle(fontSize: 13, color: Colors.grey.shade400),
            textAlign: TextAlign.center,
          ),
        ],
      ]),
    ),
  );
}

Widget errorState({required String message, VoidCallback? onRetry}) {
  return Center(
    child: Padding(
      padding: const EdgeInsets.all(40),
      child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [
        Container(
          width: 80,
          height: 80,
          decoration: BoxDecoration(
            color: AppColors.error.withOpacity(0.1),
            shape: BoxShape.circle,
          ),
          child: const Icon(Icons.error_outline_rounded, size: 36, color: AppColors.error),
        ),
        const SizedBox(height: 20),
        const Text(
          'Something went wrong',
          style: TextStyle(fontSize: 17, fontWeight: FontWeight.w700, color: AppColors.textPrimary),
        ),
        const SizedBox(height: 6),
        Text(
          message,
          style: const TextStyle(fontSize: 13, color: AppColors.textSecondary),
          textAlign: TextAlign.center,
        ),
        if (onRetry != null) ...[
          const SizedBox(height: 20),
          ElevatedButton.icon(
            onPressed: onRetry,
            icon: const Icon(Icons.refresh_rounded, size: 18),
            label: const Text('Retry'),
            style: ElevatedButton.styleFrom(
              backgroundColor: AppColors.primary,
              foregroundColor: Colors.white,
              elevation: 0,
              shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
            ),
          ),
        ],
      ]),
    ),
  );
}

class _StatCardSkeleton extends StatefulWidget {
  const _StatCardSkeleton();
  @override
  State<_StatCardSkeleton> createState() => _StatCardSkeletonState();
}

class _StatCardSkeletonState extends State<_StatCardSkeleton>
    with SingleTickerProviderStateMixin {
  late AnimationController _ctrl;
  late Animation<double> _anim;

  @override
  void initState() {
    super.initState();
    _ctrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 900));
    _anim = Tween<double>(begin: 0.4, end: 1.0)
        .animate(CurvedAnimation(parent: _ctrl, curve: Curves.easeInOut));
    _ctrl.repeat(reverse: true);
  }

  @override
  void dispose() {
    _ctrl.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return AnimatedBuilder(
      animation: _anim,
      builder: (_, __) => Container(
        padding: const EdgeInsets.symmetric(vertical: 16, horizontal: 12),
        decoration: BoxDecoration(
          color: isDark ? AppColors.darkCardBg : Colors.white,
          borderRadius: BorderRadius.circular(18),
          boxShadow: AppShadows.card(context),
        ),
        child: Column(children: [
          Container(
            width: 38,
            height: 38,
            decoration: BoxDecoration(
              color: isDark ? Colors.grey.shade800.withOpacity(_anim.value) : Colors.grey.shade200.withOpacity(_anim.value),
              borderRadius: BorderRadius.circular(11),
            ),
          ),
          const SizedBox(height: 8),
          Container(
            width: 28,
            height: 16,
            decoration: BoxDecoration(
              color: isDark ? Colors.grey.shade800.withOpacity(_anim.value) : Colors.grey.shade200.withOpacity(_anim.value),
              borderRadius: BorderRadius.circular(4),
            ),
          ),
          const SizedBox(height: 4),
          Container(
            width: 44,
            height: 10,
            decoration: BoxDecoration(
              color: isDark ? Colors.grey.shade900.withOpacity(_anim.value) : Colors.grey.shade100.withOpacity(_anim.value),
              borderRadius: BorderRadius.circular(4),
            ),
          ),
        ]),
      ),
    );
  }
}

class ToyShareLogo extends StatefulWidget {
  final double size;
  final bool animate;
  final bool white;
  const ToyShareLogo({super.key, this.size = 80, this.animate = true, this.white = false});
  @override
  State<ToyShareLogo> createState() => _ToyShareLogoState();
}

class _ToyShareLogoState extends State<ToyShareLogo>
    with SingleTickerProviderStateMixin {
  late AnimationController _ctrl;
  late Animation<double> _bounce;

  @override
  void initState() {
    super.initState();
    _ctrl = AnimationController(
        vsync: this, duration: const Duration(milliseconds: 1800));
    _bounce = Tween<double>(begin: 0.97, end: 1.03)
        .animate(CurvedAnimation(parent: _ctrl, curve: Curves.easeInOut));
    if (widget.animate) _ctrl.repeat(reverse: true);
  }

  @override
  void dispose() {
    _ctrl.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final s = widget.size;
    final logo = Container(
      width: s,
      height: s,
      decoration: BoxDecoration(
        gradient: widget.white ? null : const LinearGradient(
          colors: [Color(0xFF5B4FCF), Color(0xFF8B5CF6)],
          begin: Alignment.topLeft,
          end: Alignment.bottomRight,
        ),
        color: widget.white ? Colors.transparent : null,
        borderRadius: BorderRadius.circular(s * 0.28),
        boxShadow: widget.white ? [] : [
          BoxShadow(
            color: const Color(0xFF5B4FCF).withOpacity(0.50),
            blurRadius: 18,
            offset: const Offset(0, 7),
            spreadRadius: -4,
          ),
        ],
      ),
      child: Stack(alignment: Alignment.center, children: [
        Positioned(
          top: -s * 0.04,
          right: -s * 0.04,
          child: Container(
            width: s * 0.5,
            height: s * 0.5,
            decoration: BoxDecoration(
              color: Colors.white.withOpacity(0.10),
              shape: BoxShape.circle,
            ),
          ),
        ),
        Column(mainAxisAlignment: MainAxisAlignment.center, children: [
          Row(mainAxisAlignment: MainAxisAlignment.center, children: [
            Container(
              width: s * 0.28,
              height: s * 0.28,
              decoration: BoxDecoration(
                color: const Color(0xFFFFE066),
                borderRadius: BorderRadius.circular(s * 0.07),
              ),
              child: Icon(Icons.star_rounded, color: const Color(0xFF5B4FCF), size: s * 0.18),
            ),
            SizedBox(width: s * 0.04),
            Icon(Icons.swap_horiz_rounded,
                color: Colors.white.withOpacity(0.9), size: s * 0.22),
            SizedBox(width: s * 0.04),
            Container(
              width: s * 0.28,
              height: s * 0.28,
              decoration: BoxDecoration(
                color: const Color(0xFF4ECDC4),
                borderRadius: BorderRadius.circular(s * 0.07),
              ),
              child: Icon(Icons.favorite_rounded, color: Colors.white, size: s * 0.18),
            ),
          ]),
          SizedBox(height: s * 0.06),
          Container(
            padding: EdgeInsets.symmetric(horizontal: s * 0.05, vertical: s * 0.02),
            decoration: BoxDecoration(
              color: Colors.white.withOpacity(0.18),
              borderRadius: BorderRadius.circular(s * 0.06),
            ),
            child: Text(
              'TOYSHARE',
              style: TextStyle(
                color: Colors.white,
                fontSize: s * 0.10,
                fontWeight: FontWeight.w900,
                letterSpacing: 0.5,
              ),
            ),
          ),
        ]),
      ]),
    );
    if (!widget.animate) return logo;
    return AnimatedBuilder(
      animation: _ctrl,
      builder: (_, child) => Transform.scale(scale: _bounce.value, child: child),
      child: logo,
    );
  }
}

// ==================== LOGIN SCREEN ====================
class LoginScreen extends StatefulWidget {
  const LoginScreen({super.key});
  @override
  State<LoginScreen> createState() => _LoginScreenState();
}

class _LoginScreenState extends State<LoginScreen> with TickerProviderStateMixin {
  final _formKey = GlobalKey<FormState>();
  final _emailCtrl = TextEditingController();
  final _passwordCtrl = TextEditingController();
  final _usernameCtrl = TextEditingController();
  bool _isLogin = true;
  bool _isLoading = false;
  bool _showPassword = false;
  bool _buttonHover = false;

  late AnimationController _fadeCtrl, _slideCtrl, _cardCtrl;
  late Animation<double> _fadeAnim, _cardFade;
  late Animation<Offset> _slideAnim, _cardSlide;

  @override
  void initState() {
    super.initState();
    _fadeCtrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 800));
    _slideCtrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 700));
    _cardCtrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 650));
    _fadeAnim = CurvedAnimation(parent: _fadeCtrl, curve: Curves.easeOut);
    _slideAnim = Tween<Offset>(begin: const Offset(0, 0.08), end: Offset.zero)
        .animate(CurvedAnimation(parent: _slideCtrl, curve: Curves.easeOutCubic));
    _cardFade = CurvedAnimation(parent: _cardCtrl, curve: Curves.easeOut);
    _cardSlide = Tween<Offset>(begin: const Offset(0, 0.05), end: Offset.zero)
        .animate(CurvedAnimation(parent: _cardCtrl, curve: Curves.easeOutCubic));
    _fadeCtrl.forward();
    Future.delayed(const Duration(milliseconds: 150), () {
      if (mounted) _slideCtrl.forward();
    });
    Future.delayed(const Duration(milliseconds: 300), () {
      if (mounted) _cardCtrl.forward();
    });
  }

  @override
  void dispose() {
    _fadeCtrl.dispose();
    _slideCtrl.dispose();
    _cardCtrl.dispose();
    _emailCtrl.dispose();
    _passwordCtrl.dispose();
    _usernameCtrl.dispose();
    super.dispose();
  }

  Future<void> _submit() async {
    if (!_formKey.currentState!.validate()) return;
    setState(() => _isLoading = true);
    try {
      if (_isLogin) {
        await _auth.signInWithEmailAndPassword(
          email: _emailCtrl.text.trim(),
          password: _passwordCtrl.text,
        );
      } else {
        final cred = await _auth.createUserWithEmailAndPassword(
          email: _emailCtrl.text.trim(),
          password: _passwordCtrl.text,
        );
        await cred.user?.updateDisplayName(_usernameCtrl.text.trim());
        await cred.user?.reload();
        final refreshedUser = _auth.currentUser;
        if (refreshedUser != null && refreshedUser.displayName == null) {
          await refreshedUser.updateDisplayName(_usernameCtrl.text.trim());
          await refreshedUser.reload();
        }
      }
    } on fb_auth.FirebaseAuthException catch (e) {
      if (mounted) _showError(_friendlyAuthError(e.code));
    } catch (e) {
      if (mounted) _showError('An error occurred. Please try again.');
    }
    if (mounted) setState(() => _isLoading = false);
  }

  String _friendlyAuthError(String code) {
    switch (code) {
      case 'user-not-found':
        return 'No account found for this email.';
      case 'wrong-password':
        return 'Incorrect password. Please try again.';
      case 'invalid-credential':
        return 'Invalid email or password.';
      case 'email-already-in-use':
        return 'An account already exists with this email.';
      case 'weak-password':
        return 'Password should be at least 6 characters.';
      case 'invalid-email':
        return 'Please enter a valid email address.';
      case 'too-many-requests':
        return 'Too many attempts. Please try again later.';
      case 'network-request-failed':
        return 'Network error. Check your connection.';
      default:
        return 'Authentication failed. Please try again.';
    }
  }

  void _showError(String msg) {
    if (!mounted) return;
    ScaffoldMessenger.of(context).showSnackBar(SnackBar(
      content: Row(children: [
        const Icon(Icons.warning_amber_rounded, color: Colors.white, size: 18),
        const SizedBox(width: 8),
        Expanded(child: Text(msg, style: const TextStyle(fontWeight: FontWeight.w600))),
      ]),
      backgroundColor: AppColors.error,
      margin: const EdgeInsets.all(14),
    ));
  }

  void _toggleMode() {
    setState(() {
      _isLogin = !_isLogin;
      _formKey.currentState?.reset();
    });
    _cardCtrl.forward(from: 0);
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        decoration: const BoxDecoration(
          gradient: LinearGradient(
            begin: Alignment.topLeft,
            end: Alignment.bottomRight,
            colors: [Color(0xFF3D32A8), Color(0xFF5B4FCF), Color(0xFF7B55D4)],
          ),
        ),
        child: SafeArea(
          child: FadeTransition(
            opacity: _fadeAnim,
            child: Center(
              child: SingleChildScrollView(
                padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 32),
                child: ConstrainedBox(
                  constraints: const BoxConstraints(maxWidth: 400),
                  child: Column(children: [
                    SlideTransition(
                      position: _slideAnim,
                      child: Column(children: [
                        const ToyShareLogo(size: 86),
                        const SizedBox(height: 18),
                        const Text(
                          'ToyShare',
                          style: TextStyle(
                            color: Colors.white,
                            fontSize: 32,
                            fontWeight: FontWeight.w900,
                            letterSpacing: -1,
                          ),
                        ),
                        const SizedBox(height: 5),
                        Text(
                          'Share joy, one toy at a time',
                          style: TextStyle(
                            color: Colors.white.withOpacity(0.65),
                            fontSize: 14,
                          ),
                        ),
                      ]),
                    ),
                    const SizedBox(height: 40),
                    SlideTransition(
                      position: _cardSlide,
                      child: FadeTransition(
                        opacity: _cardFade,
                        child: Container(
                          decoration: BoxDecoration(
                            color: Colors.white,
                            borderRadius: BorderRadius.circular(28),
                            boxShadow: [
                              BoxShadow(
                                color: Colors.black.withOpacity(0.22),
                                blurRadius: 48,
                                offset: const Offset(0, 20),
                                spreadRadius: -10,
                              ),
                            ],
                          ),
                          padding: const EdgeInsets.all(28),
                          child: Form(
                            key: _formKey,
                            child: Column(
                              crossAxisAlignment: CrossAxisAlignment.start,
                              children: [
                                Text(
                                  _isLogin ? 'Welcome back!' : 'Create account',
                                  style: const TextStyle(
                                    fontSize: 22,
                                    fontWeight: FontWeight.w900,
                                    letterSpacing: -0.7,
                                    color: AppColors.textPrimary,
                                  ),
                                ),
                                const SizedBox(height: 3),
                                Text(
                                  _isLogin
                                      ? 'Sign in to continue sharing'
                                      : 'Join your neighborhood toy circle',
                                  style: const TextStyle(
                                      fontSize: 13.5, color: AppColors.textSecondary),
                                ),
                                const SizedBox(height: 24),
                                if (!_isLogin) ...[
                                  AppTextField(
                                    controller: _usernameCtrl,
                                    label: 'Username',
                                    faIcon: AppIcons.person,
                                    validator: (v) => (v == null || v.trim().isEmpty)
                                        ? 'Please enter a username'
                                        : null,
                                  ),
                                  const SizedBox(height: 14),
                                ],
                                AppTextField(
                                  controller: _emailCtrl,
                                  label: 'Email',
                                  faIcon: AppIcons.email,
                                  keyboardType: TextInputType.emailAddress,
                                  validator: (v) => (v == null || v.trim().isEmpty)
                                      ? 'Please enter your email'
                                      : (!v.contains('@') ? 'Enter a valid email' : null),
                                ),
                                const SizedBox(height: 14),
                                AppTextField(
                                  controller: _passwordCtrl,
                                  label: 'Password',
                                  faIcon: AppIcons.password,
                                  obscureText: !_showPassword,
                                  suffixIcon: IconButton(
                                    icon: Icon(
                                      _showPassword
                                          ? Icons.visibility_off_outlined
                                          : Icons.visibility_outlined,
                                      size: 20,
                                      color: AppColors.textSecondary,
                                    ),
                                    onPressed: () =>
                                        setState(() => _showPassword = !_showPassword),
                                  ),
                                  validator: (v) => (v == null || v.isEmpty)
                                      ? 'Please enter your password'
                                      : (v.length < 6
                                      ? 'Password must be at least 6 characters'
                                      : null),
                                ),
                                const SizedBox(height: 24),
                                MouseRegion(
                                  cursor: SystemMouseCursors.click,
                                  onEnter: (_) => setState(() => _buttonHover = true),
                                  onExit: (_) => setState(() => _buttonHover = false),
                                  child: AnimatedContainer(
                                    duration: const Duration(milliseconds: 200),
                                    decoration: BoxDecoration(
                                      borderRadius: BorderRadius.circular(14),
                                      boxShadow: _isLoading
                                          ? []
                                          : (_buttonHover
                                          ? [
                                        BoxShadow(
                                          color: AppColors.primary.withOpacity(0.5),
                                          blurRadius: 20,
                                          offset: const Offset(0, 6),
                                        )
                                      ]
                                          : AppShadows.button),
                                    ),
                                    child: ElevatedButton(
                                      onPressed: _isLoading ? null : _submit,
                                      style: ElevatedButton.styleFrom(
                                        backgroundColor: AppColors.primary,
                                        foregroundColor: Colors.white,
                                        shape: RoundedRectangleBorder(
                                            borderRadius: BorderRadius.circular(14)),
                                        minimumSize: const Size(double.infinity, 54),
                                        elevation: 0,
                                      ),
                                      child: _isLoading
                                          ? const SizedBox(
                                        width: 22,
                                        height: 22,
                                        child: CircularProgressIndicator(
                                            strokeWidth: 2.5,
                                            color: Colors.white),
                                      )
                                          : Text(
                                        _isLogin ? 'Sign In' : 'Create Account',
                                        style: const TextStyle(
                                            fontSize: 16, fontWeight: FontWeight.w800),
                                      ),
                                    ),
                                  ),
                                ),
                                const SizedBox(height: 16),
                                Center(
                                  child: TextButton(
                                    onPressed: _toggleMode,
                                    child: RichText(
                                      text: TextSpan(
                                        style: const TextStyle(
                                            fontSize: 13.5, color: AppColors.textSecondary),
                                        children: [
                                          TextSpan(
                                            text: _isLogin
                                                ? "Don't have an account? "
                                                : 'Already have an account? ',
                                          ),
                                          TextSpan(
                                            text: _isLogin ? 'Sign up' : 'Sign in',
                                            style: const TextStyle(
                                              color: AppColors.primary,
                                              fontWeight: FontWeight.w800,
                                            ),
                                          ),
                                        ],
                                      ),
                                    ),
                                  ),
                                ),
                              ],
                            ),
                          ),
                        ),
                      ),
                    ),
                  ]),
                ),
              ),
            ),
          ),
        ),
      ),
    );
  }
}

// ==================== HOME SCREEN ====================
class HomeScreen extends StatefulWidget {
  const HomeScreen({super.key});
  @override
  State<HomeScreen> createState() => _HomeScreenState();
}

class _HomeScreenState extends State<HomeScreen>
    with SingleTickerProviderStateMixin, AutomaticKeepAliveClientMixin {

  @override
  bool get wantKeepAlive => true;
  Map<String, int> _stats = {'toys': 0, 'exchanges': 0, 'requests': 0};
  bool _statsLoading = true;
  late AnimationController _staggerCtrl;
  late List<Animation<double>> _cardAnims;
  String? _username;
  int _unreadCount = 0;
  int _unreadMessageCount = 0;

  static const _greetings = [
    'Ready to share some joy today? 🎉',
    'Someone out there needs your toys! 🧸',
    'Sharing is caring — let\'s go! 💛',
    'Make a child\'s day — share a toy! 🌟',
    'Every toy shared is a smile earned! 😊',
    'What will you share today? 🚀',
    'Kindness starts with a simple share! 🤝',
    'One toy can change a child\'s whole day! 🎈',
    'Your old treasure is someone\'s new adventure! ✨',
    'Pass it on — spread the joy! 🌈',
    'Small act, big smile. Share a toy! 💫',
    'The more you share, the more joy goes around! 🎁',
  ];
  late String _greeting;

  String get _timeGreeting {
    final hour = DateTime.now().hour;
    if (hour < 12) return 'Good morning';
    if (hour < 17) return 'Good afternoon';
    return 'Good evening';
  }

  StreamSubscription<QuerySnapshot>? _unreadSub;
  StreamSubscription<QuerySnapshot>? _unreadMsgSub;

  @override
  void initState() {
    super.initState();
    _username = _auth.currentUser?.displayName ??
        _auth.currentUser?.email?.split('@').first ??
        'User';
    _staggerCtrl = AnimationController(
        vsync: this, duration: const Duration(milliseconds: 900));
    _cardAnims = List.generate(7, (i) {
      final start = i * 0.08;
      return Tween<double>(begin: 0, end: 1).animate(CurvedAnimation(
        parent: _staggerCtrl,
        curve: Interval(start, (start + 0.45).clamp(0.0, 1.0),
            curve: Curves.easeOutBack),
      ));
    });
    _loadStats();
    _subscribeUnread();
    _greeting = _greetings[DateTime.now().millisecondsSinceEpoch % _greetings.length];
    // Check return date reminders silently on app open
    FirestoreService.checkReturnDateReminders();

    WidgetsBinding.instance.addPostFrameCallback((_) {
      if (mounted) InAppNotificationService.init(context);
    });
  }

  void _subscribeUnread() {
    final uid = _auth.currentUser?.uid;
    if (uid == null) return;

    _auth.authStateChanges().listen((user) {
      if (user != null && mounted) {
        setState(() {
          _username = user.displayName ?? user.email?.split('@').first ?? 'User';
        });
      }
    });

    _unreadSub = _db
        .collection('notifications')
        .where('userId', isEqualTo: uid)
        .where('isRead', isEqualTo: false)
        .snapshots()
        .listen(
          (snap) {
        if (mounted) setState(() => _unreadCount = snap.docs.length);
      },
      onError: (e) => debugPrint('Unread count error: $e'),
    );

    _unreadMsgSub = _db
        .collection('messages')
        .where('receiverId', isEqualTo: uid)
        .where('isRead', isEqualTo: false)
        .snapshots()
        .listen(
          (snap) {
        if (mounted) setState(() => _unreadMessageCount = snap.docs.length);
      },
      onError: (e) => debugPrint('Unread messages error: $e'),
    );
  }

  Future<void> _loadStats() async {
    if (mounted) setState(() => _statsLoading = true);
    try {
      final stats = await FirestoreService.getDashboardStats();
      if (mounted) {
        setState(() {
          _stats = stats;
          _statsLoading = false;
        });
        _staggerCtrl.forward(from: 0);
      }
    } catch (e) {
      debugPrint('Load stats error: $e');
      if (mounted) {
        setState(() => _statsLoading = false);
        _staggerCtrl.forward(from: 0);
      }
    }
  }

  @override
  void dispose() {
    _unreadSub?.cancel();
    _unreadMsgSub?.cancel();
    InAppNotificationService.dispose();
    _staggerCtrl.dispose();
    super.dispose();
  }

  @override
  @override
  Widget build(BuildContext context) {
    super.build(context);
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Scaffold(
      backgroundColor: isDark ? AppColors.darkSurface : AppColors.surface,
      body: RefreshIndicator(
        onRefresh: _loadStats,
        color: AppColors.primary,
        displacement: 60,
        child: SingleChildScrollView(
          physics: const AlwaysScrollableScrollPhysics(),
          padding: EdgeInsets.symmetric(
            horizontal: Responsive.isMobile(context) ? 16 : 24,
            vertical: 20,
          ),
          child: Center(
            child: ConstrainedBox(
              constraints: const BoxConstraints(maxWidth: 900),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  _buildWelcomeBanner(),
                  const SizedBox(height: 18),
                  _buildStatsRow(),
                  const SizedBox(height: 26),
                  Text(
                    'Quick Actions',
                    style: TextStyle(
                      fontSize: 18,
                      fontWeight: FontWeight.w900,
                      letterSpacing: -0.5,
                      color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
                    ),
                  ),
                  const SizedBox(height: 14),
                  _buildActionGrid(),
                  const SizedBox(height: 28),
                  _buildRecentToysSection(),
                  const SizedBox(height: 24),
                ],
              ),
            ),
          ),
        ),
      ),
    );
  }

  Widget _buildWelcomeBanner() {
    return Container(
      width: double.infinity,
      padding: const EdgeInsets.all(22),
      decoration: BoxDecoration(
        gradient: const LinearGradient(
          colors: [Color(0xFF3D32A8), Color(0xFF5B4FCF), Color(0xFF7B6FE8)],
          begin: Alignment.topLeft,
          end: Alignment.bottomRight,
        ),
        borderRadius: BorderRadius.circular(24),
        boxShadow: AppShadows.hero(context),
      ),
      child: Stack(children: [
        Positioned(right: -20, top: -20,
          child: Container(width: 100, height: 100,
            decoration: BoxDecoration(color: Colors.white.withOpacity(0.06), shape: BoxShape.circle)),
        ),
        Positioned(right: 30, bottom: -30,
          child: Container(width: 70, height: 70,
            decoration: BoxDecoration(color: Colors.white.withOpacity(0.04), shape: BoxShape.circle)),
        ),
        Positioned(left: -20, top: 30,
          child: Container(width: 60, height: 60,
            decoration: BoxDecoration(color: Colors.white.withOpacity(0.04), shape: BoxShape.circle)),
        ),
        Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
          Row(children: [
            Expanded(
              child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                Text('$_timeGreeting,',
                  style: TextStyle(color: Colors.white.withOpacity(0.8), fontSize: 13.5, fontWeight: FontWeight.w500)),
                const SizedBox(height: 2),
                Text('${_username ?? 'Friend'}! 👋',
                  style: const TextStyle(color: Colors.white, fontSize: 24,
                      fontWeight: FontWeight.w900, letterSpacing: -0.5)),
              ]),
            ),
            const ToyShareLogo(size: 54, animate: false),
          ]),
          const SizedBox(height: 12),
          Container(
            padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
            decoration: BoxDecoration(
              color: Colors.white.withOpacity(0.12),
              borderRadius: BorderRadius.circular(12),
              border: Border.all(color: Colors.white.withOpacity(0.15)),
            ),
            child: Row(children: [
              const Icon(Icons.auto_awesome_rounded, color: Colors.white70, size: 14),
              const SizedBox(width: 8),
              Expanded(child: Text(_greeting,
                style: const TextStyle(color: Colors.white, fontSize: 13, fontWeight: FontWeight.w500))),
            ]),
          ),
          if ((_stats['requests'] ?? 0) > 0) ...[
            const SizedBox(height: 10),
            GestureDetector(
              onTap: () => navigateTo(context, const RequestsScreen()).then((_) => _loadStats()),
              child: Container(
                padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 7),
                decoration: BoxDecoration(
                  color: AppColors.warning.withOpacity(0.2),
                  borderRadius: BorderRadius.circular(10),
                  border: Border.all(color: AppColors.warning.withOpacity(0.4)),
                ),
                child: Row(mainAxisSize: MainAxisSize.min, children: [
                  const Icon(Icons.notifications_active_rounded, color: AppColors.warning, size: 14),
                  const SizedBox(width: 6),
                  Text('${_stats['requests']} pending request${(_stats['requests'] ?? 0) > 1 ? 's' : ''} — tap to review',
                    style: const TextStyle(color: AppColors.warning, fontSize: 12, fontWeight: FontWeight.w700)),
                ]),
              ),
            ),
          ],
        ]),
      ]),
    );
  }

  Widget _buildStatsRow() {
    if (_statsLoading) {
      return Row(
        children: List.generate(3, (i) => Expanded(
          child: Padding(
            padding: EdgeInsets.only(right: i < 2 ? 10 : 0),
            child: const _StatCardSkeleton(),
          ),
        )),
      );
    }

    final stats = [
      {'value': '${_stats['toys']}', 'label': 'My Toys', 'icon': AppIcons.myToys,
        'color': AppColors.coral, 'onTap': () => navigateTo(context, const MyToysScreen()),
        'bg': const [Color(0xFFFF9A3C), Color(0xFFFF6B35)]},
      {'value': '${_stats['exchanges']}', 'label': 'Exchanges', 'icon': AppIcons.exchanges,
        'color': AppColors.accent, 'onTap': () => navigateTo(context, const MyExchangesScreen()),
        'bg': const [Color(0xFF4ECDC4), Color(0xFF38B2AC)]},
      {'value': '${_stats['requests']}', 'label': 'Requests', 'icon': AppIcons.requests,
        'color': AppColors.primary, 'onTap': () => navigateTo(context, const RequestsScreen()),
        'bg': const [Color(0xFF5B4FCF), Color(0xFF7B6FE8)]},
    ];

    return Row(
      children: List.generate(stats.length, (index) {
        final s = stats[index];
        final color = s['color'] as Color;
        final icon = s['icon'] as IconData;
        final bg = s['bg'] as List<Color>;
        final isLast = index == stats.length - 1;
        final val = int.tryParse(s['value'] as String) ?? 0;
        return Expanded(
          child: Padding(
            padding: EdgeInsets.only(right: isLast ? 0 : 10),
            child: GestureDetector(
              onTap: s['onTap'] as VoidCallback,
              child: Container(
                padding: const EdgeInsets.symmetric(vertical: 16, horizontal: 12),
                decoration: BoxDecoration(
                  color: Theme.of(context).cardColor,
                  borderRadius: BorderRadius.circular(18),
                  boxShadow: AppShadows.card(context),
                ),
                child: Column(children: [
                  Container(
                    width: 40, height: 40,
                    decoration: BoxDecoration(
                      gradient: LinearGradient(colors: bg, begin: Alignment.topLeft, end: Alignment.bottomRight),
                      borderRadius: BorderRadius.circular(12),
                    ),
                    child: Center(child: FaIcon(icon, size: 18, color: Colors.white)),
                  ),
                  const SizedBox(height: 8),
                  Text('${s['value']}',
                    style: TextStyle(fontSize: 24, fontWeight: FontWeight.w900, color: color, height: 1)),
                  const SizedBox(height: 2),
                  Text(s['label'] as String,
                    style: TextStyle(fontSize: 10.5, color: Colors.grey.shade500, fontWeight: FontWeight.w600),
                    textAlign: TextAlign.center),
                  if (val > 0 && s['label'] == 'Requests') ...[
                    const SizedBox(height: 4),
                    Container(
                      padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2),
                      decoration: BoxDecoration(color: AppColors.error.withOpacity(0.1), borderRadius: BorderRadius.circular(20)),
                      child: Text('Needs attention', style: const TextStyle(fontSize: 9, color: AppColors.error, fontWeight: FontWeight.w700)),
                    ),
                  ],
                ]),
              ),
            ),
          ),
        );
      }),
    );
  }

  Widget _buildActionGrid() {
    final actions = [
      _ActionItem(
        icon: AppIcons.listToy,
        label: 'List Toy',
        subtitle: 'Share or donate',
        color: AppColors.success,
        gradient: const [Color(0xFF06D6A0), Color(0xFF00B386)],
        onTap: () => navigateTo(context, const AddToyScreen()).then((_) => _loadStats()),
        animIndex: 0,
      ),
      _ActionItem(
        icon: AppIcons.myToys,
        label: 'My Toys',
        subtitle: 'Manage listings',
        color: AppColors.coral,
        gradient: const [Color(0xFFFF9A3C), Color(0xFFFF6B35)],
        onTap: () => navigateTo(context, const MyToysScreen()).then((_) => _loadStats()),
        animIndex: 1,
      ),
      _ActionItem(
        icon: AppIcons.browse,
        label: 'Browse',
        subtitle: 'Find toys nearby',
        color: AppColors.primary,
        gradient: const [Color(0xFF5B4FCF), Color(0xFF7B6FE8)],
        onTap: () => navigateTo(context, const AvailableToysScreen()),
        animIndex: 2,
      ),
      _ActionItem(
        icon: AppIcons.requests,
        label: 'Requests',
        subtitle: '${_stats['requests']} pending',
        color: const Color(0xFFFF6B6B),
        gradient: const [Color(0xFFFF6B6B), Color(0xFFFF4757)],
        onTap: () => navigateTo(context, const RequestsScreen()).then((_) => _loadStats()),
        badge: (_stats['requests'] ?? 0) > 0 ? _stats['requests'] : null,
        animIndex: 3,
      ),
      _ActionItem(
        icon: AppIcons.exchanges,
        label: 'Exchanges',
        subtitle: 'View history',
        color: AppColors.accent,
        gradient: const [Color(0xFF4ECDC4), Color(0xFF38B2AC)],
        onTap: () => navigateTo(context, const MyExchangesScreen()),
        animIndex: 4,
      ),
      _ActionItem(
        icon: FontAwesomeIcons.chartBar,
        label: 'My Activities',
        subtitle: 'Lending & borrowing',
        color: const Color(0xFF8B5CF6),
        gradient: const [Color(0xFF8B5CF6), Color(0xFF7C3AED)],
        onTap: () => navigateTo(context, const UserDashboardScreen()),
        animIndex: 5,
      ),
      _ActionItem(
        icon: AppIcons.blacklist,
        label: 'Blocked',
        subtitle: 'Blocked users',
        color: const Color(0xFF8B8B9E),
        gradient: const [Color(0xFF8B8B9E), Color(0xFF6B6B8A)],
        onTap: () => navigateTo(context, const BlacklistScreen()),
        animIndex: 6,
      ),
    ];

    return GridView.builder(
      shrinkWrap: true,
      physics: const NeverScrollableScrollPhysics(),
      gridDelegate: SliverGridDelegateWithFixedCrossAxisCount(
        crossAxisCount: Responsive.gridCrossAxisCount(context),
        crossAxisSpacing: 12,
        mainAxisSpacing: 12,
        childAspectRatio: Responsive.isMobile(context) ? 0.95 : 1.05,
      ),
      itemCount: actions.length,
      itemBuilder: (_, i) {
        final item = actions[i];
        return AnimatedBuilder(
          animation: _cardAnims[item.animIndex],
          builder: (_, child) => Transform.scale(
            scale: _cardAnims[item.animIndex].value.clamp(0.0, 1.0),
            child: Opacity(
              opacity: _cardAnims[item.animIndex].value.clamp(0.0, 1.0),
              child: child,
            ),
          ),
          child: _HoverDashCard(item: item),
        );
      },
    );
  }

  Widget _buildRecentToysSection() {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
      Row(children: [
        Text('Recently Added', style: TextStyle(
          fontSize: 18, fontWeight: FontWeight.w900, letterSpacing: -0.5,
          color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
        )),
        const Spacer(),
        GestureDetector(
          onTap: () => navigateTo(context, const AvailableToysScreen()),
          child: const Text('See all', style: TextStyle(fontSize: 13, color: AppColors.primary, fontWeight: FontWeight.w700)),
        ),
      ]),
      const SizedBox(height: 14),
      StreamBuilder<QuerySnapshot>(
        stream: FirebaseFirestore.instance
            .collection('toys')
            .where('isAvailable', isEqualTo: true)
            .orderBy('createdAt', descending: true)
            .limit(5)
            .snapshots(),
        builder: (ctx, snap) {
          if (!snap.hasData) {
            return SizedBox(
              height: 130,
              child: ListView.separated(
                scrollDirection: Axis.horizontal,
                itemCount: 4,
                separatorBuilder: (_, __) => const SizedBox(width: 12),
                itemBuilder: (_, __) => Container(
                  width: 140, height: 130,
                  decoration: BoxDecoration(
                    color: isDark ? AppColors.darkCardBg : Colors.grey.shade100,
                    borderRadius: BorderRadius.circular(16),
                  ),
                ),
              ),
            );
          }
          final docs = snap.data!.docs;
          if (docs.isEmpty) {
            return Container(
              height: 100,
              decoration: BoxDecoration(
                color: isDark ? AppColors.darkCardBg : Colors.grey.shade50,
                borderRadius: BorderRadius.circular(16),
              ),
              child: Center(child: Text('No toys listed yet — be the first!',
                style: TextStyle(color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary, fontSize: 13))),
            );
          }
          return SizedBox(
            height: 148,
            child: ListView.separated(
              scrollDirection: Axis.horizontal,
              padding: const EdgeInsets.only(right: 4),
              itemCount: docs.length,
              separatorBuilder: (_, __) => const SizedBox(width: 12),
              itemBuilder: (_, i) {
                final d = docs[i].data() as Map<String, dynamic>;
                final isExchange = d['type'] == 'exchange';
                final color = isExchange ? AppColors.primary : AppColors.success;
                final isOwn = d['ownerId'] == _auth.currentUser?.uid;
                return GestureDetector(
                  onTap: () => navigateTo(context, const AvailableToysScreen()),
                  child: Container(
                    width: 148,
                    padding: const EdgeInsets.all(14),
                    decoration: BoxDecoration(
                      color: isDark ? AppColors.darkCardBg : Colors.white,
                      borderRadius: BorderRadius.circular(16),
                      boxShadow: AppShadows.card(context),
                      border: Border.all(color: isDark ? AppColors.darkDivider : AppColors.divider),
                    ),
                    child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                      Row(children: [
                        Container(
                          padding: const EdgeInsets.symmetric(horizontal: 7, vertical: 3),
                          decoration: BoxDecoration(color: color.withOpacity(0.1), borderRadius: BorderRadius.circular(8)),
                          child: Text(isExchange ? 'EXCHANGE' : 'DONATE',
                            style: TextStyle(fontSize: 9, fontWeight: FontWeight.w800, color: color)),
                        ),
                        if (isOwn) ...[
                          const SizedBox(width: 4),
                          Icon(Icons.star_rounded, size: 12, color: AppColors.accent),
                        ],
                      ]),
                      const SizedBox(height: 8),
                      Text(d['name'] ?? '',
                        style: TextStyle(fontSize: 14, fontWeight: FontWeight.w800,
                          color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
                        maxLines: 2, overflow: TextOverflow.ellipsis),
                      const Spacer(),
                      Row(children: [
                        UserAvatar(name: d['ownerName'] ?? '?', size: 18, gradient: AppColors.heroGradient),
                        const SizedBox(width: 5),
                        Expanded(child: Text(isOwn ? 'You' : (d['ownerName'] ?? ''),
                          style: TextStyle(fontSize: 11, color: isDark ? AppColors.darkTextSecondary : Colors.grey.shade500, fontWeight: FontWeight.w600),
                          overflow: TextOverflow.ellipsis)),
                      ]),
                    ]),
                  ),
                );
              },
            ),
          );
        },
      ),
    ]);
  }

  Widget _buildDrawer(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Drawer(
      backgroundColor: isDark ? AppColors.darkCardBg : Colors.white,
      shape: const RoundedRectangleBorder(
          borderRadius: BorderRadius.horizontal(right: Radius.circular(24))),
      child: Column(children: [
        Container(
          width: double.infinity,
          padding: const EdgeInsets.fromLTRB(20, 52, 20, 28),
          decoration: const BoxDecoration(
            gradient: LinearGradient(
              colors: AppColors.heroGradient,
              begin: Alignment.topLeft,
              end: Alignment.bottomRight,
            ),
            borderRadius: BorderRadius.only(bottomRight: Radius.circular(24)),
          ),
          child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
            UserAvatar(name: _username ?? '?', size: 58),
            const SizedBox(height: 14),
            Text(
              _username ?? '',
              style: const TextStyle(
                color: Colors.white,
                fontSize: 18,
                fontWeight: FontWeight.w900,
              ),
            ),
          ]),
        ),
        Expanded(
          child: ListView(
            padding: const EdgeInsets.symmetric(vertical: 10, horizontal: 8),
            children: [
              _drawerTile(AppIcons.listToy, 'List New Toy', AppColors.success,
                      () => navigateTo(context, const AddToyScreen())),
              _drawerTile(AppIcons.myToys, 'My Toys', AppColors.coral,
                      () => navigateTo(context, const MyToysScreen())),
              _drawerTile(AppIcons.browse, 'Browse Toys', AppColors.primary,
                      () => navigateTo(context, const AvailableToysScreen())),
              _drawerTile(AppIcons.requests, 'Requests Received',
                  const Color(0xFFFF6B6B), () => navigateTo(context, const RequestsScreen())),
              _drawerTile(AppIcons.exchanges, 'My Exchanges', AppColors.accent,
                      () => navigateTo(context, const MyExchangesScreen())),
              _drawerTile(FontAwesomeIcons.chartBar, 'My Activities', const Color(0xFF8B5CF6),
                      () => navigateTo(context, const UserDashboardScreen())),
              _drawerTile(AppIcons.blacklist, 'Blocked Users', Colors.grey.shade500,
                      () => navigateTo(context, const BlacklistScreen())),
              const Divider(height: 20, indent: 8, endIndent: 8),
              _drawerTile(AppIcons.person, 'My Profile', AppColors.primary,
                      () => navigateTo(context, const ProfileScreen())),
              _drawerTile(AppIcons.logout, 'Sign Out', AppColors.error, () async {
                await _auth.signOut();
              }),
            ],
          ),
        ),
      ]),
    );
  }

  Widget _drawerTile(IconData icon, String title, Color color, VoidCallback onTap) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return ListTile(
      leading: Container(
        width: 36,
        height: 36,
        decoration: BoxDecoration(
          color: color.withOpacity(0.1),
          borderRadius: BorderRadius.circular(10),
        ),
        child: Center(child: FaIcon(icon, size: 18, color: color)),
      ),
      title: Text(
        title,
        style: TextStyle(
            fontSize: 14, color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary, fontWeight: FontWeight.w600),
      ),
      onTap: () {
        Navigator.pop(context);
        onTap();
      },
      dense: true,
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
      contentPadding: const EdgeInsets.symmetric(horizontal: 8, vertical: 2),
    );
  }
}

class _ActionItem {
  final IconData icon;
  final String label;
  final String subtitle;
  final Color color;
  final List<Color>? gradient;
  final VoidCallback onTap;
  final int? badge;
  final int animIndex;
  _ActionItem({
    required this.icon,
    required this.label,
    required this.subtitle,
    required this.color,
    this.gradient,
    required this.onTap,
    required this.animIndex,
    this.badge,
  });
}

class _HoverDashCard extends StatefulWidget {
  final _ActionItem item;
  const _HoverDashCard({required this.item});
  @override
  State<_HoverDashCard> createState() => _HoverDashCardState();
}

class _HoverDashCardState extends State<_HoverDashCard>
    with SingleTickerProviderStateMixin {
  bool _hovered = false;
  late AnimationController _ctrl;
  late Animation<double> _scale;

  @override
  void initState() {
    super.initState();
    _ctrl = AnimationController(
        vsync: this, duration: const Duration(milliseconds: 200));
    _scale = Tween<double>(begin: 1.0, end: 1.04)
        .animate(CurvedAnimation(parent: _ctrl, curve: Curves.easeOutCubic));
  }

  @override
  void dispose() {
    _ctrl.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final item = widget.item;
    final gradColors = item.gradient ?? [item.color, item.color];
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return MouseRegion(
      onEnter: (_) {
        setState(() => _hovered = true);
        _ctrl.forward();
      },
      onExit: (_) {
        setState(() => _hovered = false);
        _ctrl.reverse();
      },
      cursor: SystemMouseCursors.click,
      child: AnimatedBuilder(
        animation: _ctrl,
        builder: (_, child) => Transform.scale(scale: _scale.value, child: child),
        child: AnimatedContainer(
          duration: const Duration(milliseconds: 200),
          decoration: BoxDecoration(
            color: _hovered ? item.color.withOpacity(0.04) : Theme.of(context).cardColor,
            borderRadius: BorderRadius.circular(22),
            border: Border.all(
              color: _hovered ? item.color.withOpacity(0.25) : Colors.transparent,
              width: 1.5,
            ),
            boxShadow: _hovered
                ? [
              BoxShadow(
                color: item.color.withOpacity(0.22),
                blurRadius: 24,
                offset: const Offset(0, 8),
              )
            ]
                : AppShadows.card(context),
          ),
          child: Material(
            color: Colors.transparent,
            borderRadius: BorderRadius.circular(22),
            child: InkWell(
              onTap: item.onTap,
              borderRadius: BorderRadius.circular(22),
              splashColor: item.color.withOpacity(0.12),
              child: Padding(
                padding: const EdgeInsets.all(18),
                child: Column(
                  mainAxisAlignment: MainAxisAlignment.center,
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    Stack(clipBehavior: Clip.none, children: [
                      AnimatedContainer(
                        duration: const Duration(milliseconds: 200),
                        width: 52,
                        height: 52,
                        decoration: BoxDecoration(
                          gradient: LinearGradient(
                            colors: gradColors,
                            begin: Alignment.topLeft,
                            end: Alignment.bottomRight,
                          ),
                          borderRadius: BorderRadius.circular(16),
                          boxShadow: [
                            BoxShadow(
                              color: item.color.withOpacity(_hovered ? 0.55 : 0.30),
                              blurRadius: _hovered ? 18 : 10,
                              offset: const Offset(0, 5),
                            ),
                          ],
                        ),
                        child: Center(
                          child: FaIcon(item.icon, size: 26, color: Colors.white),
                        ),
                      ),
                      if (item.badge != null && item.badge! > 0)
                        Positioned(
                          right: -5,
                          top: -5,
                          child: Container(
                            width: 22,
                            height: 22,
                            decoration: BoxDecoration(
                              color: AppColors.error,
                              shape: BoxShape.circle,
                              border: Border.all(color: isDark ? AppColors.darkCardBg : Colors.white, width: 2),
                            ),
                            child: Center(
                              child: Text(
                                '${item.badge}',
                                style: const TextStyle(
                                    color: Colors.white, fontSize: 10, fontWeight: FontWeight.w900),
                              ),
                            ),
                          ),
                        ),
                    ]),
                    const SizedBox(height: 14),
                    AnimatedDefaultTextStyle(
                      duration: const Duration(milliseconds: 200),
                      style: TextStyle(
                        fontSize: 14,
                        fontWeight: FontWeight.w800,
                        color: _hovered ? item.color : (isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
                        letterSpacing: -0.2,
                      ),
                      child: Text(item.label, maxLines: 1, overflow: TextOverflow.ellipsis),
                    ),
                    const SizedBox(height: 3),
                    Text(
                      item.subtitle,
                      style: TextStyle(
                        fontSize: 11.5,
                        color: _hovered ? item.color.withOpacity(0.65) : Colors.grey.shade500,
                      ),
                      maxLines: 1,
                    ),
                    const Spacer(),
                    AnimatedContainer(
                      duration: const Duration(milliseconds: 200),
                      height: 3,
                      width: _hovered ? 44 : 28,
                      decoration: BoxDecoration(
                        gradient: LinearGradient(colors: gradColors),
                        borderRadius: BorderRadius.circular(2),
                      ),
                    ),
                  ],
                ),
              ),
            ),
          ),
        ),
      ),
    );
  }
}

// ==================== ADD TOY SCREEN ====================
class AddToyScreen extends StatefulWidget {
  const AddToyScreen({super.key});
  @override
  State<AddToyScreen> createState() => _AddToyScreenState();
}

class _AddToyScreenState extends State<AddToyScreen> {
  final _formKey = GlobalKey<FormState>();
  final _nameCtrl = TextEditingController();
  final _descCtrl = TextEditingController();
  String _condition = 'Good';
  String _type = 'exchange';
  String _ageGroup = '3-8';
  String _category = 'Building';
  bool _isSubmitting = false;
  DateTime? _receiveDate, _returnDate;

  // ── Time-based lending ──
  String _durationMode = 'days'; // 'days' or 'hours'
  int _loanHours = 2; // default 2 hours when in hours mode
  TimeOfDay? _receiveTime, _returnTime;
  final _feeCtrl = TextEditingController();
  bool _hasFee = false;
  String? _imageUrl;
  File? _imageFile; // kept in memory only — no Firebase Storage
  bool _isUploadingImage = false;

  Future<void> _pickAndUploadImage() async {
    try {
      final picker = ImagePicker();
      final source = await showModalBottomSheet<ImageSource>(
        context: context,
        shape: const RoundedRectangleBorder(
          borderRadius: BorderRadius.vertical(top: Radius.circular(20)),
        ),
        builder: (ctx) {
          final isDark = Theme.of(ctx).brightness == Brightness.dark;
          return Container(
            padding: const EdgeInsets.fromLTRB(20, 16, 20, 32),
            color: isDark ? AppColors.darkCardBg : Colors.white,
            child: Column(mainAxisSize: MainAxisSize.min, children: [
              Container(width: 36, height: 4,
                decoration: BoxDecoration(color: Colors.grey.shade300, borderRadius: BorderRadius.circular(2))),
              const SizedBox(height: 16),
              const Text('Add Toy Photo', style: TextStyle(fontSize: 17, fontWeight: FontWeight.w800)),
              const SizedBox(height: 16),
              ListTile(
                leading: Container(width: 40, height: 40,
                  decoration: BoxDecoration(color: AppColors.primary.withOpacity(0.1), borderRadius: BorderRadius.circular(10)),
                  child: const Icon(Icons.camera_alt_rounded, color: AppColors.primary)),
                title: const Text('Take Photo', style: TextStyle(fontWeight: FontWeight.w700)),
                subtitle: const Text('Use your camera'),
                onTap: () => Navigator.pop(ctx, ImageSource.camera),
                shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
              ),
              ListTile(
                leading: Container(width: 40, height: 40,
                  decoration: BoxDecoration(color: AppColors.accent.withOpacity(0.1), borderRadius: BorderRadius.circular(10)),
                  child: const Icon(Icons.photo_library_rounded, color: AppColors.accent)),
                title: const Text('Choose from Gallery', style: TextStyle(fontWeight: FontWeight.w700)),
                subtitle: const Text('Pick an existing photo'),
                onTap: () => Navigator.pop(ctx, ImageSource.gallery),
                shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
              ),
              if (_imageFile != null) ...[
                const Divider(),
                ListTile(
                  leading: Container(width: 40, height: 40,
                    decoration: BoxDecoration(color: AppColors.error.withOpacity(0.1), borderRadius: BorderRadius.circular(10)),
                    child: const Icon(Icons.delete_rounded, color: AppColors.error)),
                  title: const Text('Remove Photo', style: TextStyle(fontWeight: FontWeight.w700, color: AppColors.error)),
                  onTap: () {
                    Navigator.pop(ctx);
                    setState(() { _imageFile = null; _imageUrl = null; });
                  },
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                ),
              ],
            ]),
          );
        },
      );
      if (source == null) return;

      setState(() => _isUploadingImage = true);

      final XFile? image = await picker.pickImage(
        source: source,
        maxWidth: 1200,
        maxHeight: 1200,
        imageQuality: 85,
      );
      if (image == null || !mounted) {
        setState(() => _isUploadingImage = false);
        return;
      }

      // Store in memory only — just the local file path
      setState(() {
        _imageFile = File(image.path);
        _imageUrl = image.path;
        _isUploadingImage = false;
      });

    } catch (e) {
      debugPrint('Image pick error: $e');
      if (mounted) {
        setState(() => _isUploadingImage = false);
        ScaffoldMessenger.of(context).showSnackBar(SnackBar(
          content: Row(children: [
            const Icon(Icons.error_outline, color: Colors.white, size: 18),
            const SizedBox(width: 8),
            Expanded(child: Text('Failed to pick image: ${e.toString().replaceAll('Exception:', '').trim()}',
              style: const TextStyle(fontWeight: FontWeight.w600))),
          ]),
          backgroundColor: AppColors.error,
          margin: const EdgeInsets.all(14),
        ));
      }
    }
  }

  static const _conditions = ['New', 'Like New', 'Good', 'Fair', 'Poor'];
  static const _ageGroups = ['0-3', '3-8', '6-10', '8+', '12+', 'All ages'];
  static const _categories = [
    'Building', 'Board Games', 'Stuffed Animals', 'Dolls',
    'Vehicles', 'Outdoor', 'Arts & Crafts', 'Electronics', 'Sports', 'Other',
  ];

  @override
  void dispose() {
    _nameCtrl.dispose();
    _descCtrl.dispose();
    super.dispose();
  }

  Future<void> _pickDate(bool isReceive) async {
    final now = DateTime.now();
    DateTime initialDate;
    DateTime firstDate;

    if (isReceive) {
      initialDate = now;
      firstDate = now;
    } else {
      final base = _receiveDate ?? now;
      initialDate = base.add(const Duration(days: 1));
      firstDate = base;
    }

    final lastDate = now.add(const Duration(days: 365));
    if (initialDate.isBefore(firstDate)) initialDate = firstDate;
    if (initialDate.isAfter(lastDate)) initialDate = lastDate;

    final picked = await showDatePicker(
      context: context,
      initialDate: initialDate,
      firstDate: firstDate,
      lastDate: lastDate,
      builder: (context, child) => Theme(
        data: Theme.of(context).copyWith(
            colorScheme: const ColorScheme.light(primary: AppColors.primary)),
        child: child!,
      ),
    );
    if (picked == null || !mounted) return;

    // Also pick time
    final pickedTime = await showTimePicker(
      context: context,
      initialTime: TimeOfDay.now(),
      builder: (context, child) => Theme(
        data: Theme.of(context).copyWith(
            colorScheme: const ColorScheme.light(primary: AppColors.primary)),
        child: child!,
      ),
    );
    if (!mounted) return;

    setState(() {
      final fullDateTime = DateTime(picked.year, picked.month, picked.day,
          pickedTime?.hour ?? 12, pickedTime?.minute ?? 0);
      if (isReceive) {
        _receiveDate = fullDateTime;
        _receiveTime = pickedTime;
        if (_returnDate != null && !_returnDate!.isAfter(fullDateTime)) {
          _returnDate = null;
          _returnTime = null;
        }
      } else {
        _returnDate = fullDateTime;
        _returnTime = pickedTime;
      }
    });
  }

  Future<void> _pickHoursSheet() async {
    final result = await showModalBottomSheet<int>(
      context: context,
      shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.vertical(top: Radius.circular(24)),
      ),
      builder: (ctx) {
        final isDark = Theme.of(ctx).brightness == Brightness.dark;
        int selected = _loanHours;
        final presets = [
          {'label': '5 min', 'value': 0, 'minutes': 5},
          {'label': '15 min', 'value': 0, 'minutes': 15},
          {'label': '30 min', 'value': 0, 'minutes': 30},
          {'label': '1 hour', 'value': 1, 'minutes': 0},
          {'label': '2 hours', 'value': 2, 'minutes': 0},
          {'label': '3 hours', 'value': 3, 'minutes': 0},
          {'label': '4 hours', 'value': 4, 'minutes': 0},
          {'label': '6 hours', 'value': 6, 'minutes': 0},
          {'label': '8 hours', 'value': 8, 'minutes': 0},
          {'label': '12 hours', 'value': 12, 'minutes': 0},
          {'label': '24 hours', 'value': 24, 'minutes': 0},
          {'label': '48 hours', 'value': 48, 'minutes': 0},
        ];
        return StatefulBuilder(
          builder: (sCtx, setS) => Container(
            padding: const EdgeInsets.fromLTRB(20, 20, 20, 30),
            decoration: BoxDecoration(
              color: isDark ? AppColors.darkCardBg : Colors.white,
              borderRadius: const BorderRadius.vertical(top: Radius.circular(24)),
            ),
            child: Column(mainAxisSize: MainAxisSize.min, crossAxisAlignment: CrossAxisAlignment.start, children: [
              Row(children: [
                Container(width: 36, height: 36,
                  decoration: BoxDecoration(color: AppColors.accent.withOpacity(0.1), borderRadius: BorderRadius.circular(10)),
                  child: const Icon(Icons.schedule_rounded, color: AppColors.accent, size: 20)),
                const SizedBox(width: 12),
                const Text('How long?', style: TextStyle(fontSize: 18, fontWeight: FontWeight.w900)),
                const Spacer(),
                IconButton(icon: const Icon(Icons.close_rounded), onPressed: () => Navigator.pop(ctx)),
              ]),
              const SizedBox(height: 6),
              Text('Quick lending durations', style: TextStyle(fontSize: 12.5, color: Colors.grey.shade500)),
              const SizedBox(height: 16),
              Wrap(
                spacing: 10, runSpacing: 10,
                children: presets.map((p) {
                  final mins = (p['minutes'] as int);
                  final hrs = (p['value'] as int);
                  final totalMins = hrs * 60 + mins;
                  final isSelected = selected == totalMins;
                  return GestureDetector(
                    onTap: () => setS(() => selected = totalMins),
                    child: AnimatedContainer(
                      duration: const Duration(milliseconds: 150),
                      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 10),
                      decoration: BoxDecoration(
                        color: isSelected ? AppColors.accent : (isDark ? AppColors.darkInputBg : const Color(0xFFF3F4F8)),
                        borderRadius: BorderRadius.circular(12),
                        border: Border.all(color: isSelected ? AppColors.accent : (isDark ? AppColors.darkBorder : AppColors.divider)),
                      ),
                      child: Text(p['label'] as String, style: TextStyle(
                        fontSize: 13.5, fontWeight: FontWeight.w700,
                        color: isSelected ? Colors.white : (isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
                      )),
                    ),
                  );
                }).toList(),
              ),
              const SizedBox(height: 20),
              SizedBox(
                width: double.infinity,
                child: ElevatedButton(
                  onPressed: () => Navigator.pop(ctx, selected),
                  style: ElevatedButton.styleFrom(
                    backgroundColor: AppColors.accent, foregroundColor: Colors.white,
                    padding: const EdgeInsets.symmetric(vertical: 14),
                    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
                    elevation: 0,
                  ),
                  child: Text(
                    selected == 0 ? 'Set Duration' : 'Set ${_formatMinutes(selected)}',
                    style: const TextStyle(fontSize: 15, fontWeight: FontWeight.w800),
                  ),
                ),
              ),
            ]),
          ),
        );
      },
    );
    if (result != null && mounted) {
      setState(() {
        _loanHours = result;
        // Auto-set return date from now + duration
        final now = DateTime.now();
        _receiveDate = now;
        _returnDate = now.add(Duration(minutes: result));
      });
    }
  }

  String _formatMinutes(int totalMinutes) {
    if (totalMinutes < 60) return '$totalMinutes min';
    final h = totalMinutes ~/ 60;
    final m = totalMinutes % 60;
    if (m == 0) return '$h hr${h > 1 ? 's' : ''}';
    return '$h hr ${m} min';
  }

  String _formatDateTime(DateTime dt) {
    final date = DateFormat('MMM d').format(dt);
    final time = DateFormat('h:mm a').format(dt);
    return '$date at $time';
  }

  String _loanDurationLabel() {
    if (_receiveDate == null || _returnDate == null) return '';
    final diff = _returnDate!.difference(_receiveDate!);
    if (diff.inMinutes < 60) return '${diff.inMinutes} minutes';
    if (diff.inHours < 24) {
      final h = diff.inHours;
      final m = diff.inMinutes % 60;
      return m > 0 ? '$h hr $m min' : '$h hr${h > 1 ? 's' : ''}';
    }
    return '${diff.inDays} day${diff.inDays > 1 ? 's' : ''}';
  }

  Future<void> _submit() async {
    if (!_formKey.currentState!.validate()) return;
    if (_type == 'exchange') {
      if (_receiveDate == null) { _showError('Please set a receive date/time'); return; }
      if (_returnDate == null) { _showError('Please set a return date/time'); return; }
      if (!_returnDate!.isAfter(_receiveDate!)) {
        _showError('Return must be after receive time');
        return;
      }
      final diff = _returnDate!.difference(_receiveDate!);
      if (diff.inMinutes < 1) {
        _showError('Duration must be at least 1 minute');
        return;
      }
    }

    setState(() => _isSubmitting = true);
    try {
      final toyData = <String, dynamic>{
        'name': _nameCtrl.text.trim(),
        'category': _category,
        'description': _descCtrl.text.trim(),
        'condition': _condition,
        'type': _type,
        'ageGroup': _ageGroup,
        if (_imageUrl != null) 'imageUrl': _imageUrl,
      };
      if (_type == 'exchange') {
        toyData['receiveDate'] = _receiveDate?.toIso8601String();
        toyData['returnDate'] = _returnDate?.toIso8601String();
        toyData['durationMode'] = _durationMode;
        toyData['durationLabel'] = _loanDurationLabel();
        if (_hasFee && _feeCtrl.text.trim().isNotEmpty) {
          toyData['fee'] = double.tryParse(_feeCtrl.text.trim()) ?? 0;
          toyData['feeCurrency'] = 'PKR';
        }
      }
      await FirestoreService.addToy(toyData);

      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
        content: Row(children: [
          Icon(Icons.check_circle_rounded, color: Colors.white, size: 18),
          SizedBox(width: 8),
          Expanded(child: Text('Toy listed successfully!', style: TextStyle(fontWeight: FontWeight.w600))),
        ]),
        backgroundColor: AppColors.success,
        margin: EdgeInsets.all(14),
      ));
      Navigator.pop(context);
    } catch (e) {
      if (mounted) _showError('Failed to list toy. Please try again.');
      debugPrint('Add toy error: $e');
    }
    if (mounted) setState(() => _isSubmitting = false);
  }

  void _showError(String msg) => ScaffoldMessenger.of(context).showSnackBar(SnackBar(
    content: Text(msg, style: const TextStyle(fontWeight: FontWeight.w600)),
    backgroundColor: AppColors.error,
    margin: const EdgeInsets.all(14),
  ));

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Scaffold(
      appBar: AppBar(title: const Text('List a Toy')),
      body: SingleChildScrollView(
        padding: EdgeInsets.symmetric(
          horizontal: Responsive.isMobile(context) ? 16 : 24,
          vertical: 20,
        ),
        child: Center(
          child: ConstrainedBox(
            constraints: BoxConstraints(maxWidth: Responsive.formMaxWidth(context)),
            child: Form(
              key: _formKey,
              child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                sectionLabel('Basic Information'),
                AppTextField(
                  controller: _nameCtrl,
                  label: 'Toy Name',
                  prefixIcon: Icons.toys_rounded,
                  validator: (v) =>
                  (v == null || v.trim().isEmpty) ? 'Enter a toy name' : null,
                ),
                const SizedBox(height: 14),
                AppTextField(
                  controller: _descCtrl,
                  label: 'Description',
                  prefixIcon: Icons.notes_rounded,
                  maxLines: 3,
                  hintText: 'Describe the toy, any missing parts, etc.',
                  validator: (v) =>
                  (v == null || v.trim().isEmpty) ? 'Enter a description' : null,
                ),
                const SizedBox(height: 24),
                // ── Toy Photo ──
                sectionLabel('Toy Photo', color: AppColors.accent),
                const SizedBox(height: 10),
                GestureDetector(
                  onTap: _isUploadingImage ? null : _pickAndUploadImage,
                  child: Container(
                    width: double.infinity,
                    height: 160,
                    decoration: BoxDecoration(
                      color: isDark ? AppColors.darkInputBg : const Color(0xFFF3F4F8),
                      borderRadius: BorderRadius.circular(16),
                      border: Border.all(
                        color: _imageUrl != null
                            ? AppColors.accent.withOpacity(0.4)
                            : (isDark ? AppColors.darkBorder : AppColors.divider),
                        width: _imageUrl != null ? 2 : 1,
                      ),
                    ),
                    child: _isUploadingImage
                        ? Column(mainAxisAlignment: MainAxisAlignment.center, children: [
                            const CircularProgressIndicator(color: AppColors.accent, strokeWidth: 3),
                            const SizedBox(height: 12),
                            Text('Uploading photo...', style: TextStyle(
                              fontSize: 13, color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary,
                              fontWeight: FontWeight.w500)),
                          ])
                        : _imageFile != null
                        ? Stack(children: [
                            ClipRRect(
                              borderRadius: BorderRadius.circular(15),
                              child: Image.file(_imageFile!, width: double.infinity, height: 160, fit: BoxFit.cover),
                            ),
                            Positioned(top: 8, right: 8,
                              child: GestureDetector(
                                onTap: () => setState(() => _imageUrl = null),
                                child: Container(
                                  width: 30, height: 30,
                                  decoration: BoxDecoration(
                                    color: Colors.black.withOpacity(0.6),
                                    shape: BoxShape.circle,
                                  ),
                                  child: const Icon(Icons.close_rounded, color: Colors.white, size: 18),
                                ),
                              ),
                            ),
                            Positioned(bottom: 8, right: 8,
                              child: Container(
                                padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 5),
                                decoration: BoxDecoration(
                                  color: Colors.black.withOpacity(0.5),
                                  borderRadius: BorderRadius.circular(20),
                                ),
                                child: const Row(mainAxisSize: MainAxisSize.min, children: [
                                  Icon(Icons.edit_rounded, color: Colors.white, size: 12),
                                  SizedBox(width: 4),
                                  Text('Change', style: TextStyle(color: Colors.white, fontSize: 11, fontWeight: FontWeight.w600)),
                                ]),
                              ),
                            ),
                          ])
                        : Column(mainAxisAlignment: MainAxisAlignment.center, children: [
                            Container(
                              width: 52, height: 52,
                              decoration: BoxDecoration(
                                color: AppColors.accent.withOpacity(0.1),
                                shape: BoxShape.circle,
                              ),
                              child: const Icon(Icons.add_photo_alternate_rounded, size: 28, color: AppColors.accent),
                            ),
                            const SizedBox(height: 10),
                            const Text('Tap to add photo', style: TextStyle(fontSize: 13.5, color: AppColors.accent, fontWeight: FontWeight.w700)),
                            const SizedBox(height: 3),
                            Text('Camera or gallery • Optional', style: TextStyle(
                              fontSize: 11.5, color: Colors.grey.shade400)),
                          ]),
                  ),
                ),
                const SizedBox(height: 24),
                sectionLabel('Details'),
                Row(children: [
                  Expanded(
                    child: _buildDropdown('Condition', _condition, _conditions,
                            (v) => setState(() => _condition = v!)),
                  ),
                  const SizedBox(width: 12),
                  Expanded(
                    child: _buildDropdown('Age Group', _ageGroup, _ageGroups,
                            (v) => setState(() => _ageGroup = v!)),
                  ),
                ]),
                const SizedBox(height: 14),
                _buildDropdown('Category', _category, _categories,
                        (v) => setState(() => _category = v!)),
                const SizedBox(height: 24),
                sectionLabel('Listing Type'),
                _buildTypeSelector(),
                if (_type == 'exchange') ...[
                  const SizedBox(height: 24),
                  // ── Optional Fee ──
                  sectionLabel('Lending Fee', color: AppColors.warning),
                  const SizedBox(height: 10),
                  Row(children: [
                    Switch(
                      value: _hasFee,
                      onChanged: (v) => setState(() { _hasFee = v; if (!v) _feeCtrl.clear(); }),
                      activeColor: AppColors.warning,
                    ),
                    const SizedBox(width: 8),
                    Text(_hasFee ? 'Charge a fee for this exchange' : 'Free exchange (no fee)',
                      style: TextStyle(fontSize: 13.5, fontWeight: FontWeight.w600,
                        color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                  ]),
                  if (_hasFee) ...[
                    const SizedBox(height: 10),
                    AppTextField(
                      controller: _feeCtrl,
                      label: 'Fee Amount (PKR)',
                      prefixIcon: Icons.payments_rounded,
                      keyboardType: TextInputType.number,
                      hintText: 'e.g. 200',
                      validator: (v) {
                        if (!_hasFee) return null;
                        if (v == null || v.trim().isEmpty) return 'Enter fee amount';
                        if (double.tryParse(v.trim()) == null) return 'Enter a valid number';
                        return null;
                      },
                    ),
                    const SizedBox(height: 8),
                    Container(
                      padding: const EdgeInsets.all(10),
                      decoration: BoxDecoration(
                        color: AppColors.warning.withOpacity(0.07),
                        borderRadius: BorderRadius.circular(10),
                        border: Border.all(color: AppColors.warning.withOpacity(0.2)),
                      ),
                      child: const Row(children: [
                        Icon(Icons.info_outline_rounded, size: 14, color: AppColors.warning),
                        SizedBox(width: 8),
                        Expanded(child: Text('Fee is shown to borrowers and is agreed upon before exchange.', style: TextStyle(fontSize: 12, color: AppColors.warning, height: 1.4))),
                      ]),
                    ),
                  ],
                  const SizedBox(height: 24),
                  sectionLabel('Exchange Duration', color: AppColors.success),
                  const SizedBox(height: 10),
                  // ── Mode toggle: Days vs Hours ──
                  Container(
                    padding: const EdgeInsets.all(4),
                    decoration: BoxDecoration(
                      color: isDark ? AppColors.darkInputBg : const Color(0xFFF0F0F8),
                      borderRadius: BorderRadius.circular(14),
                      border: Border.all(color: isDark ? AppColors.darkBorder : AppColors.divider),
                    ),
                    child: Row(children: [
                      Expanded(child: _modeBtn('days', Icons.calendar_month_rounded, 'By Date')),
                      Expanded(child: _modeBtn('hours', Icons.schedule_rounded, 'By Duration')),
                    ]),
                  ),
                  const SizedBox(height: 14),
                  if (_durationMode == 'hours') ...[
                    // ── Hours mode ──
                    GestureDetector(
                      onTap: _pickHoursSheet,
                      child: Container(
                        width: double.infinity,
                        padding: const EdgeInsets.all(16),
                        decoration: BoxDecoration(
                          color: AppColors.accent.withOpacity(0.07),
                          borderRadius: BorderRadius.circular(14),
                          border: Border.all(color: AppColors.accent.withOpacity(0.25)),
                        ),
                        child: Row(children: [
                          Container(width: 40, height: 40,
                            decoration: BoxDecoration(color: AppColors.accent.withOpacity(0.12), borderRadius: BorderRadius.circular(11)),
                            child: const Icon(Icons.schedule_rounded, color: AppColors.accent, size: 22)),
                          const SizedBox(width: 14),
                          Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                            const Text('Loan Duration', style: TextStyle(fontSize: 12, fontWeight: FontWeight.w600, color: AppColors.textSecondary)),
                            const SizedBox(height: 2),
                            Text(
                              _receiveDate != null && _returnDate != null
                                  ? _loanDurationLabel()
                                  : 'Tap to set duration',
                              style: TextStyle(fontSize: 15, fontWeight: FontWeight.w800,
                                color: _receiveDate != null ? AppColors.accent : Colors.grey.shade400),
                            ),
                          ])),
                          Icon(Icons.keyboard_arrow_right_rounded, color: AppColors.accent.withOpacity(0.6)),
                        ]),
                      ),
                    ),
                    if (_receiveDate != null && _returnDate != null) ...[
                      const SizedBox(height: 10),
                      Container(
                        padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 11),
                        decoration: BoxDecoration(
                          color: isDark ? AppColors.darkCardBg : Colors.white,
                          borderRadius: BorderRadius.circular(12),
                          border: Border.all(color: isDark ? AppColors.darkDivider : AppColors.divider),
                        ),
                        child: Column(children: [
                          Row(children: [
                            const Icon(Icons.play_circle_outline_rounded, size: 16, color: AppColors.success),
                            const SizedBox(width: 8),
                            const Text('From:', style: TextStyle(fontSize: 12, color: AppColors.textSecondary)),
                            const SizedBox(width: 6),
                            Text(_formatDateTime(_receiveDate!),
                              style: const TextStyle(fontSize: 13, fontWeight: FontWeight.w700)),
                          ]),
                          const SizedBox(height: 6),
                          Row(children: [
                            const Icon(Icons.stop_circle_outlined, size: 16, color: AppColors.coral),
                            const SizedBox(width: 8),
                            const Text('Until:', style: TextStyle(fontSize: 12, color: AppColors.textSecondary)),
                            const SizedBox(width: 6),
                            Text(_formatDateTime(_returnDate!),
                              style: const TextStyle(fontSize: 13, fontWeight: FontWeight.w700)),
                          ]),
                        ]),
                      ),
                    ],
                  ] else ...[
                    // ── Days mode ──
                    Text(
                      'Set when the borrower receives and returns the toy',
                      style: TextStyle(fontSize: 12.5, color: Colors.grey.shade500),
                    ),
                    const SizedBox(height: 12),
                    _buildDateTile(
                      label: 'Receive Date & Time',
                      subtitle: 'Tap to set',
                      icon: Icons.calendar_month_rounded,
                      date: _receiveDate,
                      onTap: () => _pickDate(true),
                      color: AppColors.primary,
                    ),
                    const SizedBox(height: 10),
                    _buildDateTile(
                      label: 'Return Date & Time',
                      subtitle: 'Tap to set',
                      icon: Icons.event_available_rounded,
                      date: _returnDate,
                      onTap: () => _pickDate(false),
                      color: AppColors.coral,
                    ),
                    if (_receiveDate != null && _returnDate != null) ...[
                      const SizedBox(height: 10),
                      Container(
                        padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 11),
                        decoration: BoxDecoration(
                          color: AppColors.accent.withOpacity(0.08),
                          borderRadius: BorderRadius.circular(12),
                          border: Border.all(color: AppColors.accent.withOpacity(0.2)),
                        ),
                        child: Row(children: [
                          const Icon(Icons.timelapse_rounded, size: 16, color: AppColors.accent),
                          const SizedBox(width: 8),
                          Text(
                            'Loan duration: ${_loanDurationLabel()}',
                            style: const TextStyle(fontSize: 13, color: AppColors.accent, fontWeight: FontWeight.w700),
                          ),
                        ]),
                      ),
                    ],
                  ],
                ],
                const SizedBox(height: 32),
                SizedBox(
                  width: double.infinity,
                  height: 54,
                  child: DecoratedBox(
                    decoration: BoxDecoration(
                      borderRadius: BorderRadius.circular(14),
                      boxShadow: _isSubmitting ? [] : AppShadows.button,
                    ),
                    child: ElevatedButton(
                      onPressed: _isSubmitting ? null : _submit,
                      style: ElevatedButton.styleFrom(
                        backgroundColor: AppColors.primary,
                        foregroundColor: Colors.white,
                        shape: RoundedRectangleBorder(
                            borderRadius: BorderRadius.circular(14)),
                        elevation: 0,
                      ),
                      child: _isSubmitting
                          ? const SizedBox(
                        width: 22,
                        height: 22,
                        child: CircularProgressIndicator(
                            strokeWidth: 2.5, color: Colors.white),
                      )
                          : const Text('List My Toy',
                          style: TextStyle(fontSize: 16, fontWeight: FontWeight.w800)),
                    ),
                  ),
                ),
                const SizedBox(height: 20),
              ]),
            ),
          ),
        ),
      ),
    );
  }

  Widget _modeBtn(String mode, IconData icon, String label) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final isActive = _durationMode == mode;
    return GestureDetector(
      onTap: () => setState(() {
        _durationMode = mode;
        _receiveDate = null;
        _returnDate = null;
        _loanHours = 2;
      }),
      child: AnimatedContainer(
        duration: const Duration(milliseconds: 180),
        padding: const EdgeInsets.symmetric(vertical: 10),
        decoration: BoxDecoration(
          color: isActive ? AppColors.accent : Colors.transparent,
          borderRadius: BorderRadius.circular(11),
          boxShadow: isActive ? [BoxShadow(color: AppColors.accent.withOpacity(0.3), blurRadius: 8, offset: const Offset(0, 2))] : [],
        ),
        child: Row(mainAxisAlignment: MainAxisAlignment.center, children: [
          Icon(icon, size: 16, color: isActive ? Colors.white : (isDark ? AppColors.darkTextSecondary : Colors.grey.shade500)),
          const SizedBox(width: 6),
          Text(label, style: TextStyle(
            fontSize: 13.5, fontWeight: FontWeight.w700,
            color: isActive ? Colors.white : (isDark ? AppColors.darkTextSecondary : Colors.grey.shade500),
          )),
        ]),
      ),
    );
  }

  Widget _buildDropdown(
      String label, String value, List<String> items, void Function(String?) onChanged) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return DropdownButtonFormField<String>(
      value: value,
      decoration: InputDecoration(
        labelText: label,
        contentPadding: const EdgeInsets.symmetric(horizontal: 14, vertical: 14),
        border: OutlineInputBorder(borderRadius: BorderRadius.circular(14)),
        enabledBorder: OutlineInputBorder(
          borderRadius: BorderRadius.circular(14),
          borderSide: BorderSide(color: isDark ? AppColors.darkBorder : const Color(0xFFE0E0F0)),
        ),
        filled: true,
        fillColor: isDark ? AppColors.darkInputBg : const Color(0xFFF8F8FC),
      ),
      icon: Icon(Icons.keyboard_arrow_down_rounded, color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary),
      items: items
          .map((c) => DropdownMenuItem(value: c, child: Text(c, style: const TextStyle(fontSize: 14))))
          .toList(),
      onChanged: onChanged,
    );
  }

  Widget _buildTypeSelector() {
    return Row(children: [
      Expanded(
        child: _typeOption('exchange', 'Exchange',
            Icons.swap_horizontal_circle_rounded, AppColors.primary, 'Lend & get back'),
      ),
      const SizedBox(width: 12),
      Expanded(
        child: _typeOption('donation', 'Donation',
            Icons.volunteer_activism_rounded, AppColors.success, 'Give away forever'),
      ),
    ]);
  }

  Widget _typeOption(String value, String label, IconData icon, Color color, String desc) {
    final selected = _type == value;
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return GestureDetector(
      onTap: () => setState(() {
        _type = value;
        if (value == 'donation') { _receiveDate = null; _returnDate = null; }
      }),
      child: AnimatedContainer(
        duration: const Duration(milliseconds: 220),
        padding: const EdgeInsets.symmetric(vertical: 18, horizontal: 14),
        decoration: BoxDecoration(
          color: selected ? color.withOpacity(0.07) : (isDark ? AppColors.darkInputBg : const Color(0xFFF8F8FC)),
          borderRadius: BorderRadius.circular(16),
          border: Border.all(
            color: selected ? color : (isDark ? AppColors.darkBorder : const Color(0xFFE0E0F0)),
            width: selected ? 2 : 1,
          ),
          boxShadow: selected
              ? [BoxShadow(color: color.withOpacity(0.15), blurRadius: 12, offset: const Offset(0, 4))]
              : [],
        ),
        child: Column(children: [
          Icon(icon, color: selected ? color : Colors.grey.shade400, size: 28),
          const SizedBox(height: 6),
          Text(label,
              style: TextStyle(
                  fontWeight: FontWeight.w800,
                  fontSize: 14,
                  color: selected ? color : Colors.grey.shade500)),
          const SizedBox(height: 3),
          Text(desc,
              style: TextStyle(
                  fontSize: 11,
                  color: selected ? color.withOpacity(0.7) : Colors.grey.shade400)),
        ]),
      ),
    );
  }

  Widget _buildDateTile({
    required String label,
    required String subtitle,
    required IconData icon,
    required DateTime? date,
    required VoidCallback onTap,
    required Color color,
  }) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Material(
      color: Colors.transparent,
      child: InkWell(
        onTap: onTap,
        borderRadius: BorderRadius.circular(14),
        child: AnimatedContainer(
          duration: const Duration(milliseconds: 200),
          padding: const EdgeInsets.all(14),
          decoration: BoxDecoration(
            border: Border.all(
              color: date != null ? color.withOpacity(0.4) : (isDark ? AppColors.darkBorder : const Color(0xFFE0E0F0)),
            ),
            borderRadius: BorderRadius.circular(14),
            color: date != null ? color.withOpacity(0.04) : (isDark ? AppColors.darkInputBg : const Color(0xFFF8F8FC)),
          ),
          child: Row(children: [
            Container(
              width: 44,
              height: 44,
              decoration: BoxDecoration(
                color: color.withOpacity(0.1),
                borderRadius: BorderRadius.circular(12),
              ),
              child: Icon(icon, color: color, size: 22),
            ),
            const SizedBox(width: 12),
            Expanded(
              child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                Text(label,
                    style: TextStyle(
                        fontWeight: FontWeight.w700,
                        fontSize: 14,
                        color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                const SizedBox(height: 2),
                Text(
                  date != null
                      ? _formatDateTime(date)
                      : subtitle,
                  style: TextStyle(
                      fontSize: 12.5,
                      color: date != null ? (isDark ? AppColors.darkTextSecondary : AppColors.textSecondary) : Colors.grey.shade400),
                ),
              ]),
            ),
            Icon(Icons.chevron_right_rounded, color: Colors.grey.shade400, size: 22),
          ]),
        ),
      ),
    );
  }
}

// ==================== MY TOYS SCREEN ====================
class MyToysScreen extends StatelessWidget {
  const MyToysScreen({super.key});

  Color _conditionColor(String c) {
    switch (c) {
      case 'New': return AppColors.success;
      case 'Like New': return AppColors.accent;
      case 'Good': return AppColors.primary;
      case 'Fair': return AppColors.warning;
      default: return AppColors.error;
    }
  }

  Future<void> _delete(BuildContext context, String docId) async {
    final confirm = await showDialog<bool>(
      context: context,
      builder: (_) => AlertDialog(
        title: const Text('Remove Listing?',
            style: TextStyle(fontWeight: FontWeight.w800)),
        content: const Text('This will permanently remove this toy from your listings.'),
        actions: [
          TextButton(
              onPressed: () => Navigator.pop(context, false),
              child: const Text('Cancel')),
          TextButton(
            onPressed: () => Navigator.pop(context, true),
            style: TextButton.styleFrom(foregroundColor: AppColors.error),
            child: const Text('Remove', style: TextStyle(fontWeight: FontWeight.w700)),
          ),
        ],
      ),
    );
    if (confirm != true) return;
    try {
      await FirestoreService.deleteToy(docId);
      if (context.mounted) {
        ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
          content: Text('Toy removed from listings'),
          margin: EdgeInsets.all(14),
        ));
      }
    } catch (e) {
      if (context.mounted) {
        ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
          content: Text('Failed to remove toy. Please try again.'),
          backgroundColor: AppColors.error,
          margin: EdgeInsets.all(14),
        ));
      }
    }
  }

  void _showToyHistory(BuildContext context, String toyId, String toyName) {
    showModalBottomSheet(
      context: context,
      isScrollControlled: true,
      shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.vertical(top: Radius.circular(28)),
      ),
      builder: (ctx) => DraggableScrollableSheet(
        expand: false,
        initialChildSize: 0.6,
        minChildSize: 0.35,
        maxChildSize: 0.92,
        builder: (_, scrollCtrl) => Column(children: [
          Padding(
            padding: const EdgeInsets.fromLTRB(24, 16, 24, 0),
            child: Column(children: [
              Center(
                child: Container(
                  width: 40, height: 4,
                  decoration: BoxDecoration(color: Colors.grey.shade200, borderRadius: BorderRadius.circular(2)),
                ),
              ),
              const SizedBox(height: 16),
              Row(children: [
                Container(
                  width: 40, height: 40,
                  decoration: BoxDecoration(color: AppColors.primary.withOpacity(0.1), borderRadius: BorderRadius.circular(12)),
                  child: const Icon(Icons.history_rounded, color: AppColors.primary, size: 20),
                ),
                const SizedBox(width: 12),
                Expanded(
                  child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                    const Text('Transaction History', style: TextStyle(fontSize: 16, fontWeight: FontWeight.w800, color: AppColors.textPrimary)),
                    Text(toyName, style: const TextStyle(fontSize: 12.5, color: AppColors.textSecondary)),
                  ]),
                ),
              ]),
              const SizedBox(height: 12),
              const Divider(height: 1),
            ]),
          ),
          Expanded(
            child: StreamBuilder<QuerySnapshot>(
              stream: FirestoreService.streamToyHistory(toyId),
              builder: (ctx2, snap) {
                if (snap.connectionState == ConnectionState.waiting && !snap.hasData) {
                  return const Center(child: CircularProgressIndicator(color: AppColors.primary));
                }
                final docs = snap.data?.docs ?? [];
                if (docs.isEmpty) {
                  return emptyState(
                    icon: Icons.swap_horiz_rounded,
                    title: 'No history yet',
                    subtitle: 'Exchange records will appear here',
                    color: AppColors.accent,
                  );
                }
                return ListView.separated(
                  controller: scrollCtrl,
                  padding: const EdgeInsets.fromLTRB(16, 12, 16, 24),
                  itemCount: docs.length,
                  separatorBuilder: (_, __) => const SizedBox(height: 10),
                  itemBuilder: (_, i) {
                    final e = docs[i].data() as Map<String, dynamic>;
                    final status = e['status'] ?? 'active';
                    final holderName = e['holderName'] ?? 'Unknown';
                    final isDonation = e['type'] == 'donation';
                    final isCompleted = status == 'completed';
                    final isPending = status == 'pending_receipt';
                    final isDisputed = status == 'disputed';

                    Color statusColor;
                    String statusLabel;
                    IconData statusIcon;
                    if (isCompleted) {
                      statusColor = AppColors.success;
                      statusLabel = isDonation ? 'Donated' : 'Returned';
                      statusIcon = Icons.check_circle_rounded;
                    } else if (isPending) {
                      statusColor = AppColors.primary;
                      statusLabel = 'Pending Receipt';
                      statusIcon = Icons.hourglass_top_rounded;
                    } else if (isDisputed) {
                      statusColor = AppColors.error;
                      statusLabel = 'Disputed';
                      statusIcon = Icons.warning_amber_rounded;
                    } else {
                      statusColor = AppColors.coral;
                      statusLabel = isDonation ? 'Donated' : 'In-Use';
                      statusIcon = isDonation ? Icons.volunteer_activism_rounded : Icons.play_circle_rounded;
                    }

                    final startTs = e['startedAt'] as Timestamp?;
                    final startDate = startTs != null ? DateFormat('MMM d, yyyy').format(startTs.toDate()) : null;
                    final completedTs = e['completedAt'] as Timestamp?;
                    final completedDate = completedTs != null ? DateFormat('MMM d, yyyy').format(completedTs.toDate()) : null;
                    final returnDateStr = e['returnDate'] as String?;
                    final returnDate = returnDateStr != null ? DateTime.tryParse(returnDateStr) : null;

                    return Container(
                      padding: const EdgeInsets.all(14),
                      decoration: BoxDecoration(
                        color: Theme.of(ctx2).cardColor,
                        borderRadius: BorderRadius.circular(16),
                        border: Border.all(color: Theme.of(ctx2).brightness == Brightness.dark ? AppColors.darkDivider : AppColors.divider),
                      ),
                      child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                        Row(children: [
                          UserAvatar(name: holderName, size: 38, gradient: isDonation ? AppColors.cardGradientTeal : AppColors.heroGradient),
                          const SizedBox(width: 10),
                          Expanded(
                            child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                              Text(holderName,
                                style: TextStyle(fontSize: 14, fontWeight: FontWeight.w800,
                                  color: Theme.of(ctx2).brightness == Brightness.dark ? AppColors.darkTextPrimary : AppColors.textPrimary),
                              ),
                              if (startDate != null)
                                Text('Started $startDate', style: TextStyle(fontSize: 11.5, color: Colors.grey.shade500)),
                            ]),
                          ),
                          Container(
                            padding: const EdgeInsets.symmetric(horizontal: 9, vertical: 5),
                            decoration: BoxDecoration(color: statusColor.withOpacity(0.1), borderRadius: BorderRadius.circular(20)),
                            child: Row(mainAxisSize: MainAxisSize.min, children: [
                              Icon(statusIcon, size: 12, color: statusColor),
                              const SizedBox(width: 4),
                              Text(statusLabel, style: TextStyle(fontSize: 11, fontWeight: FontWeight.w700, color: statusColor)),
                            ]),
                          ),
                        ]),
                        if (isDonation) ...[
                          const SizedBox(height: 8),
                          Container(
                            padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
                            decoration: BoxDecoration(color: AppColors.success.withOpacity(0.07), borderRadius: BorderRadius.circular(8)),
                            child: Row(children: [
                              const Icon(Icons.volunteer_activism_rounded, size: 13, color: AppColors.success),
                              const SizedBox(width: 6),
                              Text(isCompleted ? 'Donated to $holderName' : 'Donation in progress',
                                style: const TextStyle(fontSize: 12, color: AppColors.success, fontWeight: FontWeight.w600)),
                            ]),
                          ),
                        ] else ...[
                          if (returnDate != null || completedDate != null) ...[
                            const SizedBox(height: 10),
                            const Divider(height: 1),
                            const SizedBox(height: 8),
                            Row(children: [
                              if (returnDate != null) ...[
                                const Icon(Icons.event_available_rounded, size: 13, color: AppColors.textSecondary),
                                const SizedBox(width: 4),
                                Text('Due: ${DateFormat('MMM d, yyyy').format(returnDate)}',
                                  style: TextStyle(fontSize: 12, color: Colors.grey.shade600, fontWeight: FontWeight.w600)),
                                const SizedBox(width: 12),
                              ],
                              if (completedDate != null) ...[
                                const Icon(Icons.check_circle_outline_rounded, size: 13, color: AppColors.success),
                                const SizedBox(width: 4),
                                Text('Returned: $completedDate',
                                  style: const TextStyle(fontSize: 12, color: AppColors.success, fontWeight: FontWeight.w600)),
                              ],
                            ]),
                          ],
                          if (isDisputed && (e['disputeReason'] ?? '').isNotEmpty) ...[
                            const SizedBox(height: 8),
                            Container(
                              padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
                              decoration: BoxDecoration(color: AppColors.error.withOpacity(0.06), borderRadius: BorderRadius.circular(8)),
                              child: Row(children: [
                                const Icon(Icons.warning_amber_rounded, size: 13, color: AppColors.error),
                                const SizedBox(width: 6),
                                Expanded(child: Text('Dispute: ${e['disputeReason']}',
                                  style: const TextStyle(fontSize: 11.5, color: AppColors.error, fontWeight: FontWeight.w600))),
                              ]),
                            ),
                          ],
                        ],
                      ]),
                    );
                  },
                );
              },
            ),
          ),
        ]),
      ),
    );
  }

  @override
  Widget build(BuildContext context) {
    final uid = _auth.currentUser?.uid;
    final isDark = Theme.of(context).brightness == Brightness.dark;
    if (uid == null) {
      return Scaffold(
        appBar: AppBar(title: const Text('My Toys')),
        body: emptyState(
          icon: Icons.person_off_rounded,
          title: 'Not logged in',
          subtitle: 'Please sign in to view your toys',
        ),
      );
    }

    return Scaffold(
      appBar: AppBar(title: const Text('My Toys')),
      floatingActionButton: FloatingActionButton.extended(
        onPressed: () => navigateTo(context, const AddToyScreen()),
        icon: const Icon(Icons.add_box_rounded),
        label: const Text('Add Toy', style: TextStyle(fontWeight: FontWeight.w700)),
      ),
      body: StreamBuilder<QuerySnapshot>(
        stream: FirestoreService.streamMyToys(),
        builder: (context, snap) {
          if (snap.hasError) {
            final errorMsg = snap.error.toString();
            debugPrint('MyToys stream error: $errorMsg');
            if (errorMsg.contains('index') || errorMsg.contains('FAILED_PRECONDITION')) {
              return errorState(
                message: 'Database index required. Please check Firebase Console for the index creation link in your debug logs.',
              );
            }
            return errorState(message: 'Failed to load toys. Pull down to retry.');
          }
          if (snap.connectionState == ConnectionState.waiting && !snap.hasData) {
            return const Center(child: CircularProgressIndicator(color: AppColors.primary));
          }
          final docs = snap.data?.docs ?? [];
          if (docs.isEmpty) {
            return emptyState(
                icon: Icons.inventory_2_rounded,
                title: 'No toys listed yet',
                subtitle: 'Tap the button below to share your first toy',
                color: AppColors.coral);
          }
          return Center(
            child: ConstrainedBox(
              constraints: const BoxConstraints(maxWidth: 800),
              child: ListView.separated(
                padding: const EdgeInsets.fromLTRB(16, 16, 16, 100),
                itemCount: docs.length,
                separatorBuilder: (_, __) => const SizedBox(height: 10),
                itemBuilder: (_, i) {
                  final doc = docs[i];
                  final data = doc.data() as Map<String, dynamic>;
                  final isExchange = data['type'] == 'exchange';
                  final typeColor = isExchange ? AppColors.primary : AppColors.success;
                  final isAvail = data['isAvailable'] == true;
                  final condition = data['condition'] ?? 'Good';
                  final receiveDate = data['receiveDate'] != null ? DateTime.tryParse(data['receiveDate']) : null;
                  final returnDate = data['returnDate'] != null ? DateTime.tryParse(data['returnDate']) : null;
                  final currentHolderName = data['currentHolderName'] as String?;
                  final toyStatus = data['toyStatus'] as String? ?? (isAvail ? 'available' : 'taken');

                  return Container(
                    decoration: BoxDecoration(
                      color: Theme.of(context).cardColor,
                      borderRadius: BorderRadius.circular(18),
                      boxShadow: AppShadows.card(context),
                    ),
                    child: Padding(
                      padding: const EdgeInsets.all(16),
                      child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                        Wrap(
                          spacing: 6,
                          runSpacing: 6,
                          crossAxisAlignment: WrapCrossAlignment.center,
                          children: [
                          ToyChip(
                            label: (data['type'] ?? 'exchange').toUpperCase(),
                            color: typeColor.withOpacity(0.1),
                            textColor: typeColor,
                            icon: isExchange ? Icons.swap_horiz_rounded : Icons.volunteer_activism_rounded,
                          ),
                          ToyChip(
                            label: toyStatus == 'exchanged'
                                ? 'IN-USE'
                                : toyStatus == 'pending_receipt'
                                ? 'PENDING RECEIPT'
                                : toyStatus == 'disputed'
                                ? 'DISPUTED'
                                : isAvail ? 'AVAILABLE' : 'TAKEN',
                            color: (toyStatus == 'available' || isAvail && toyStatus != 'exchanged' && toyStatus != 'pending_receipt' && toyStatus != 'disputed')
                                ? AppColors.success.withOpacity(0.1)
                                : toyStatus == 'pending_receipt'
                                ? AppColors.primary.withOpacity(0.1)
                                : toyStatus == 'disputed'
                                ? AppColors.error.withOpacity(0.1)
                                : AppColors.warning.withOpacity(0.15),
                            textColor: (toyStatus == 'available' || isAvail && toyStatus != 'exchanged' && toyStatus != 'pending_receipt' && toyStatus != 'disputed')
                                ? AppColors.success
                                : toyStatus == 'pending_receipt'
                                ? AppColors.primary
                                : toyStatus == 'disputed'
                                ? AppColors.error
                                : AppColors.coral,
                          ),
                          ToyChip(
                            label: condition.toUpperCase(),
                            color: _conditionColor(condition).withOpacity(0.1),
                            textColor: _conditionColor(condition),
                          ),
                        ]),
                        // Delete button aligned to top-right separately
                        Align(
                          alignment: Alignment.centerRight,
                          child: IconButton(
                            icon: Container(
                              padding: const EdgeInsets.all(6),
                              decoration: BoxDecoration(
                                color: AppColors.error.withOpacity(0.08),
                                borderRadius: BorderRadius.circular(8),
                              ),
                              child: Icon(Icons.delete_rounded,
                                  color: AppColors.error.withOpacity(0.75), size: 18),
                            ),
                            onPressed: () => _delete(context, doc.id),
                            visualDensity: VisualDensity.compact,
                            tooltip: 'Remove listing',
                          ),
                        ),
                        const SizedBox(height: 12),
                        Text(
                          data['name'] ?? '',
                          style: TextStyle(
                              fontSize: 16,
                              fontWeight: FontWeight.w800,
                              color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
                              letterSpacing: -0.3),
                        ),
                        const SizedBox(height: 4),
                        Text(
                          data['description'] ?? '',
                          style: TextStyle(fontSize: 13, color: isDark ? AppColors.darkTextSecondary : Colors.grey.shade600, height: 1.4),
                          maxLines: 2,
                          overflow: TextOverflow.ellipsis,
                        ),
                        if (data['ageGroup'] != null || data['category'] != null) ...[
                          const SizedBox(height: 10),
                          Wrap(spacing: 6, runSpacing: 4, children: [
                            if (data['category'] != null)
                              ToyChip(
                                label: data['category'],
                                color: const Color(0xFFEFF6FF),
                                textColor: const Color(0xFF2563EB),
                                icon: Icons.category_outlined,
                              ),
                            if (data['ageGroup'] != null)
                              ToyChip(
                                label: 'Age ${data['ageGroup']}',
                                color: const Color(0xFFF5F3FF),
                                textColor: AppColors.primary,
                                icon: Icons.child_care_rounded,
                              ),
                          ]),
                        ],
                        if (!isAvail && currentHolderName != null && currentHolderName.isNotEmpty) ...[
                          const SizedBox(height: 12),
                          Container(
                            padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 10),
                            decoration: BoxDecoration(
                              color: AppColors.primary.withOpacity(0.06),
                              borderRadius: BorderRadius.circular(12),
                              border: Border.all(color: AppColors.primary.withOpacity(0.15)),
                            ),
                            child: Row(children: [
                              UserAvatar(name: currentHolderName, size: 28, gradient: AppColors.heroGradient),
                              const SizedBox(width: 10),
                              Expanded(
                                child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                                  const Text('Currently with',
                                      style: TextStyle(fontSize: 11, color: AppColors.textSecondary, fontWeight: FontWeight.w600)),
                                  Text(currentHolderName,
                                      style: const TextStyle(fontSize: 13.5, fontWeight: FontWeight.w800, color: AppColors.primary)),
                                ]),
                              ),
                              if (returnDate != null) ...[
                                Column(crossAxisAlignment: CrossAxisAlignment.end, children: [
                                  const Text('Due back',
                                      style: TextStyle(fontSize: 10.5, color: AppColors.textSecondary)),
                                  Text(DateFormat('MMM d').format(returnDate),
                                      style: const TextStyle(fontSize: 12.5, fontWeight: FontWeight.w700, color: AppColors.coral)),
                                ]),
                              ],
                            ]),
                          ),
                        ],
                        if (isExchange && receiveDate != null && returnDate != null && isAvail) ...[
                          const SizedBox(height: 12),
                          const Divider(height: 1),
                          const SizedBox(height: 12),
                          Row(children: [
                            Icon(Icons.calendar_month_rounded, size: 14, color: Colors.grey.shade500),
                            const SizedBox(width: 5),
                            Text(DateFormat('MMM d').format(receiveDate),
                                style: TextStyle(fontSize: 12.5, color: Colors.grey.shade700, fontWeight: FontWeight.w600)),
                            Padding(
                              padding: const EdgeInsets.symmetric(horizontal: 8),
                              child: Icon(Icons.arrow_forward_rounded, size: 13, color: Colors.grey.shade400),
                            ),
                            Icon(Icons.event_available_rounded, size: 14, color: Colors.grey.shade500),
                            const SizedBox(width: 5),
                            Text(DateFormat('MMM d, yyyy').format(returnDate),
                                style: TextStyle(fontSize: 12.5, color: Colors.grey.shade700, fontWeight: FontWeight.w600)),
                          ]),
                        ],
                        const SizedBox(height: 10),
                        const Divider(height: 1),
                        const SizedBox(height: 6),
                        GestureDetector(
                          onTap: () => _showToyHistory(context, doc.id, data['name'] ?? ''),
                          child: Row(children: [
                            const Icon(Icons.history_rounded, size: 15, color: AppColors.textSecondary),
                            const SizedBox(width: 6),
                            const Text('View Transaction History',
                                style: TextStyle(fontSize: 12.5, color: AppColors.primary, fontWeight: FontWeight.w600)),
                            const Spacer(),
                            const Icon(Icons.chevron_right_rounded, size: 16, color: AppColors.textSecondary),
                          ]),
                        ),
                      ]),
                    ),
                  );
                },
              ),
            ),
          );
        },
      ),
    );
  }
}

// ==================== USER DASHBOARD (MY ACTIVITIES) SCREEN ====================
class UserDashboardScreen extends StatefulWidget {
  const UserDashboardScreen({super.key});
  @override
  State<UserDashboardScreen> createState() => _UserDashboardScreenState();
}

class _UserDashboardScreenState extends State<UserDashboardScreen>
    with SingleTickerProviderStateMixin {
  late TabController _tabCtrl;

  @override
  void initState() {
    super.initState();
    _tabCtrl = TabController(length: 2, vsync: this);
  }

  @override
  void dispose() {
    _tabCtrl.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('My Activities'),
        bottom: TabBar(
          controller: _tabCtrl,
          indicatorColor: AppColors.primary,
          labelColor: AppColors.primary,
          unselectedLabelColor: AppColors.textSecondary,
          labelStyle: const TextStyle(fontWeight: FontWeight.w700, fontSize: 13.5),
          tabs: const [
            Tab(icon: Icon(Icons.call_made_rounded, size: 18), text: 'Lending'),
            Tab(icon: Icon(Icons.call_received_rounded, size: 18), text: 'Borrowing'),
          ],
        ),
      ),
      body: TabBarView(
        controller: _tabCtrl,
        children: [
          _LendingTab(),
          _BorrowingTab(),
        ],
      ),
    );
  }
}

class _LendingTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return StreamBuilder<QuerySnapshot>(
      stream: FirestoreService.streamExchangesOwned(),
      builder: (context, snap) {
        if (snap.hasError) return errorState(message: 'Failed to load lending data.');
        if (snap.connectionState == ConnectionState.waiting && !snap.hasData) {
          return const Center(child: CircularProgressIndicator(color: AppColors.primary));
        }
        final allDocs = snap.data?.docs ?? [];
        final activeDocs = allDocs.where((d) {
          final s = (d.data() as Map)['status'];
          return s == 'active' || s == 'pending_receipt' || s == 'disputed';
        }).toList();

        if (activeDocs.isEmpty) {
          return emptyState(
            icon: Icons.call_made_rounded,
            title: 'Not lending anything',
            subtitle: 'Toys you have lent to others will appear here',
            color: AppColors.coral,
          );
        }

        return Center(
          child: ConstrainedBox(
            constraints: const BoxConstraints(maxWidth: 800),
            child: ListView.separated(
              padding: const EdgeInsets.all(16),
              itemCount: activeDocs.length,
              separatorBuilder: (_, __) => const SizedBox(height: 10),
              itemBuilder: (_, i) {
                final e = activeDocs[i].data() as Map<String, dynamic>;
                final holderName = e['holderName'] ?? 'Unknown';
                final toyName = e['toyName'] ?? '';
                final returnDate = e['returnDate'] != null ? DateTime.tryParse(e['returnDate']) : null;
                final isPending = e['status'] == 'pending_receipt';
                final isDisputed = e['status'] == 'disputed';
                final daysLeft = returnDate != null ? returnDate.difference(DateTime.now()).inDays : null;
                final isOverdue = daysLeft != null && daysLeft < 0;

                return Container(
                  decoration: BoxDecoration(
                    color: Theme.of(context).cardColor,
                    borderRadius: BorderRadius.circular(18),
                    boxShadow: AppShadows.card(context),
                    border: isPending ? Border.all(color: AppColors.primary.withOpacity(0.3), width: 1.5)
                        : isDisputed ? Border.all(color: AppColors.error.withOpacity(0.3), width: 1.5)
                        : null,
                  ),
                  child: Padding(
                    padding: const EdgeInsets.all(16),
                    child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                      Row(children: [
                        Expanded(
                          child: Text(toyName,
                              style: TextStyle(fontSize: 15, fontWeight: FontWeight.w800, color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                        ),
                        ToyChip(
                          label: isDisputed ? 'DISPUTED' : isPending ? 'PENDING RECEIPT' : 'IN-USE',
                          color: isDisputed ? AppColors.error.withOpacity(0.1) : isPending ? AppColors.primary.withOpacity(0.1) : AppColors.warning.withOpacity(0.12),
                          textColor: isDisputed ? AppColors.error : isPending ? AppColors.primary : AppColors.coral,
                        ),
                      ]),
                      const SizedBox(height: 10),
                      Row(children: [
                        UserAvatar(name: holderName, size: 32, gradient: AppColors.heroGradient),
                        const SizedBox(width: 10),
                        Expanded(
                          child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                            const Text('Borrower', style: TextStyle(fontSize: 11, color: AppColors.textSecondary, fontWeight: FontWeight.w600)),
                            Text(holderName, style: TextStyle(fontSize: 13.5, fontWeight: FontWeight.w700, color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                          ]),
                        ),
                        if (returnDate != null) ...[
                          Column(crossAxisAlignment: CrossAxisAlignment.end, children: [
                            const Text('Due back', style: TextStyle(fontSize: 11, color: AppColors.textSecondary)),
                            Text(
                              DateFormat('MMM d, yyyy').format(returnDate),
                              style: TextStyle(
                                fontSize: 12.5,
                                fontWeight: FontWeight.w700,
                                color: isOverdue ? AppColors.error : AppColors.coral,
                              ),
                            ),
                          ]),
                        ],
                      ]),
                      if (daysLeft != null) ...[
                        const SizedBox(height: 8),
                        Container(
                          padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
                          decoration: BoxDecoration(
                            color: isOverdue ? AppColors.error.withOpacity(0.07) : AppColors.accent.withOpacity(0.07),
                            borderRadius: BorderRadius.circular(8),
                          ),
                          child: Row(children: [
                            Icon(
                              isOverdue ? Icons.warning_amber_rounded : Icons.timer_rounded,
                              size: 14,
                              color: isOverdue ? AppColors.error : AppColors.accent,
                            ),
                            const SizedBox(width: 6),
                            Text(
                              isOverdue
                                  ? '${daysLeft.abs()} day(s) overdue'
                                  : daysLeft == 0
                                  ? 'Due today!'
                                  : '$daysLeft day(s) remaining',
                              style: TextStyle(
                                fontSize: 12,
                                fontWeight: FontWeight.w600,
                                color: isOverdue ? AppColors.error : AppColors.accent,
                              ),
                            ),
                          ]),
                        ),
                      ],
                      if (isPending) ...[
                        const SizedBox(height: 10),
                        Container(
                          padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
                          decoration: BoxDecoration(
                            color: AppColors.primary.withOpacity(0.06),
                            borderRadius: BorderRadius.circular(10),
                            border: Border.all(color: AppColors.primary.withOpacity(0.2)),
                          ),
                          child: const Row(children: [
                            Icon(Icons.info_outline_rounded, size: 15, color: AppColors.primary),
                            SizedBox(width: 8),
                            Expanded(
                              child: Text('Borrower has initiated return. Go to Exchanges to confirm receipt.',
                                  style: TextStyle(fontSize: 12, color: AppColors.primary, fontWeight: FontWeight.w600)),
                            ),
                          ]),
                        ),
                      ],
                      if (isDisputed) ...[
                        const SizedBox(height: 10),
                        Container(
                          padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
                          decoration: BoxDecoration(
                            color: AppColors.error.withOpacity(0.06),
                            borderRadius: BorderRadius.circular(10),
                            border: Border.all(color: AppColors.error.withOpacity(0.2)),
                          ),
                          child: Row(children: [
                            const Icon(Icons.warning_amber_rounded, size: 15, color: AppColors.error),
                            const SizedBox(width: 8),
                            Expanded(
                              child: Text('This exchange is under dispute: ${e['disputeReason'] ?? 'No reason provided'}',
                                  style: const TextStyle(fontSize: 12, color: AppColors.error, fontWeight: FontWeight.w600)),
                            ),
                          ]),
                        ),
                      ],
                    ]),
                  ),
                );
              },
            ),
          ),
        );
      },
    );
  }
}

class _BorrowingTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return StreamBuilder<QuerySnapshot>(
      stream: FirestoreService.streamExchangesHeld(),
      builder: (context, snap) {
        if (snap.hasError) return errorState(message: 'Failed to load borrowing data.');
        if (snap.connectionState == ConnectionState.waiting && !snap.hasData) {
          return const Center(child: CircularProgressIndicator(color: AppColors.primary));
        }
        final allDocs = snap.data?.docs ?? [];
        final activeDocs = allDocs.where((d) {
          final s = (d.data() as Map)['status'];
          return s == 'active' || s == 'pending_receipt' || s == 'disputed';
        }).toList();

        if (activeDocs.isEmpty) {
          return emptyState(
            icon: Icons.call_received_rounded,
            title: 'Not borrowing anything',
            subtitle: 'Toys you are borrowing will appear here',
            color: AppColors.primary,
          );
        }

        return Center(
          child: ConstrainedBox(
            constraints: const BoxConstraints(maxWidth: 800),
            child: ListView.separated(
              padding: const EdgeInsets.all(16),
              itemCount: activeDocs.length,
              separatorBuilder: (_, __) => const SizedBox(height: 10),
              itemBuilder: (_, i) {
                final doc = activeDocs[i];
                final e = doc.data() as Map<String, dynamic>;
                final ownerName = e['ownerName'] ?? 'Unknown';
                final toyName = e['toyName'] ?? '';
                final returnDate = e['returnDate'] != null ? DateTime.tryParse(e['returnDate']) : null;
                final isActive = e['status'] == 'active';
                final isPending = e['status'] == 'pending_receipt';
                final isDisputed = e['status'] == 'disputed';
                final daysLeft = returnDate != null ? returnDate.difference(DateTime.now()).inDays : null;
                final isOverdue = daysLeft != null && daysLeft < 0;

                return Container(
                  decoration: BoxDecoration(
                    color: Theme.of(context).cardColor,
                    borderRadius: BorderRadius.circular(18),
                    boxShadow: AppShadows.card(context),
                  ),
                  child: Padding(
                    padding: const EdgeInsets.all(16),
                    child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                      Row(children: [
                        Expanded(
                          child: Text(toyName,
                              style: TextStyle(fontSize: 15, fontWeight: FontWeight.w800, color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                        ),
                        ToyChip(
                          label: isDisputed ? 'DISPUTED' : isPending ? 'RETURN INITIATED' : 'IN MY POSSESSION',
                          color: isDisputed ? AppColors.error.withOpacity(0.1) : isPending ? AppColors.primary.withOpacity(0.1) : AppColors.accent.withOpacity(0.1),
                          textColor: isDisputed ? AppColors.error : isPending ? AppColors.primary : AppColors.accent,
                        ),
                      ]),
                      const SizedBox(height: 10),
                      Row(children: [
                        UserAvatar(name: ownerName, size: 32, gradient: AppColors.cardGradientTeal),
                        const SizedBox(width: 10),
                        Expanded(
                          child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                            const Text('Owner', style: TextStyle(fontSize: 11, color: AppColors.textSecondary, fontWeight: FontWeight.w600)),
                            Text(ownerName, style: TextStyle(fontSize: 13.5, fontWeight: FontWeight.w700, color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                          ]),
                        ),
                        if (returnDate != null) ...[
                          Column(crossAxisAlignment: CrossAxisAlignment.end, children: [
                            const Text('Return by', style: TextStyle(fontSize: 11, color: AppColors.textSecondary)),
                            Text(
                              DateFormat('MMM d, yyyy').format(returnDate),
                              style: TextStyle(
                                fontSize: 12.5,
                                fontWeight: FontWeight.w700,
                                color: isOverdue ? AppColors.error : AppColors.coral,
                              ),
                            ),
                          ]),
                        ],
                      ]),
                      if (daysLeft != null) ...[
                        const SizedBox(height: 8),
                        Container(
                          padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
                          decoration: BoxDecoration(
                            color: isOverdue ? AppColors.error.withOpacity(0.07) : AppColors.accent.withOpacity(0.07),
                            borderRadius: BorderRadius.circular(8),
                          ),
                          child: Row(children: [
                            Icon(
                              isOverdue ? Icons.warning_amber_rounded : Icons.timer_rounded,
                              size: 14,
                              color: isOverdue ? AppColors.error : AppColors.accent,
                            ),
                            const SizedBox(width: 6),
                            Text(
                              isOverdue
                                  ? '${daysLeft.abs()} day(s) overdue'
                                  : daysLeft == 0 ? 'Due today!' : '$daysLeft day(s) remaining',
                              style: TextStyle(
                                fontSize: 12,
                                fontWeight: FontWeight.w600,
                                color: isOverdue ? AppColors.error : AppColors.accent,
                              ),
                            ),
                          ]),
                        ),
                      ],
                      if (isActive) ...[
                        const SizedBox(height: 12),
                        SizedBox(
                          width: double.infinity,
                          child: ElevatedButton.icon(
                            onPressed: () => navigateTo(context, const MyExchangesScreen()),
                            icon: const Icon(Icons.assignment_return_rounded, size: 16),
                            label: const Text('Return This Toy', style: TextStyle(fontWeight: FontWeight.w800)),
                            style: ElevatedButton.styleFrom(
                              backgroundColor: AppColors.coral,
                              foregroundColor: Colors.white,
                              padding: const EdgeInsets.symmetric(vertical: 11),
                              shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                              elevation: 0,
                            ),
                          ),
                        ),
                      ],
                      if (isPending) ...[
                        const SizedBox(height: 10),
                        Container(
                          padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
                          decoration: BoxDecoration(
                            color: AppColors.primary.withOpacity(0.06),
                            borderRadius: BorderRadius.circular(10),
                            border: Border.all(color: AppColors.primary.withOpacity(0.2)),
                          ),
                          child: const Row(children: [
                            Icon(Icons.hourglass_top_rounded, size: 15, color: AppColors.primary),
                            SizedBox(width: 8),
                            Expanded(
                              child: Text('Return initiated — waiting for owner to confirm receipt.',
                                  style: TextStyle(fontSize: 12, color: AppColors.primary, fontWeight: FontWeight.w600)),
                            ),
                          ]),
                        ),
                      ],
                      if (isDisputed) ...[
                        const SizedBox(height: 10),
                        Container(
                          padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
                          decoration: BoxDecoration(
                            color: AppColors.error.withOpacity(0.06),
                            borderRadius: BorderRadius.circular(10),
                            border: Border.all(color: AppColors.error.withOpacity(0.2)),
                          ),
                          child: Row(children: [
                            const Icon(Icons.warning_amber_rounded, size: 15, color: AppColors.error),
                            const SizedBox(width: 8),
                            Expanded(
                              child: Text('This exchange is under dispute: ${e['disputeReason'] ?? 'No reason provided'}',
                                  style: const TextStyle(fontSize: 12, color: AppColors.error, fontWeight: FontWeight.w600)),
                            ),
                          ]),
                        ),
                      ],
                    ]),
                  ),
                );
              },
            ),
          ),
        );
      },
    );
  }
}

// ==================== AVAILABLE TOYS SCREEN ====================
// ── Toy Card Skeleton for Browse loading state ──
class _ToyCardSkeleton extends StatefulWidget {
  const _ToyCardSkeleton();
  @override
  State<_ToyCardSkeleton> createState() => _ToyCardSkeletonState();
}

class _ToyCardSkeletonState extends State<_ToyCardSkeleton> with SingleTickerProviderStateMixin {
  late AnimationController _ctrl;
  late Animation<double> _anim;
  @override
  void initState() {
    super.initState();
    _ctrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 900));
    _anim = Tween<double>(begin: 0.4, end: 1.0).animate(CurvedAnimation(parent: _ctrl, curve: Curves.easeInOut));
    _ctrl.repeat(reverse: true);
  }
  @override
  void dispose() { _ctrl.dispose(); super.dispose(); }
  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    Widget box(double w, double h, {bool circle = false}) => Container(
      width: w == 0 ? double.infinity : w, height: h,
      decoration: BoxDecoration(
        color: isDark ? Colors.grey.shade800.withOpacity(_anim.value) : Colors.grey.shade200.withOpacity(_anim.value),
        borderRadius: circle ? BorderRadius.circular(100) : BorderRadius.circular(8),
      ),
    );
    return AnimatedBuilder(
      animation: _anim,
      builder: (_, __) => Container(
        padding: const EdgeInsets.all(16),
        decoration: BoxDecoration(
          color: isDark ? AppColors.darkCardBg : Colors.white,
          borderRadius: BorderRadius.circular(18),
          boxShadow: AppShadows.card(context),
        ),
        child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
          Row(children: [box(80, 22), const SizedBox(width: 8), box(56, 22)]),
          const SizedBox(height: 12),
          box(0, 17),
          const SizedBox(height: 6),
          box(200, 13),
          const SizedBox(height: 14),
          Row(children: [
            box(24, 24, circle: true), const SizedBox(width: 8), box(70, 13),
            const Spacer(), box(80, 32),
          ]),
        ]),
      ),
    );
  }
}

class AvailableToysScreen extends StatefulWidget {
  const AvailableToysScreen({super.key});
  @override
  State<AvailableToysScreen> createState() => _AvailableToysScreenState();
}

class _AvailableToysScreenState extends State<AvailableToysScreen> {
  String _search = '';
  String _filter = 'all';
  final _searchCtrl = TextEditingController();

  @override
  void dispose() {
    _searchCtrl.dispose();
    super.dispose();
  }

  List<QueryDocumentSnapshot> _applyFilters(List<QueryDocumentSnapshot> docs) {
    var list = docs.toList();

    if (_search.isNotEmpty) {
      final q = _search.toLowerCase();
      list = list.where((doc) {
        final d = doc.data() as Map<String, dynamic>;
        return (d['name']?.toString().toLowerCase().contains(q) ?? false) ||
            (d['description']?.toString().toLowerCase().contains(q) ?? false) ||
            (d['category']?.toString().toLowerCase().contains(q) ?? false);
      }).toList();
    }
    if (_filter != 'all') {
      list = list.where((doc) {
        final d = doc.data() as Map<String, dynamic>;
        return d['type'] == _filter;
      }).toList();
    }
    return list;
  }

  void _requestToy(BuildContext context, QueryDocumentSnapshot doc) async {
    final data = doc.data() as Map<String, dynamic>;

    final canBorrow = await FirestoreService.canBorrowMore();
    if (!canBorrow) {
      if (context.mounted) {
        ScaffoldMessenger.of(context).showSnackBar(SnackBar(
          content: const Row(children: [
            Icon(Icons.warning_amber_rounded, color: Colors.white, size: 18),
            SizedBox(width: 8),
            Expanded(child: Text('You have reached the maximum limit of ${FirestoreService.maxActiveBorrows} active borrows. Please return some toys first.', style: TextStyle(fontWeight: FontWeight.w600))),
          ]),
          backgroundColor: AppColors.error,
          margin: const EdgeInsets.all(14),
        ));
      }
      return;
    }

    final messageCtrl = TextEditingController();
    showModalBottomSheet(
      context: context,
      isScrollControlled: true,
      builder: (ctx) => Padding(
        padding: EdgeInsets.only(bottom: MediaQuery.of(ctx).viewInsets.bottom),
        child: Padding(
          padding: const EdgeInsets.fromLTRB(24, 20, 24, 28),
          child: Column(mainAxisSize: MainAxisSize.min, children: [
            Center(
              child: Container(
                width: 40,
                height: 4,
                decoration: BoxDecoration(
                    color: Colors.grey.shade200, borderRadius: BorderRadius.circular(2)),
              ),
            ),
            const SizedBox(height: 20),
            const Icon(Icons.send_rounded, size: 36, color: AppColors.primary),
            const SizedBox(height: 12),
            Text(
              'Request "${data['name']}"',
              style: const TextStyle(
                  fontSize: 18, fontWeight: FontWeight.w900, color: AppColors.textPrimary),
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 6),
            Text(
              'from ${data['ownerName'] ?? 'the owner'}',
              style: const TextStyle(color: AppColors.textSecondary, fontSize: 13.5),
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 8),
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                UserRatingBadge(userId: data['ownerId'] ?? ''),
              ],
            ),
            const SizedBox(height: 18),
            TextField(
              controller: messageCtrl,
              style: const TextStyle(fontSize: 14),
              maxLines: 3,
              decoration: InputDecoration(
                hintText: 'Add a message to the owner (optional)...',
                border: OutlineInputBorder(borderRadius: BorderRadius.circular(14)),
                filled: true,
                fillColor: Theme.of(ctx).brightness == Brightness.dark
                    ? AppColors.darkInputBg
                    : const Color(0xFFF8F8FC),
              ),
            ),
            const SizedBox(height: 18),
            Row(children: [
              TextButton(
                onPressed: () => Navigator.pop(ctx),
                child: const Text('Cancel',
                    style: TextStyle(color: AppColors.textSecondary)),
              ),
              const Spacer(),
              ElevatedButton.icon(
                onPressed: () async {
                  Navigator.pop(ctx);
                  try {
                    await FirestoreService.requestToy(
                      toyDocId: doc.id,
                      toyName: data['name'] ?? '',
                      ownerUid: data['ownerId'] ?? '',
                      message: messageCtrl.text.trim(),
                    );
                    if (context.mounted) {
                      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
                        content: Row(children: [
                          Icon(Icons.check_circle_rounded, color: Colors.white, size: 18),
                          SizedBox(width: 8),
                          Expanded(child: Text('Request sent!',
                              style: TextStyle(fontWeight: FontWeight.w600))),
                        ]),
                        backgroundColor: AppColors.success,
                        margin: EdgeInsets.all(14),
                      ));
                    }
                  } catch (e) {
                    if (context.mounted) {
                      final msg = e.toString().contains('own toy')
                          ? 'You cannot request your own toy.'
                          : e.toString().contains('already_requested')
                          ? 'You have already requested this toy. Wait for the owner to respond.'
                          : e.toString().contains('limit')
                          ? e.toString().replaceAll('Exception: ', '')
                          : 'Failed to send request. Please try again.';
                      ScaffoldMessenger.of(context).showSnackBar(SnackBar(
                        content: Row(children: [
                          const Icon(Icons.info_outline_rounded, color: Colors.white, size: 18),
                          const SizedBox(width: 8),
                          Expanded(child: Text(msg, style: const TextStyle(fontWeight: FontWeight.w600))),
                        ]),
                        backgroundColor: e.toString().contains('already_requested')
                            ? AppColors.primary
                            : AppColors.error,
                        margin: const EdgeInsets.all(14),
                      ));
                    }
                  }
                },
                icon: const Icon(Icons.send_rounded, size: 16),
                label: const Text('Send Request',
                    style: TextStyle(fontWeight: FontWeight.w800)),
                style: ElevatedButton.styleFrom(
                  backgroundColor: AppColors.primary,
                  foregroundColor: Colors.white,
                  elevation: 0,
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                ),
              ),
            ]),
          ]),
        ),
      ),
    );
  }

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Scaffold(
      appBar: AppBar(
        title: const Text('Browse Toys'),
        actions: [
          IconButton(
            icon: const Icon(Icons.tune_rounded),
            onPressed: () => setState(() => _filter = _filter == 'all' ? 'exchange' : 'all'),
            tooltip: 'Toggle filter',
          ),
        ],
      ),
      body: Column(children: [
        Container(
          color: Theme.of(context).appBarTheme.backgroundColor,
          padding: const EdgeInsets.fromLTRB(16, 12, 16, 0),
          child: TextField(
            controller: _searchCtrl,
            style: TextStyle(fontSize: 14, color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
            decoration: InputDecoration(
              hintText: 'Search toys, categories...',
              prefixIcon: const Icon(Icons.search_rounded, size: 22, color: AppColors.textSecondary),
              border: OutlineInputBorder(borderRadius: BorderRadius.circular(14)),
              enabledBorder: OutlineInputBorder(
                borderRadius: BorderRadius.circular(14),
                borderSide: BorderSide(color: isDark ? AppColors.darkBorder : AppColors.divider),
              ),
              filled: true,
              fillColor: isDark ? AppColors.darkInputBg : AppColors.surface,
              contentPadding: const EdgeInsets.symmetric(vertical: 13),
              suffixIcon: _search.isNotEmpty
                  ? IconButton(
                icon: const Icon(Icons.clear_rounded, size: 18, color: AppColors.textSecondary),
                onPressed: () { _searchCtrl.clear(); setState(() => _search = ''); },
              ) : null,
            ),
            onChanged: (v) => setState(() => _search = v),
          ),
        ),
        Container(
          color: Theme.of(context).appBarTheme.backgroundColor,
          padding: const EdgeInsets.fromLTRB(12, 10, 12, 12),
          child: Row(children: [
            _filterChip('all', 'All', Icons.apps_rounded),
            const SizedBox(width: 8),
            _filterChip('exchange', 'Exchange', Icons.swap_horiz_rounded),
            const SizedBox(width: 8),
            _filterChip('donation', 'Donation', Icons.favorite_rounded),
          ]),
        ),
        const Divider(height: 1),
        Expanded(
          child: StreamBuilder<QuerySnapshot>(
            stream: FirestoreService.streamAvailableToys(),
            builder: (context, snap) {
              if (snap.hasError) {
                debugPrint('Available toys error: ${snap.error}');
                return errorState(message: 'Failed to load toys. Please try again.');
              }
              if (snap.connectionState == ConnectionState.waiting && !snap.hasData) {
                return const Center(child: CircularProgressIndicator(color: AppColors.primary));
              }
              final toys = _applyFilters(snap.data?.docs ?? []);
              if (toys.isEmpty) {
                return emptyState(
                    icon: Icons.search_off_rounded,
                    title: _search.isNotEmpty ? 'No results for "$_search"' : 'No toys available',
                    subtitle: _search.isNotEmpty ? 'Try a different keyword or clear the filter' : 'Check back soon or list your own toy!',
                    color: AppColors.primary);
              }
              return Column(children: [
                Padding(
                  padding: const EdgeInsets.fromLTRB(16, 10, 16, 4),
                  child: Row(children: [
                    Text('${toys.length} toy${toys.length != 1 ? 's' : ''} found',
                      style: TextStyle(fontSize: 12.5, fontWeight: FontWeight.w600,
                        color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary)),
                    const Spacer(),
                    if (_search.isNotEmpty || _filter != 'all')
                      GestureDetector(
                        onTap: () { _searchCtrl.clear(); setState(() { _search = ''; _filter = 'all'; }); },
                        child: const Text('Clear all', style: TextStyle(fontSize: 12.5, color: AppColors.primary, fontWeight: FontWeight.w700)),
                      ),
                  ]),
                ),
                Expanded(
                  child: Center(
                    child: ConstrainedBox(
                      constraints: const BoxConstraints(maxWidth: 800),
                      child: ListView.separated(
                        padding: const EdgeInsets.all(16),
                        itemCount: toys.length,
                        separatorBuilder: (_, __) => const SizedBox(height: 10),
                        itemBuilder: (_, i) => _buildToyCard(context, toys[i]),
                      ),
                    ),
                  ),
                ),
              ]);
            },
          ),
        ),
      ]),
    );
  }

  Widget _filterChip(String value, String label, IconData icon) {
    final selected = _filter == value;
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return GestureDetector(
      onTap: () => setState(() => _filter = value),
      child: AnimatedContainer(
        duration: const Duration(milliseconds: 180),
        padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 8),
        decoration: BoxDecoration(
          color: selected ? AppColors.primary : Colors.transparent,
          borderRadius: BorderRadius.circular(24),
          border: Border.all(color: selected ? AppColors.primary : (isDark ? AppColors.darkBorder : AppColors.divider)),
        ),
        child: Row(mainAxisSize: MainAxisSize.min, children: [
          Icon(icon, size: 14, color: selected ? Colors.white : Colors.grey.shade500),
          const SizedBox(width: 5),
          Text(
            label,
            style: TextStyle(
              fontSize: 13,
              color: selected ? Colors.white : Colors.grey.shade600,
              fontWeight: selected ? FontWeight.w700 : FontWeight.w500,
            ),
          ),
        ]),
      ),
    );
  }

  Widget _buildToyCard(BuildContext context, QueryDocumentSnapshot doc) {
    final data = doc.data() as Map<String, dynamic>;
    final bool isExchange = data['type'] == 'exchange';
    final Color color = isExchange ? AppColors.primary : AppColors.success;
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final bool isOwn = data['ownerId'] == _auth.currentUser?.uid;
    return Container(
      decoration: BoxDecoration(
        color: Theme.of(context).cardColor,
        borderRadius: BorderRadius.circular(18),
        boxShadow: AppShadows.card(context),
      ),
      child: Material(
        color: Colors.transparent,
        borderRadius: BorderRadius.circular(18),
        child: InkWell(
          onTap: () {
            if (isOwn) {
              ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
                content: Row(children: [
                  Icon(Icons.info_outline_rounded, color: Colors.white, size: 18),
                  SizedBox(width: 8),
                  Expanded(child: Text('This is your own listing.', style: TextStyle(fontWeight: FontWeight.w600))),
                ]),
                backgroundColor: AppColors.primary,
                margin: EdgeInsets.all(14),
                duration: Duration(seconds: 2),
              ));
              return;
            }
            _requestToy(context, doc);
          },
          borderRadius: BorderRadius.circular(18),
          splashColor: color.withOpacity(0.08),
          child: Padding(
            padding: const EdgeInsets.all(16),
            child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
              Wrap(
                spacing: 6,
                runSpacing: 6,
                children: [
                ToyChip(
                  label: (data['type'] ?? 'exchange').toUpperCase(),
                  color: color.withOpacity(0.1),
                  textColor: color,
                  icon: isExchange
                      ? Icons.swap_horiz_rounded
                      : Icons.volunteer_activism_rounded,
                ),
                ToyChip(
                  label: data['condition'] ?? 'Good',
                  color: Colors.grey.shade100,
                  textColor: Colors.grey.shade600,
                ),
                if (data['category'] != null)
                  ToyChip(
                    label: data['category'],
                    color: const Color(0xFFEFF6FF),
                    textColor: const Color(0xFF2563EB),
                  ),
                if (isOwn)
                  ToyChip(
                    label: 'YOUR LISTING',
                    color: AppColors.accent.withOpacity(0.12),
                    textColor: AppColors.accent,
                    icon: Icons.star_rounded,
                  ),
                if (data['fee'] != null && (data['fee'] as num) > 0)
                  ToyChip(
                    label: 'PKR ${(data['fee'] as num).toStringAsFixed(0)}',
                    color: AppColors.warning.withOpacity(0.12),
                    textColor: AppColors.warning,
                    icon: Icons.payments_rounded,
                  ),
              ]),
              const SizedBox(height: 12),
              // Toy photo if available
              if (data['imageUrl'] != null && (data['imageUrl'] as String).isNotEmpty) ...[
                ClipRRect(
                  borderRadius: BorderRadius.circular(12),
                  child: Image.network(
                    data['imageUrl'] as String,
                    height: 130,
                    width: double.infinity,
                    fit: BoxFit.cover,
                    errorBuilder: (_, __, ___) => const SizedBox.shrink(),
                    loadingBuilder: (ctx, child, progress) => progress == null
                        ? child
                        : Container(height: 130,
                            decoration: BoxDecoration(
                              color: isDark ? AppColors.darkInputBg : const Color(0xFFF3F4F8),
                              borderRadius: BorderRadius.circular(12),
                            ),
                            child: const Center(child: CircularProgressIndicator(strokeWidth: 2, color: AppColors.accent))),
                  ),
                ),
                const SizedBox(height: 10),
              ],
              Text(
                data['name'] ?? '',
                style: TextStyle(
                    fontSize: 16,
                    fontWeight: FontWeight.w800,
                    color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
                    letterSpacing: -0.3),
              ),
              const SizedBox(height: 4),
              Text(
                data['description'] ?? '',
                style: TextStyle(fontSize: 13, color: isDark ? AppColors.darkTextSecondary : Colors.grey.shade600, height: 1.4),
                maxLines: 2,
                overflow: TextOverflow.ellipsis,
              ),
              const SizedBox(height: 12),
              Row(children: [
                GestureDetector(
                  onTap: isOwn ? null : () => navigateTo(context, PublicProfileScreen(
                    userId: data['ownerId'] ?? '',
                    userName: data['ownerName'] ?? 'User',
                  )),
                  child: UserAvatar(name: data['ownerName'] ?? '?', size: 22, gradient: AppColors.heroGradient),
                ),
                const SizedBox(width: 6),
                GestureDetector(
                  onTap: isOwn ? null : () => navigateTo(context, PublicProfileScreen(
                    userId: data['ownerId'] ?? '',
                    userName: data['ownerName'] ?? 'User',
                  )),
                  child: Flexible(
                    child: Text(
                      isOwn ? 'You' : (data['ownerName'] ?? 'unknown'),
                      style: TextStyle(
                        fontSize: 12.5,
                        color: isOwn ? AppColors.accent : AppColors.primary,
                        fontWeight: FontWeight.w700,
                        decoration: isOwn ? null : TextDecoration.underline,
                        decorationColor: AppColors.primary.withOpacity(0.4),
                      ),
                      overflow: TextOverflow.ellipsis,
                    ),
                  ),
                ),
                const SizedBox(width: 8),
                if (!isOwn) UserRatingBadge(userId: data['ownerId'] ?? ''),
              ]),
              const SizedBox(height: 8),
              Row(children: [
                if (data['ageGroup'] != null) ...[
                  Icon(Icons.child_care_rounded, size: 14, color: Colors.grey.shade400),
                  const SizedBox(width: 3),
                  Text('Age ${data['ageGroup']}',
                      style: TextStyle(fontSize: 12, color: Colors.grey.shade500)),
                ],
                const Spacer(),
                if (isOwn)
                  Container(
                    padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 7),
                    decoration: BoxDecoration(
                      color: AppColors.accent.withOpacity(0.10),
                      borderRadius: BorderRadius.circular(10),
                      border: Border.all(color: AppColors.accent.withOpacity(0.3)),
                    ),
                    child: Row(mainAxisSize: MainAxisSize.min, children: [
                      Icon(Icons.star_rounded, size: 13, color: AppColors.accent),
                      const SizedBox(width: 4),
                      Text('Yours', style: TextStyle(fontSize: 12.5, fontWeight: FontWeight.w800, color: AppColors.accent)),
                    ]),
                  )
                else
                  StreamBuilder<bool>(
                    stream: FirestoreService.streamHasRequested(doc.id),
                    builder: (context, snap) {
                      final hasRequested = snap.data ?? false;
                      return GestureDetector(
                        onTap: hasRequested ? null : () => _requestToy(context, doc),
                        child: AnimatedContainer(
                          duration: const Duration(milliseconds: 200),
                          padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
                          decoration: BoxDecoration(
                            color: hasRequested ? Colors.grey.shade300 : color,
                            borderRadius: BorderRadius.circular(10),
                            boxShadow: hasRequested ? [] : [
                              BoxShadow(
                                color: color.withOpacity(0.35),
                                blurRadius: 8,
                                offset: const Offset(0, 3),
                              )
                            ],
                          ),
                          child: Row(mainAxisSize: MainAxisSize.min, children: [
                            if (hasRequested) ...[
                              Icon(Icons.check_rounded, size: 13, color: Colors.grey.shade600),
                              const SizedBox(width: 4),
                            ],
                            Text(
                              hasRequested ? 'Requested' : 'Request',
                              style: TextStyle(
                                fontSize: 13,
                                fontWeight: FontWeight.w800,
                                color: hasRequested ? Colors.grey.shade600 : Colors.white,
                              ),
                            ),
                          ]),
                        ),
                      );
                    },
                  ),
              ]),
            ]),
          ),
        ),
      ),
    );
  }
}

// ==================== REQUESTS SCREEN ====================
class RequestsScreen extends StatefulWidget {
  const RequestsScreen({super.key});
  @override
  State<RequestsScreen> createState() => _RequestsScreenState();
}

class _RequestsScreenState extends State<RequestsScreen> {
  final Set<String> _processingIds = {};

  Future<void> _respond(QueryDocumentSnapshot doc, bool accept) async {
    if (_processingIds.contains(doc.id)) return;
    setState(() => _processingIds.add(doc.id));
    try {
      final data = doc.data() as Map<String, dynamic>;
      await FirestoreService.respondToRequest(
        requestDocId: doc.id,
        toyDocId: data['toyId'] ?? '',
        accept: accept,
      );
      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(SnackBar(
        content: Row(children: [
          Icon(accept ? Icons.check_circle_rounded : Icons.cancel_rounded,
              color: Colors.white, size: 18),
          const SizedBox(width: 8),
          Expanded(child: Text(accept ? 'Request accepted!' : 'Request declined',
              style: const TextStyle(fontWeight: FontWeight.w600))),
        ]),
        backgroundColor: accept ? AppColors.success : Colors.grey.shade600,
        margin: const EdgeInsets.all(14),
      ));
    } catch (e) {
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
          content: Text('Something went wrong. Please try again.'),
          backgroundColor: AppColors.error,
          margin: EdgeInsets.all(14),
        ));
      }
    }
    if (mounted) setState(() => _processingIds.remove(doc.id));
  }

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Scaffold(
      appBar: AppBar(title: const Text('Requests')),
      body: StreamBuilder<QuerySnapshot>(
        stream: FirestoreService.streamRequestsReceived(),
        builder: (context, snap) {
          if (snap.hasError) {
            debugPrint('Requests stream error: ${snap.error}');
            return errorState(message: 'Failed to load requests. Please try again.');
          }
          if (snap.connectionState == ConnectionState.waiting && !snap.hasData) {
            return const Center(child: CircularProgressIndicator(color: AppColors.primary));
          }
          final docs = snap.data?.docs ?? [];
          if (docs.isEmpty) {
            return emptyState(
                icon: Icons.mark_email_read_rounded,
                title: 'No pending requests',
                subtitle: 'Requests for your toys will appear here',
                color: AppColors.primary);
          }
          return Center(
            child: ConstrainedBox(
              constraints: const BoxConstraints(maxWidth: 800),
              child: ListView.separated(
                padding: const EdgeInsets.all(16),
                itemCount: docs.length,
                separatorBuilder: (_, __) => const SizedBox(height: 10),
                itemBuilder: (_, i) => _buildCard(docs[i]),
              ),
            ),
          );
        },
      ),
    );
  }

  Widget _buildCard(QueryDocumentSnapshot doc) {
    final req = doc.data() as Map<String, dynamic>;
    final requesterName = req['requesterName'] ?? 'Unknown';
    final message = (req['message'] ?? '').toString().trim();
    final ts = req['createdAt'] as Timestamp?;
    final createdAt = ts?.toDate() ?? DateTime.now();
    final isProcessing = _processingIds.contains(doc.id);

    return Container(
      decoration: BoxDecoration(
          color: Theme.of(context).cardColor,
          borderRadius: BorderRadius.circular(18),
          boxShadow: AppShadows.card(context)),
      child: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
          Row(children: [
            UserAvatar(name: requesterName, size: 46),
            const SizedBox(width: 12),
            Expanded(
              child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                Text(requesterName,
                    style: TextStyle(
                        fontWeight: FontWeight.w800,
                        fontSize: 15,
                        color: Theme.of(context).brightness == Brightness.dark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                const SizedBox(height: 3),
                Text('Wants to borrow',
                    style: TextStyle(fontSize: 12, color: Colors.grey.shade500)),
              ]),
            ),
            IconButton(
              icon: Container(
                padding: const EdgeInsets.all(6),
                decoration: BoxDecoration(
                    color: AppColors.error.withOpacity(0.08),
                    borderRadius: BorderRadius.circular(8)),
                child: Icon(Icons.person_off_rounded,
                    color: AppColors.error.withOpacity(0.7), size: 18),
              ),
              onPressed: () async {
                final requesterId = req['requesterId'] ?? '';
                if (requesterId.isEmpty) return;
                await FirestoreService.blacklistUser(requesterId, requesterName);
                if (mounted) {
                  ScaffoldMessenger.of(context).showSnackBar(SnackBar(
                    content: Text('$requesterName blocked'),
                    margin: const EdgeInsets.all(14),
                  ));
                }
              },
              tooltip: 'Block user',
              visualDensity: VisualDensity.compact,
            ),
          ]),
          const SizedBox(height: 12),
          ToyChip(
            label: 'FOR: ${req['toyName'] ?? ''}',
            color: AppColors.primary.withOpacity(0.08),
            textColor: AppColors.primary,
            icon: Icons.toys_rounded,
          ),
          if (message.isNotEmpty) ...[
            const SizedBox(height: 12),
            Container(
              padding: const EdgeInsets.all(12),
              decoration: BoxDecoration(
                color: Theme.of(context).brightness == Brightness.dark ? AppColors.darkInputBg : AppColors.surface,
                borderRadius: BorderRadius.circular(12),
                border: Border.all(color: Theme.of(context).brightness == Brightness.dark ? AppColors.darkBorder : AppColors.divider),
              ),
              child: Row(crossAxisAlignment: CrossAxisAlignment.start, children: [
                Icon(Icons.format_quote_rounded, size: 18, color: Colors.grey.shade300),
                const SizedBox(width: 6),
                Expanded(
                  child: Text(message,
                      style: TextStyle(
                          fontSize: 13, color: Colors.grey.shade700, height: 1.4)),
                ),
              ]),
            ),
          ],
          const SizedBox(height: 14),
          Text(
            'Requested ${DateFormat('MMM d, h:mm a').format(createdAt.toLocal())}',
            style: TextStyle(fontSize: 11.5, color: Colors.grey.shade400),
          ),
          const SizedBox(height: 12),
          Row(children: [
            Expanded(
              child: OutlinedButton.icon(
                onPressed: isProcessing ? null : () => _respond(doc, false),
                icon: const Icon(Icons.close_rounded, size: 16),
                label: const Text('Decline'),
                style: OutlinedButton.styleFrom(
                  foregroundColor: Colors.grey.shade600,
                  side: BorderSide(color: Colors.grey.shade300),
                  padding: const EdgeInsets.symmetric(vertical: 12),
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                ),
              ),
            ),
            const SizedBox(width: 10),
            Expanded(
              flex: 2,
              child: ElevatedButton.icon(
                onPressed: isProcessing ? null : () => _respond(doc, true),
                icon: isProcessing
                    ? const SizedBox(
                  width: 16,
                  height: 16,
                  child: CircularProgressIndicator(strokeWidth: 2, color: Colors.white),
                )
                    : const Icon(Icons.check_rounded, size: 18),
                label: const Text('Accept', style: TextStyle(fontWeight: FontWeight.w800)),
                style: ElevatedButton.styleFrom(
                  backgroundColor: AppColors.success,
                  foregroundColor: Colors.white,
                  padding: const EdgeInsets.symmetric(vertical: 12),
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                  elevation: 0,
                ),
              ),
            ),
          ]),
        ]),
      ),
    );
  }
}

// ==================== CHAT SCREEN ====================
class ChatScreen extends StatefulWidget {
  final String exchangeId;
  final String otherPartyName;
  final String receiverId;
  const ChatScreen({super.key, required this.exchangeId, required this.otherPartyName, this.receiverId = ''});

  @override
  State<ChatScreen> createState() => _ChatScreenState();
}

class _ChatScreenState extends State<ChatScreen> {
  final _messageCtrl = TextEditingController();
  final _scrollCtrl = ScrollController();
  bool _isSending = false;

  @override
  void initState() {
    super.initState();
    // Mark all unread messages as read when chat is opened
    FirestoreService.markMessagesRead(widget.exchangeId);
  }

  @override
  void dispose() {
    _messageCtrl.dispose();
    _scrollCtrl.dispose();
    super.dispose();
  }

  Future<void> _sendMessage() async {
    final text = _messageCtrl.text.trim();
    if (text.isEmpty) {
      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
        content: Text('Cannot send an empty message.'),
        backgroundColor: AppColors.warning,
        margin: EdgeInsets.all(14),
        duration: Duration(seconds: 2),
      ));
      return;
    }
    final currentUser = _auth.currentUser;
    if (currentUser == null) {
      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
        content: Text('You must be logged in to send messages.'),
        backgroundColor: AppColors.error,
        margin: EdgeInsets.all(14),
      ));
      return;
    }
    setState(() => _isSending = true);
    try {
      await FirestoreService.sendMessage(
        exchangeId: widget.exchangeId,
        text: text,
        receiverId: widget.receiverId,
        receiverName: widget.otherPartyName,
      );
      _messageCtrl.clear();
      _scrollToBottom();
    } catch (e) {
      debugPrint('Send message error: $e');
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(SnackBar(
          content: const Row(children: [
            Icon(Icons.error_outline, color: Colors.white, size: 18),
            SizedBox(width: 8),
            Expanded(child: Text('Failed to send message. Please try again.', style: TextStyle(fontWeight: FontWeight.w600))),
          ]),
          backgroundColor: AppColors.error,
          margin: const EdgeInsets.all(14),
        ));
      }
    }
    if (mounted) setState(() => _isSending = false);
  }

  void _scrollToBottom() {
    Future.delayed(const Duration(milliseconds: 100), () {
      if (_scrollCtrl.hasClients) {
        _scrollCtrl.animateTo(
          _scrollCtrl.position.maxScrollExtent,
          duration: const Duration(milliseconds: 300),
          curve: Curves.easeOut,
        );
      }
    });
  }

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final currentUser = _auth.currentUser;
    if (currentUser == null) {
      return Scaffold(
        appBar: AppBar(title: const Text('Chat')),
        body: Center(
          child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [
            const Icon(Icons.lock_outline_rounded, size: 48, color: AppColors.textSecondary),
            const SizedBox(height: 16),
            const Text('Please log in to access chat.',
                style: TextStyle(fontSize: 15, color: AppColors.textSecondary)),
          ]),
        ),
      );
    }
    final currentUid = currentUser.uid;
    return Scaffold(
      appBar: AppBar(
        title: Row(
          mainAxisSize: MainAxisSize.min,
          children: [
            UserAvatar(name: widget.otherPartyName, size: 32),
            const SizedBox(width: 10),
            Text(widget.otherPartyName),
          ],
        ),
      ),
      body: Column(
        children: [
          Expanded(
            child: StreamBuilder<QuerySnapshot>(
              stream: FirestoreService.streamMessages(widget.exchangeId),
              builder: (context, snap) {
                if (snap.hasError) {
                  return errorState(message: 'Failed to load messages.');
                }
                if (snap.connectionState == ConnectionState.waiting && !snap.hasData) {
                  return const Center(child: CircularProgressIndicator(color: AppColors.primary));
                }
                final docs = snap.data?.docs ?? [];
                if (docs.isEmpty) {
                  return emptyState(
                    icon: Icons.chat_bubble_outline_rounded,
                    title: 'No messages yet',
                    subtitle: 'Start coordinating pickup/dropoff with ${widget.otherPartyName}',
                    color: AppColors.primary,
                  );
                }

                WidgetsBinding.instance.addPostFrameCallback((_) => _scrollToBottom());

                final reversedDocs = docs.reversed.toList();

                return ListView.builder(
                  controller: _scrollCtrl,
                  reverse: true,
                  padding: const EdgeInsets.all(16),
                  itemCount: reversedDocs.length,
                  itemBuilder: (_, i) {
                    final msg = reversedDocs[i].data() as Map<String, dynamic>;
                    final isMe = msg['senderId'] == currentUid;
                    final ts = msg['createdAt'] as Timestamp?;
                    final time = ts != null
                        ? DateFormat('h:mm a').format(ts.toDate())
                        : 'Sending...';

                    return Align(
                      alignment: isMe ? Alignment.centerRight : Alignment.centerLeft,
                      child: Container(
                        margin: const EdgeInsets.only(bottom: 8),
                        constraints: BoxConstraints(
                          maxWidth: MediaQuery.of(context).size.width * 0.75,
                        ),
                        padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 10),
                        decoration: BoxDecoration(
                          color: isMe
                              ? AppColors.primary
                              : (isDark ? AppColors.darkInputBg : Colors.grey.shade100),
                          borderRadius: BorderRadius.circular(16).copyWith(
                            bottomRight: isMe ? const Radius.circular(4) : null,
                            bottomLeft: !isMe ? const Radius.circular(4) : null,
                          ),
                        ),
                        child: Column(
                          crossAxisAlignment: CrossAxisAlignment.end,
                          mainAxisSize: MainAxisSize.min,
                          children: [
                            Text(
                              msg['text'] ?? '',
                              style: TextStyle(
                                fontSize: 14,
                                color: isMe ? Colors.white : (isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
                                height: 1.4,
                              ),
                            ),
                            const SizedBox(height: 4),
                            Text(
                              time,
                              style: TextStyle(
                                fontSize: 10,
                                color: isMe ? Colors.white.withOpacity(0.7) : Colors.grey.shade500,
                              ),
                            ),
                          ],
                        ),
                      ),
                    );
                  },
                );
              },
            ),
          ),
          Container(
            padding: const EdgeInsets.fromLTRB(16, 8, 16, 16),
            decoration: BoxDecoration(
              color: Theme.of(context).cardColor,
              border: Border(
                top: BorderSide(color: isDark ? AppColors.darkBorder : AppColors.divider),
              ),
            ),
            child: SafeArea(
              child: Row(
                children: [
                  Expanded(
                    child: TextField(
                      controller: _messageCtrl,
                      style: TextStyle(fontSize: 14, color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
                      maxLines: null,
                      textCapitalization: TextCapitalization.sentences,
                      decoration: InputDecoration(
                        hintText: 'Type a message...',
                        hintStyle: TextStyle(color: isDark ? AppColors.darkTextSecondary : Colors.grey.shade400),
                        filled: true,
                        fillColor: isDark ? AppColors.darkInputBg : Colors.grey.shade100,
                        border: OutlineInputBorder(
                          borderRadius: BorderRadius.circular(24),
                          borderSide: BorderSide.none,
                        ),
                        contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
                      ),
                      onSubmitted: (_) => _sendMessage(),
                    ),
                  ),
                  const SizedBox(width: 8),
                  GestureDetector(
                    onTap: _isSending ? null : _sendMessage,
                    child: Container(
                      width: 44,
                      height: 44,
                      decoration: BoxDecoration(
                        color: AppColors.primary,
                        shape: BoxShape.circle,
                        boxShadow: [
                          BoxShadow(
                            color: AppColors.primary.withOpacity(0.3),
                            blurRadius: 8,
                            offset: const Offset(0, 2),
                          ),
                        ],
                      ),
                      child: _isSending
                          ? const SizedBox(
                        width: 20,
                        height: 20,
                        child: CircularProgressIndicator(
                          strokeWidth: 2,
                          color: Colors.white,
                        ),
                      )
                          : const Icon(Icons.send_rounded, color: Colors.white, size: 20),
                    ),
                  ),
                ],
              ),
            ),
          ),
        ],
      ),
    );
  }
}

// ==================== MY EXCHANGES SCREEN ====================
class MyExchangesScreen extends StatefulWidget {
  const MyExchangesScreen({super.key});
  @override
  State<MyExchangesScreen> createState() => _MyExchangesScreenState();
}

class _MyExchangesScreenState extends State<MyExchangesScreen> {
  final Set<String> _loadingIds = {};

  void _setLoading(String id, bool val) {
    if (mounted) setState(() => val ? _loadingIds.add(id) : _loadingIds.remove(id));
  }

  Future<void> _returnToy(BuildContext ctx, QueryDocumentSnapshot doc) async {
    final data = doc.data() as Map<String, dynamic>;
    final toyName = data['toyName'] ?? 'this toy';
    final ownerName = data['ownerName'] ?? 'the owner';

    final confirm = await showDialog<bool>(
      context: ctx,
      builder: (dCtx) => AlertDialog(
        insetPadding: const EdgeInsets.symmetric(horizontal: 20, vertical: 24),
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
        titlePadding: const EdgeInsets.fromLTRB(20, 20, 20, 0),
        contentPadding: const EdgeInsets.fromLTRB(20, 14, 20, 0),
        actionsPadding: const EdgeInsets.fromLTRB(16, 8, 16, 16),
        title: Row(children: [
          Container(
            width: 36, height: 36,
            decoration: BoxDecoration(color: AppColors.coral.withOpacity(0.1), borderRadius: BorderRadius.circular(10)),
            child: const Icon(Icons.assignment_return_rounded, color: AppColors.coral, size: 20),
          ),
          const SizedBox(width: 12),
          const Flexible(child: Text('Return Toy?', style: TextStyle(fontWeight: FontWeight.w800, fontSize: 17))),
        ]),
        content: Column(mainAxisSize: MainAxisSize.min, crossAxisAlignment: CrossAxisAlignment.start, children: [
          Text('You are returning "$toyName" to $ownerName.', style: const TextStyle(fontSize: 13.5, height: 1.5)),
          const SizedBox(height: 10),
          Container(
            padding: const EdgeInsets.all(11),
            decoration: BoxDecoration(color: AppColors.primary.withOpacity(0.06), borderRadius: BorderRadius.circular(12)),
            child: const Row(crossAxisAlignment: CrossAxisAlignment.start, children: [
              Icon(Icons.info_outline_rounded, size: 15, color: AppColors.primary),
              SizedBox(width: 8),
              Expanded(child: Text('The owner will receive a notification and must confirm receipt to complete the exchange.', style: TextStyle(fontSize: 12, color: AppColors.primary, height: 1.4))),
            ]),
          ),
        ]),
        actions: [
          Row(children: [
            Expanded(child: TextButton(onPressed: () => Navigator.pop(dCtx, false), child: const Text('Cancel', style: TextStyle(color: AppColors.textSecondary)))),
            const SizedBox(width: 8),
            Expanded(
              flex: 2,
              child: ElevatedButton.icon(
                onPressed: () => Navigator.pop(dCtx, true),
                icon: const Icon(Icons.assignment_return_rounded, size: 15),
                label: const Text('Yes, Return It', style: TextStyle(fontWeight: FontWeight.w700, fontSize: 13)),
                style: ElevatedButton.styleFrom(backgroundColor: AppColors.coral, foregroundColor: Colors.white, elevation: 0, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
              ),
            ),
          ]),
        ],
      ),
    );
    if (confirm != true) return;
    _setLoading(doc.id, true);
    try {
      await FirestoreService.returnToy(
        exchangeDocId: doc.id,
        toyDocId: data['toyId'] ?? '',
        ownerUid: data['ownerId'] ?? '',
        toyName: toyName,
      );
      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(SnackBar(
        content: const Row(children: [
          Icon(Icons.hourglass_top_rounded, color: Colors.white, size: 18),
          SizedBox(width: 8),
          Expanded(child: Text('Return initiated! Waiting for owner to confirm receipt.', style: TextStyle(fontWeight: FontWeight.w600))),
        ]),
        backgroundColor: AppColors.primary,
        margin: const EdgeInsets.all(14),
        duration: const Duration(seconds: 4),
      ));
    } catch (e) {
      debugPrint('returnToy error: $e');
      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(SnackBar(
        content: Row(children: [
          const Icon(Icons.error_outline, color: Colors.white, size: 18),
          const SizedBox(width: 8),
          Expanded(child: Text('Failed: ${e.toString().replaceAll('Exception: ', '')}', style: const TextStyle(fontWeight: FontWeight.w600))),
        ]),
        backgroundColor: AppColors.error,
        margin: const EdgeInsets.all(14),
      ));
    } finally {
      _setLoading(doc.id, false);
    }
  }

  Future<void> _confirmReceipt(BuildContext ctx, QueryDocumentSnapshot doc) async {
    final data = doc.data() as Map<String, dynamic>;
    final toyName = data['toyName'] ?? 'this toy';
    final holderName = data['holderName'] ?? 'the borrower';

    final confirm = await showDialog<bool>(
      context: ctx,
      builder: (dCtx) => AlertDialog(
        insetPadding: const EdgeInsets.symmetric(horizontal: 20, vertical: 24),
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
        titlePadding: const EdgeInsets.fromLTRB(20, 20, 20, 0),
        contentPadding: const EdgeInsets.fromLTRB(20, 14, 20, 0),
        actionsPadding: const EdgeInsets.fromLTRB(16, 8, 16, 16),
        title: Row(children: [
          Container(
            width: 36, height: 36,
            decoration: BoxDecoration(color: AppColors.success.withOpacity(0.1), borderRadius: BorderRadius.circular(10)),
            child: const Icon(Icons.check_circle_rounded, color: AppColors.success, size: 20),
          ),
          const SizedBox(width: 12),
          const Flexible(child: Text('Confirm Receipt?', style: TextStyle(fontWeight: FontWeight.w800, fontSize: 17))),
        ]),
        content: Column(mainAxisSize: MainAxisSize.min, crossAxisAlignment: CrossAxisAlignment.start, children: [
          Text('Confirm you have physically received "$toyName" back from $holderName?', style: const TextStyle(fontSize: 13.5, height: 1.5)),
          const SizedBox(height: 10),
          Container(
            padding: const EdgeInsets.all(11),
            decoration: BoxDecoration(color: AppColors.success.withOpacity(0.06), borderRadius: BorderRadius.circular(12)),
            child: const Row(crossAxisAlignment: CrossAxisAlignment.start, children: [
              Icon(Icons.info_outline_rounded, size: 15, color: AppColors.success),
              SizedBox(width: 8),
              Expanded(child: Text('This will complete the exchange and make the toy available again.', style: TextStyle(fontSize: 12, color: AppColors.success, height: 1.4))),
            ]),
          ),
        ]),
        actions: [
          Row(children: [
            Expanded(child: TextButton(onPressed: () => Navigator.pop(dCtx, false), child: const Text('Not Yet', style: TextStyle(color: AppColors.textSecondary)))),
            const SizedBox(width: 8),
            Expanded(
              flex: 2,
              child: ElevatedButton.icon(
                onPressed: () => Navigator.pop(dCtx, true),
                icon: const Icon(Icons.check_rounded, size: 15),
                label: const Text('Yes, I Have It', style: TextStyle(fontWeight: FontWeight.w700, fontSize: 13)),
                style: ElevatedButton.styleFrom(backgroundColor: AppColors.success, foregroundColor: Colors.white, elevation: 0, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
              ),
            ),
          ]),
        ],
      ),
    );
    if (confirm != true) return;
    _setLoading(doc.id, true);
    try {
      await FirestoreService.confirmToyReceipt(
        exchangeDocId: doc.id,
        toyDocId: data['toyId'] ?? '',
        holderUid: data['holderId'] ?? '',
        toyName: toyName,
      );
      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
        content: Row(children: [
          Icon(Icons.check_circle_rounded, color: Colors.white, size: 18),
          SizedBox(width: 8),
          Text('Receipt confirmed! Exchange complete. 🎉', style: TextStyle(fontWeight: FontWeight.w600)),
        ]),
        backgroundColor: AppColors.success,
        margin: EdgeInsets.all(14),
        duration: Duration(seconds: 3),
      ));
      if (mounted) {
        _showFeedbackSheet(context, data['holderId'] ?? '', holderName, toyName, doc.id);
      }
    } catch (e) {
      debugPrint('confirmReceipt error: $e');
      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(SnackBar(
        content: Row(children: [
          const Icon(Icons.error_outline, color: Colors.white, size: 18),
          const SizedBox(width: 8),
          Expanded(child: Text('Failed: ${e.toString().replaceAll('Exception: ', '')}', style: const TextStyle(fontWeight: FontWeight.w600))),
        ]),
        backgroundColor: AppColors.error,
        margin: const EdgeInsets.all(14),
      ));
    } finally {
      _setLoading(doc.id, false);
    }
  }

  Future<void> _reportDispute(BuildContext ctx, QueryDocumentSnapshot doc) async {
    final data = doc.data() as Map<String, dynamic>;
    final currentUid = _auth.currentUser?.uid ?? '';
    final isOwner = data['ownerId'] == currentUid;
    final otherPartyId = isOwner ? (data['holderId'] ?? '') : (data['ownerId'] ?? '');
    final otherPartyName = isOwner ? (data['holderName'] ?? 'Unknown') : (data['ownerName'] ?? 'Unknown');
    final toyName = data['toyName'] ?? 'this toy';

    final reasonCtrl = TextEditingController();
    String? validationError;

    final confirm = await showDialog<bool>(
      context: ctx,
      builder: (dCtx) => StatefulBuilder(
        builder: (sCtx, setSState) => AlertDialog(
          insetPadding: const EdgeInsets.symmetric(horizontal: 20, vertical: 24),
          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
          titlePadding: const EdgeInsets.fromLTRB(20, 20, 20, 0),
          contentPadding: const EdgeInsets.fromLTRB(20, 14, 20, 0),
          actionsPadding: const EdgeInsets.fromLTRB(16, 8, 16, 16),
          title: Row(children: [
            Container(
              width: 36, height: 36,
              decoration: BoxDecoration(color: AppColors.error.withOpacity(0.1), borderRadius: BorderRadius.circular(10)),
              child: const Icon(Icons.warning_amber_rounded, color: AppColors.error, size: 20),
            ),
            const SizedBox(width: 12),
            const Flexible(child: Text('Report Issue', style: TextStyle(fontWeight: FontWeight.w800, fontSize: 17))),
          ]),
          content: SingleChildScrollView(
            child: Column(mainAxisSize: MainAxisSize.min, crossAxisAlignment: CrossAxisAlignment.start, children: [
              RichText(
                text: TextSpan(
                  style: const TextStyle(fontSize: 13, color: AppColors.textSecondary, height: 1.5),
                  children: [
                    const TextSpan(text: 'Reporting issue with '),
                    TextSpan(text: '"$toyName"', style: const TextStyle(fontWeight: FontWeight.w700, color: AppColors.textPrimary)),
                    const TextSpan(text: ' involving '),
                    TextSpan(text: otherPartyName, style: const TextStyle(fontWeight: FontWeight.w700, color: AppColors.error)),
                    const TextSpan(text: '.'),
                  ],
                ),
              ),
              const SizedBox(height: 12),
              TextField(
                controller: reasonCtrl,
                maxLines: 3,
                onChanged: (_) => setSState(() => validationError = null),
                decoration: InputDecoration(
                  hintText: 'Describe the issue in detail...',
                  errorText: validationError,
                  border: OutlineInputBorder(borderRadius: BorderRadius.circular(12)),
                  filled: true,
                  fillColor: Theme.of(sCtx).brightness == Brightness.dark
                      ? AppColors.darkInputBg
                      : const Color(0xFFF8F8FC),
                  contentPadding: const EdgeInsets.all(12),
                ),
              ),
              const SizedBox(height: 10),
              Container(
                padding: const EdgeInsets.all(10),
                decoration: BoxDecoration(color: AppColors.error.withOpacity(0.05), borderRadius: BorderRadius.circular(10)),
                child: const Row(crossAxisAlignment: CrossAxisAlignment.start, children: [
                  Icon(Icons.info_outline_rounded, size: 13, color: AppColors.error),
                  SizedBox(width: 6),
                  Expanded(child: Text('The exchange will be marked as disputed until resolved.', style: TextStyle(fontSize: 11.5, color: AppColors.error, height: 1.4))),
                ]),
              ),
            ]),
          ),
          actions: [
            Row(children: [
              Expanded(child: TextButton(onPressed: () => Navigator.pop(dCtx, false), child: const Text('Cancel', style: TextStyle(color: AppColors.textSecondary)))),
              const SizedBox(width: 8),
              Expanded(
                flex: 2,
                child: ElevatedButton.icon(
                  onPressed: () {
                    if (reasonCtrl.text.trim().isEmpty) {
                      setSState(() => validationError = 'Please describe the issue');
                      return;
                    }
                    Navigator.pop(dCtx, true);
                  },
                  icon: const Icon(Icons.flag_rounded, size: 15),
                  label: const Text('Submit Report', style: TextStyle(fontWeight: FontWeight.w700, fontSize: 13)),
                  style: ElevatedButton.styleFrom(backgroundColor: AppColors.error, foregroundColor: Colors.white, elevation: 0, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
                ),
              ),
            ]),
          ],
        ),
      ),
    );
    if (confirm != true) return;
    _setLoading(doc.id, true);
    try {
      await FirestoreService.reportDispute(
        exchangeDocId: doc.id,
        toyDocId: data['toyId'] ?? '',
        otherPartyId: otherPartyId,
        otherPartyName: otherPartyName,
        reason: reasonCtrl.text.trim(),
      );
      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
        content: Row(children: [
          Icon(Icons.warning_amber_rounded, color: Colors.white, size: 18),
          SizedBox(width: 8),
          Expanded(child: Text('Issue reported. Exchange marked as disputed.', style: TextStyle(fontWeight: FontWeight.w600))),
        ]),
        backgroundColor: AppColors.error,
        margin: EdgeInsets.all(14),
        duration: Duration(seconds: 4),
      ));
    } catch (e) {
      debugPrint('reportDispute error: $e');
      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(SnackBar(
        content: Row(children: [
          const Icon(Icons.error_outline, color: Colors.white, size: 18),
          const SizedBox(width: 8),
          Expanded(child: Text('Failed: ${e.toString().replaceAll('Exception: ', '')}', style: const TextStyle(fontWeight: FontWeight.w600))),
        ]),
        backgroundColor: AppColors.error,
        margin: const EdgeInsets.all(14),
      ));
    } finally {
      _setLoading(doc.id, false);
    }
  }

  void _showFeedbackSheet(
      BuildContext ctx, String targetUserId, String targetName, String toyName, String exchangeId) {
    if (!ctx.mounted) return;
    final commentCtrl = TextEditingController();
    showModalBottomSheet(
      context: ctx,
      isScrollControlled: true,
      builder: (sheetCtx) => SafeArea(
        child: Padding(
          padding: EdgeInsets.only(bottom: MediaQuery.of(sheetCtx).viewInsets.bottom),
          child: SingleChildScrollView(
            child: Padding(
              padding: const EdgeInsets.fromLTRB(24, 20, 24, 28),
              child: Column(mainAxisSize: MainAxisSize.min, children: [
                Center(
                  child: Container(
                    width: 40,
                    height: 4,
                    decoration: BoxDecoration(
                        color: Colors.grey.shade200, borderRadius: BorderRadius.circular(2)),
                  ),
                ),
                const SizedBox(height: 20),
                const Icon(Icons.rate_review_rounded, size: 36, color: AppColors.primary),
                const SizedBox(height: 12),
                const Text('Leave Feedback',
                    style: TextStyle(
                        fontSize: 18, fontWeight: FontWeight.w900, color: AppColors.textPrimary)),
                const SizedBox(height: 6),
                Text('How was your experience with $targetName for "$toyName"?',
                    style: const TextStyle(color: AppColors.textSecondary, fontSize: 13.5),
                    textAlign: TextAlign.center),
                const SizedBox(height: 18),
                TextField(
                  controller: commentCtrl,
                  style: const TextStyle(fontSize: 14),
                  maxLines: 3,
                  decoration: InputDecoration(
                    hintText: 'Share your experience...',
                    border: OutlineInputBorder(borderRadius: BorderRadius.circular(14)),
                    filled: true,
                    fillColor: Theme.of(sheetCtx).brightness == Brightness.dark
                        ? AppColors.darkInputBg
                        : const Color(0xFFF8F8FC),
                  ),
                ),
                const SizedBox(height: 18),
                Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [
                  Row(children: [
                    Expanded(
                      child: OutlinedButton.icon(
                        onPressed: () async {
                          try {
                            await FirestoreService.giveFeedback(
                              targetUserId: targetUserId,
                              isPositive: false,
                              comment: commentCtrl.text,
                              exchangeId: exchangeId,
                              toyName: toyName,
                              rating: 2,
                            );
                          } catch (e) {
                            debugPrint('Feedback error: $e');
                          }
                          if (!sheetCtx.mounted) return;
                          Navigator.pop(sheetCtx);
                          if (ctx.mounted) {
                            ScaffoldMessenger.of(ctx).showSnackBar(const SnackBar(
                              content: Text('Feedback submitted'),
                              margin: EdgeInsets.all(14),
                            ));
                          }
                        },
                        icon: const Icon(Icons.thumb_down_rounded, size: 16),
                        label: const Text('Not Great'),
                        style: OutlinedButton.styleFrom(
                          foregroundColor: AppColors.error,
                          side: BorderSide(color: AppColors.error.withOpacity(0.4)),
                          padding: const EdgeInsets.symmetric(vertical: 13),
                          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                        ),
                      ),
                    ),
                    const SizedBox(width: 10),
                    Expanded(
                      child: ElevatedButton.icon(
                        onPressed: () async {
                          try {
                            await FirestoreService.giveFeedback(
                              targetUserId: targetUserId,
                              isPositive: true,
                              comment: commentCtrl.text,
                              exchangeId: exchangeId,
                              toyName: toyName,
                              rating: 5,
                            );
                          } catch (e) {
                            debugPrint('Feedback error: $e');
                          }
                          if (!sheetCtx.mounted) return;
                          Navigator.pop(sheetCtx);
                          if (ctx.mounted) {
                            ScaffoldMessenger.of(ctx).showSnackBar(const SnackBar(
                              content: Row(children: [
                                Icon(Icons.check_circle_rounded, color: Colors.white, size: 18),
                                SizedBox(width: 8),
                                Text('Thanks for the feedback!',
                                    style: TextStyle(fontWeight: FontWeight.w600)),
                              ]),
                              backgroundColor: AppColors.success,
                              margin: EdgeInsets.all(14),
                            ));
                          }
                        },
                        icon: const Icon(Icons.thumb_up_rounded, size: 16),
                        label: const Text('Great!'),
                        style: ElevatedButton.styleFrom(
                          backgroundColor: AppColors.success,
                          foregroundColor: Colors.white,
                          elevation: 0,
                          padding: const EdgeInsets.symmetric(vertical: 13),
                          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                        ),
                      ),
                    ),
                  ]),
                  const SizedBox(height: 10),
                  TextButton(
                    onPressed: () => Navigator.pop(sheetCtx),
                    child: const Text('Skip for now',
                        style: TextStyle(color: AppColors.textSecondary)),
                  ),
                ]),
              ]),
            ),
          ),
        ),
      ),
    );
  }

  void _openChat(BuildContext ctx, String exchangeDocId, Map<String, dynamic> data) {
    final currentUid = _auth.currentUser?.uid ?? '';
    if (currentUid.isEmpty) {
      ScaffoldMessenger.of(ctx).showSnackBar(const SnackBar(
        content: Text('Please log in to open chat.'),
        backgroundColor: AppColors.error,
        margin: EdgeInsets.all(14),
      ));
      return;
    }
    final isOwner = data['ownerId'] == currentUid;
    final otherPartyName = isOwner ? (data['holderName'] ?? 'Borrower') : (data['ownerName'] ?? 'Owner');
    final otherPartyId = isOwner ? (data['holderId'] ?? '') : (data['ownerId'] ?? '');
    navigateTo(ctx, ChatScreen(
      exchangeId: exchangeDocId,
      otherPartyName: otherPartyName,
      receiverId: otherPartyId,
    ));
  }

  Widget _buildCard(BuildContext ctx, QueryDocumentSnapshot doc, String currentUid) {
    final e = doc.data() as Map<String, dynamic>;
    final bool isOwner = e['ownerId'] == currentUid;
    final bool isHolder = e['holderId'] == currentUid;
    final bool isActive = e['status'] == 'active';
    final bool isPendingReceipt = e['status'] == 'pending_receipt';
    final bool isDisputed = e['status'] == 'disputed';
    final bool isCompleted = e['status'] == 'completed';
    final bool isDonation = e['type'] == 'donation';
    final Color typeColor = isDonation ? AppColors.success : AppColors.primary;
    final receiveDate =
    e['receiveDate'] != null ? DateTime.tryParse(e['receiveDate']) : null;
    final returnDate =
    e['returnDate'] != null ? DateTime.tryParse(e['returnDate']) : null;

    String statusLabel;
    Color statusColor;
    Color statusTextColor;
    if (isActive) {
      statusLabel = isDonation ? 'DONATED' : 'IN-USE';
      statusColor = AppColors.warning.withOpacity(0.12);
      statusTextColor = AppColors.coral;
    } else if (isPendingReceipt) {
      statusLabel = 'PENDING RECEIPT';
      statusColor = AppColors.primary.withOpacity(0.10);
      statusTextColor = AppColors.primary;
    } else if (isDisputed) {
      statusLabel = 'DISPUTED';
      statusColor = AppColors.error.withOpacity(0.1);
      statusTextColor = AppColors.error;
    } else {
      statusLabel = isDonation ? 'GIFTED ✓' : 'RETURNED ✓';
      statusColor = AppColors.success.withOpacity(0.1);
      statusTextColor = AppColors.success;
    }

    return Container(
      decoration: BoxDecoration(
          color: Theme.of(ctx).cardColor, borderRadius: BorderRadius.circular(18), boxShadow: AppShadows.card(ctx)),
      child: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
          // ── Chips row — use Wrap so they never overflow ──
          Wrap(
            spacing: 6,
            runSpacing: 6,
            children: [
              ToyChip(
                label: isDonation ? 'DONATION' : 'EXCHANGE',
                color: typeColor.withOpacity(0.1),
                textColor: typeColor,
                icon: isDonation ? Icons.volunteer_activism_rounded : Icons.swap_horiz_rounded,
              ),
              ToyChip(
                label: statusLabel,
                color: statusColor,
                textColor: statusTextColor,
              ),
              ToyChip(
                label: isOwner ? 'LENT' : 'BORROWED',
                color: Colors.grey.shade100,
                textColor: Colors.grey.shade600,
              ),
            ],
          ),
          const SizedBox(height: 12),
          Text(e['toyName'] ?? '',
              style: TextStyle(
                  fontSize: 16,
                  fontWeight: FontWeight.w800,
                  color: Theme.of(ctx).brightness == Brightness.dark ? AppColors.darkTextPrimary : AppColors.textPrimary,
                  letterSpacing: -0.3)),
          const SizedBox(height: 8),
          Row(children: [
            UserAvatar(
              name: isOwner ? (e['holderName'] ?? '?') : (e['ownerName'] ?? '?'),
              size: 28,
              gradient: AppColors.heroGradient,
            ),
            const SizedBox(width: 8),
            Expanded(
              child: Text(
                isOwner
                    ? 'Borrowed by ${e['holderName'] ?? 'unknown'}'
                    : 'Owned by ${e['ownerName'] ?? 'unknown'}',
                style: TextStyle(
                    fontSize: 13, color: Colors.grey.shade600, fontWeight: FontWeight.w600),
              ),
            ),
          ]),
          if (!isDonation && receiveDate != null && returnDate != null) ...[
            const SizedBox(height: 12),
            const Divider(height: 1),
            const SizedBox(height: 10),
            Row(children: [
              Expanded(
                child: Row(children: [
                  Icon(Icons.calendar_month_rounded, size: 13, color: Colors.grey.shade500),
                  const SizedBox(width: 3),
                  Flexible(child: Text(
                    returnDate.difference(receiveDate).inHours < 48
                        ? DateFormat('MMM d, h:mm a').format(receiveDate)
                        : DateFormat('MMM d').format(receiveDate),
                    overflow: TextOverflow.ellipsis,
                    style: TextStyle(fontSize: 12, color: Colors.grey.shade700, fontWeight: FontWeight.w600))),
                  Padding(
                    padding: const EdgeInsets.symmetric(horizontal: 5),
                    child: Icon(Icons.arrow_forward_rounded, size: 12, color: Colors.grey.shade400),
                  ),
                  Icon(Icons.event_available_rounded, size: 13, color: Colors.grey.shade500),
                  const SizedBox(width: 3),
                  Flexible(child: Text(
                    returnDate.difference(receiveDate).inHours < 48
                        ? DateFormat('MMM d, h:mm a').format(returnDate)
                        : DateFormat('MMM d, yyyy').format(returnDate),
                    overflow: TextOverflow.ellipsis,
                    style: TextStyle(fontSize: 12, color: Colors.grey.shade700, fontWeight: FontWeight.w600))),
                ]),
              ),
              if (isActive || isPendingReceipt || isDisputed) ...[
                const SizedBox(width: 6),
                Builder(builder: (context) {
                  final diff = returnDate.difference(DateTime.now());
                  final isOverdue = diff.isNegative;
                  String label;
                  if (isOverdue) {
                    label = diff.inMinutes.abs() < 60
                        ? '${diff.inMinutes.abs()}m overdue'
                        : diff.inHours.abs() < 24
                        ? '${diff.inHours.abs()}h overdue'
                        : '${diff.inDays.abs()}d overdue';
                  } else {
                    label = diff.inMinutes < 60
                        ? '${diff.inMinutes}m left'
                        : diff.inHours < 24
                        ? '${diff.inHours}h left'
                        : '${diff.inDays}d left';
                  }
                  return Container(
                    padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
                    decoration: BoxDecoration(
                      color: isOverdue ? AppColors.error.withOpacity(0.1) : AppColors.accent.withOpacity(0.1),
                      borderRadius: BorderRadius.circular(20),
                    ),
                    child: Text(label, style: TextStyle(
                      fontSize: 11, fontWeight: FontWeight.w700,
                      color: isOverdue ? AppColors.error : AppColors.accent,
                    )),
                  );
                }),
              ],
            ]),
          ],
          if (!isCompleted) ...[
            const SizedBox(height: 10),
            StreamBuilder<int>(
              stream: FirestoreService.streamUnreadMessageCount(doc.id),
              builder: (context, unreadSnap) {
                final unreadCount = unreadSnap.data ?? 0;
                return GestureDetector(
                  onTap: () => _openChat(ctx, doc.id, e),
                  child: Container(
                    padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
                    decoration: BoxDecoration(
                      color: AppColors.primary.withOpacity(0.06),
                      borderRadius: BorderRadius.circular(10),
                      border: Border.all(color: AppColors.primary.withOpacity(0.2)),
                    ),
                    child: Row(children: [
                      Stack(clipBehavior: Clip.none, children: [
                        const Icon(Icons.chat_bubble_rounded, size: 16, color: AppColors.primary),
                        if (unreadCount > 0)
                          Positioned(
                            right: -4,
                            top: -4,
                            child: Container(
                              width: 10,
                              height: 10,
                              decoration: BoxDecoration(
                                color: AppColors.error,
                                shape: BoxShape.circle,
                                border: Border.all(
                                  color: Theme.of(ctx).cardColor,
                                  width: 1.5,
                                ),
                              ),
                            ),
                          ),
                      ]),
                      const SizedBox(width: 8),
                      Flexible(child: Text(
                        unreadCount > 0 ? 'Open Chat ($unreadCount new)' : 'Open Chat',
                        overflow: TextOverflow.ellipsis,
                        style: TextStyle(
                          fontSize: 12.5,
                          color: AppColors.primary,
                          fontWeight: unreadCount > 0 ? FontWeight.w800 : FontWeight.w600,
                        ),
                      )),
                      const Spacer(),
                      const Icon(Icons.chevron_right_rounded, size: 16, color: AppColors.primary),
                    ]),
                  ),
                );
              },
            ),
            const SizedBox(height: 8),
            // QR Receipt button
            GestureDetector(
              onTap: () => navigateTo(ctx, ToyQRReceiptScreen(
                exchangeData: e,
                exchangeId: doc.id,
              )),
              child: Container(
                width: double.infinity,
                padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
                decoration: BoxDecoration(
                  color: AppColors.accent.withOpacity(0.07),
                  borderRadius: BorderRadius.circular(10),
                  border: Border.all(color: AppColors.accent.withOpacity(0.2)),
                ),
                child: const Row(mainAxisAlignment: MainAxisAlignment.center, children: [
                  Icon(Icons.qr_code_rounded, size: 16, color: AppColors.accent),
                  SizedBox(width: 8),
                  Text('View QR Receipt', style: TextStyle(fontSize: 12.5, color: AppColors.accent, fontWeight: FontWeight.w700)),
                ]),
              ),
            ),
          ],
          if (isActive && isHolder && !isDonation) ...[
            const SizedBox(height: 14),
            SizedBox(
              width: double.infinity,
              child: ElevatedButton.icon(
                onPressed: _loadingIds.contains(doc.id) ? null : () => _returnToy(ctx, doc),
                icon: _loadingIds.contains(doc.id)
                    ? const SizedBox(width: 16, height: 16, child: CircularProgressIndicator(strokeWidth: 2, color: Colors.white))
                    : const Icon(Icons.assignment_return_rounded, size: 18),
                label: Text(_loadingIds.contains(doc.id) ? 'Processing...' : 'Return Toy', style: const TextStyle(fontWeight: FontWeight.w800)),
                style: ElevatedButton.styleFrom(
                  backgroundColor: AppColors.coral,
                  foregroundColor: Colors.white,
                  padding: const EdgeInsets.symmetric(vertical: 12),
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                  elevation: 0,
                ),
              ),
            ),
          ],
          if (isPendingReceipt && isHolder) ...[
            const SizedBox(height: 14),
            Container(
              padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 10),
              decoration: BoxDecoration(
                color: AppColors.primary.withOpacity(0.06),
                borderRadius: BorderRadius.circular(10),
                border: Border.all(color: AppColors.primary.withOpacity(0.2)),
              ),
              child: const Row(children: [
                Icon(Icons.hourglass_top_rounded, size: 16, color: AppColors.primary),
                SizedBox(width: 8),
                Expanded(
                  child: Text('Waiting for owner to confirm receipt',
                      style: TextStyle(fontSize: 12.5, color: AppColors.primary, fontWeight: FontWeight.w600)),
                ),
              ]),
            ),
          ],
          if (isPendingReceipt && isOwner) ...[
            const SizedBox(height: 14),
            SizedBox(
              width: double.infinity,
              child: ElevatedButton.icon(
                onPressed: _loadingIds.contains(doc.id) ? null : () => _confirmReceipt(ctx, doc),
                icon: _loadingIds.contains(doc.id)
                    ? const SizedBox(width: 16, height: 16, child: CircularProgressIndicator(strokeWidth: 2, color: Colors.white))
                    : const Icon(Icons.check_circle_rounded, size: 18),
                label: Text(_loadingIds.contains(doc.id) ? 'Processing...' : 'Confirm Receipt', style: const TextStyle(fontWeight: FontWeight.w800)),
                style: ElevatedButton.styleFrom(
                  backgroundColor: AppColors.success,
                  foregroundColor: Colors.white,
                  padding: const EdgeInsets.symmetric(vertical: 12),
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                  elevation: 0,
                ),
              ),
            ),
          ],
          if ((isPendingReceipt || isActive) && !isDonation) ...[
            const SizedBox(height: 10),
            SizedBox(
              width: double.infinity,
              child: OutlinedButton.icon(
                onPressed: _loadingIds.contains(doc.id) ? null : () => _reportDispute(ctx, doc),
                icon: _loadingIds.contains(doc.id)
                    ? const SizedBox(width: 14, height: 14, child: CircularProgressIndicator(strokeWidth: 2, color: AppColors.error))
                    : const Icon(Icons.warning_amber_rounded, size: 16, color: AppColors.error),
                label: const Text('Report Issue', style: TextStyle(fontWeight: FontWeight.w700, color: AppColors.error)),
                style: OutlinedButton.styleFrom(
                  foregroundColor: AppColors.error,
                  side: BorderSide(color: AppColors.error.withOpacity(0.4)),
                  padding: const EdgeInsets.symmetric(vertical: 12),
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                ),
              ),
            ),
          ],
          if (isDisputed) ...[
            const SizedBox(height: 14),
            Container(
              padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 10),
              decoration: BoxDecoration(
                color: AppColors.error.withOpacity(0.06),
                borderRadius: BorderRadius.circular(10),
                border: Border.all(color: AppColors.error.withOpacity(0.2)),
              ),
              child: Row(children: [
                const Icon(Icons.warning_amber_rounded, size: 16, color: AppColors.error),
                const SizedBox(width: 8),
                Expanded(
                  child: Text(
                    'Dispute: ${e['disputeReason'] ?? 'No reason provided'}',
                    style: const TextStyle(fontSize: 12.5, color: AppColors.error, fontWeight: FontWeight.w600),
                  ),
                ),
              ]),
            ),
          ],
        ]),
      ),
    );
  }

  @override
  Widget build(BuildContext context) {
    final currentUid = _auth.currentUser?.uid ?? '';
    return Scaffold(
      appBar: AppBar(
        title: const Text('My Exchanges'),
        actions: [
          IconButton(
            icon: const Icon(Icons.qr_code_scanner_rounded),
            onPressed: () => navigateTo(context, const QRScannerScreen()),
            tooltip: 'Scan QR Code',
          ),
          IconButton(
            icon: const Icon(Icons.refresh_rounded),
            onPressed: () => setState(() {}),
            tooltip: 'Refresh',
          ),
        ],
      ),
      body: StreamBuilder<QuerySnapshot>(
        stream: FirestoreService.streamExchangesOwned(),
        builder: (context, ownedSnap) {
          return StreamBuilder<QuerySnapshot>(
            stream: FirestoreService.streamExchangesHeld(),
            builder: (context, heldSnap) {
              if (ownedSnap.hasError || heldSnap.hasError) {
                debugPrint('Exchanges error: ${ownedSnap.error ?? heldSnap.error}');
                return errorState(message: 'Failed to load exchanges. Please try again.');
              }
              if (ownedSnap.connectionState == ConnectionState.waiting &&
                  heldSnap.connectionState == ConnectionState.waiting &&
                  !ownedSnap.hasData &&
                  !heldSnap.hasData) {
                return const Center(
                    child: CircularProgressIndicator(color: AppColors.primary));
              }

              final allDocs = [
                ...(ownedSnap.data?.docs ?? []),
                ...(heldSnap.data?.docs ?? []),
              ];
              final seen = <String>{};
              final docs = allDocs.where((d) => seen.add(d.id)).toList();
              docs.sort((a, b) {
                final aTs = (a.data() as Map)['createdAt'] as Timestamp?;
                final bTs = (b.data() as Map)['createdAt'] as Timestamp?;
                if (aTs == null && bTs == null) return 0;
                if (aTs == null) return 1;
                if (bTs == null) return -1;
                return bTs.compareTo(aTs);
              });

              if (docs.isEmpty) {
                return emptyState(
                    icon: Icons.swap_horizontal_circle_rounded,
                    title: 'No exchanges yet',
                    subtitle: 'Your active and completed exchanges will appear here',
                    color: AppColors.accent);
              }

              final active =
              docs.where((d) {
                final s = (d.data() as Map)['status'];
                return s == 'active' || s == 'pending_receipt' || s == 'disputed';
              }).toList();
              final completed =
              docs.where((d) => (d.data() as Map)['status'] == 'completed').toList();

              return Center(
                child: ConstrainedBox(
                  constraints: const BoxConstraints(maxWidth: 800),
                  child: ListView(padding: const EdgeInsets.all(16), children: [
                    if (active.isNotEmpty) ...[
                      _sectionHeader('Active', active.length, AppColors.coral),
                      const SizedBox(height: 10),
                      ...active.map((d) => Padding(
                          padding: const EdgeInsets.only(bottom: 10),
                          child: _buildCard(context, d, currentUid))),
                    ],
                    if (completed.isNotEmpty) ...[
                      if (active.isNotEmpty) const SizedBox(height: 8),
                      _sectionHeader('Completed', completed.length, AppColors.success),
                      const SizedBox(height: 10),
                      ...completed.map((d) => Padding(
                          padding: const EdgeInsets.only(bottom: 10),
                          child: _buildCard(context, d, currentUid))),
                    ],
                  ]),
                ),
              );
            },
          );
        },
      ),
    );
  }

  Widget _sectionHeader(String title, int count, Color color) {
    return Row(children: [
      Container(
        width: 3.5,
        height: 16,
        decoration: BoxDecoration(color: color, borderRadius: BorderRadius.circular(2)),
      ),
      const SizedBox(width: 8),
      Text(title,
          style: TextStyle(
              fontSize: 14, fontWeight: FontWeight.w800, color: color, letterSpacing: 0.3)),
      const SizedBox(width: 8),
      Container(
        padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 2),
        decoration: BoxDecoration(
            color: color.withOpacity(0.1), borderRadius: BorderRadius.circular(20)),
        child: Text('$count',
            style: TextStyle(fontSize: 12, fontWeight: FontWeight.w800, color: color)),
      ),
    ]);
  }
}

// ==================== NOTIFICATIONS SCREEN ====================
class NotificationsScreen extends StatefulWidget {
  const NotificationsScreen({super.key});
  @override
  State<NotificationsScreen> createState() => _NotificationsScreenState();
}

class _NotificationsScreenState extends State<NotificationsScreen> {
  bool _isMarkingRead = false;

  IconData _typeIcon(String type) {
    switch (type) {
      case 'request_received': return Icons.inbox_rounded;
      case 'request_accepted': return Icons.check_circle_rounded;
      case 'request_rejected': return Icons.cancel_rounded;
      case 'toy_returned': return Icons.assignment_return_rounded;
      case 'receipt_confirmed': return Icons.verified_rounded;
      case 'new_message': return Icons.message_rounded;
      case 'dispute_opened': return Icons.warning_amber_rounded;
      case 'return_reminder': return Icons.event_rounded;
      case 'return_overdue': return Icons.warning_amber_rounded;
      default: return Icons.notifications_rounded;
    }
  }

  Color _typeColor(String type) {
    switch (type) {
      case 'request_received': return AppColors.primary;
      case 'request_accepted': return AppColors.success;
      case 'request_rejected': return AppColors.error;
      case 'toy_returned': return AppColors.coral;
      case 'receipt_confirmed': return AppColors.success;
      case 'new_message': return AppColors.accent;
      case 'dispute_opened': return AppColors.error;
      case 'return_reminder': return AppColors.warning;
      case 'return_overdue': return AppColors.error;
      default: return AppColors.accent;
    }
  }

  String _timeAgo(Timestamp? ts) {
    if (ts == null) return '';
    final diff = DateTime.now().difference(ts.toDate().toLocal());
    if (diff.inSeconds < 60) return 'Just now';
    if (diff.inMinutes < 60) return '${diff.inMinutes}m ago';
    if (diff.inHours < 24) return '${diff.inHours}h ago';
    if (diff.inDays == 1) return 'Yesterday';
    return '${diff.inDays}d ago';
  }

  Future<void> _markAllRead() async {
    if (_isMarkingRead) return;
    setState(() => _isMarkingRead = true);
    try {
      await FirestoreService.markAllRead();
    } catch (e) {
      debugPrint('markAllRead error: $e');
    }
    if (mounted) setState(() => _isMarkingRead = false);
  }

  void _handleNotificationTap(Map<String, dynamic> data, String docId) async {
    final type = data['type'] ?? '';
    final relatedId = data['relatedId'] ?? '';

    if (data['isRead'] == false) {
      try {
        await FirestoreService.markRead(docId);
      } catch (e) {
        debugPrint('markRead error: $e');
      }
    }

    if (!mounted) return;

    switch (type) {
      case 'request_received':
        navigateTo(context, const RequestsScreen());
        break;
      case 'request_accepted':
      case 'request_rejected':
        navigateTo(context, const MyExchangesScreen());
        break;
      case 'toy_returned':
      case 'receipt_confirmed':
        navigateTo(context, const MyExchangesScreen());
        break;
      case 'new_message':
        if (relatedId.isNotEmpty) {
          final otherPartyName = data['body']?.toString().contains('from') ?? false
              ? data['body'].toString().split('from').last.trim().replaceAll('.', '')
              : 'Chat';
          navigateTo(context, ChatScreen(
            exchangeId: relatedId,
            otherPartyName: otherPartyName,
          ));
        }
        break;
      case 'dispute_opened':
        if (relatedId.isNotEmpty) {
          navigateTo(context, DisputeResolutionScreen(exchangeId: relatedId));
        }
        break;
      case 'return_reminder':
      case 'return_overdue':
        navigateTo(context, const MyExchangesScreen());
        break;
      default:
        break;
    }
  }

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return StreamBuilder<QuerySnapshot>(
      stream: FirestoreService.streamNotifications(),
      builder: (context, snap) {
        if (snap.hasError) debugPrint('Notifications stream error: ${snap.error}');
        final docs = snap.data?.docs ?? [];
        final isLoading = snap.connectionState == ConnectionState.waiting && !snap.hasData;
        final unread = docs.where((d) {
          final data = d.data() as Map<String, dynamic>;
          return data['isRead'] == false;
        }).length;

        return Scaffold(
          appBar: AppBar(
            title: Row(mainAxisSize: MainAxisSize.min, children: [
              const Text('Notifications'),
              if (unread > 0) ...[
                const SizedBox(width: 8),
                Container(
                  padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 2),
                  decoration: BoxDecoration(
                      color: AppColors.error, borderRadius: BorderRadius.circular(20)),
                  child: Text('$unread',
                      style: const TextStyle(
                          color: Colors.white, fontSize: 11, fontWeight: FontWeight.w800)),
                ),
              ],
            ]),
            actions: [
              if (unread > 0)
                _isMarkingRead
                    ? const Padding(
                  padding: EdgeInsets.symmetric(horizontal: 16),
                  child: SizedBox(
                    width: 18,
                    height: 18,
                    child: CircularProgressIndicator(
                        strokeWidth: 2, color: AppColors.primary),
                  ),
                )
                    : TextButton(
                  onPressed: _markAllRead,
                  child: const Text('Mark all read',
                      style: TextStyle(
                          color: AppColors.primary,
                          fontWeight: FontWeight.w700,
                          fontSize: 13)),
                ),
            ],
          ),
          body: Center(
            child: ConstrainedBox(
              constraints: const BoxConstraints(maxWidth: 700),
              child: isLoading
                  ? const Center(
                  child: CircularProgressIndicator(color: AppColors.primary))
                  : docs.isEmpty
                  ? emptyState(
                  icon: Icons.notifications_off_rounded,
                  title: 'No notifications yet',
                  subtitle: "You'll be notified here when things happen",
                  color: AppColors.primary)
                  : ListView.separated(
                padding: const EdgeInsets.all(16),
                itemCount: docs.length,
                separatorBuilder: (_, __) => const SizedBox(height: 8),
                itemBuilder: (_, i) {
                  final doc = docs[i];
                  final n = doc.data() as Map<String, dynamic>;
                  final isUnread = n['isRead'] == false;
                  final type = n['type'] ?? '';
                  final color = _typeColor(type);
                  return GestureDetector(
                    onTap: () => _handleNotificationTap(n, doc.id),
                    child: AnimatedContainer(
                      duration: const Duration(milliseconds: 200),
                      decoration: BoxDecoration(
                        color: isUnread ? color.withOpacity(0.04) : Theme.of(context).cardColor,
                        borderRadius: BorderRadius.circular(16),
                        boxShadow: AppShadows.card(context),
                        border: isUnread
                            ? Border.all(
                            color: color.withOpacity(0.2), width: 1.5)
                            : null,
                      ),
                      child: Padding(
                        padding: const EdgeInsets.all(14),
                        child: Row(
                          crossAxisAlignment: CrossAxisAlignment.start,
                          children: [
                            Container(
                              width: 42,
                              height: 42,
                              decoration: BoxDecoration(
                                color: color.withOpacity(0.12),
                                borderRadius: BorderRadius.circular(13),
                              ),
                              child: Icon(_typeIcon(type), size: 20, color: color),
                            ),
                            const SizedBox(width: 12),
                            Expanded(
                              child: Column(
                                crossAxisAlignment: CrossAxisAlignment.start,
                                children: [
                                  Row(children: [
                                    Expanded(
                                      child: Text(
                                        n['title'] ?? '',
                                        style: TextStyle(
                                          fontWeight: isUnread
                                              ? FontWeight.w800
                                              : FontWeight.w600,
                                          fontSize: 14,
                                          color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
                                        ),
                                      ),
                                    ),
                                    if (isUnread)
                                      Container(
                                        width: 8,
                                        height: 8,
                                        decoration: BoxDecoration(
                                            color: color,
                                            shape: BoxShape.circle),
                                      ),
                                  ]),
                                  const SizedBox(height: 4),
                                  Text(
                                    n['body'] ?? '',
                                    style: TextStyle(
                                        fontSize: 13,
                                        color: isDark ? AppColors.darkTextSecondary : Colors.grey.shade600,
                                        height: 1.4),
                                  ),
                                  const SizedBox(height: 6),
                                  Text(
                                    _timeAgo(n['createdAt'] as Timestamp?),
                                    style: TextStyle(
                                      fontSize: 11.5,
                                      color: Colors.grey.shade400,
                                      fontWeight: FontWeight.w600,
                                    ),
                                  ),
                                ],
                              ),
                            ),
                          ],
                        ),
                      ),
                    ),
                  );
                },
              ),
            ),
          ),
        );
      },
    );
  }
}

// ==================== DISPUTE RESOLUTION SCREEN ====================
class DisputeResolutionScreen extends StatefulWidget {
  final String exchangeId;
  const DisputeResolutionScreen({super.key, required this.exchangeId});

  @override
  State<DisputeResolutionScreen> createState() => _DisputeResolutionScreenState();
}

class _DisputeResolutionScreenState extends State<DisputeResolutionScreen> {
  final _resolutionCtrl = TextEditingController();
  bool _isLoading = false;
  Map<String, dynamic>? _exchangeData;
  Map<String, dynamic>? _disputeData;

  @override
  void initState() {
    super.initState();
    _loadData();
  }

  @override
  void dispose() {
    _resolutionCtrl.dispose();
    super.dispose();
  }

  Future<void> _loadData() async {
    setState(() => _isLoading = true);
    try {
      final exchangeDoc = await _db.collection('exchanges').doc(widget.exchangeId).get();
      if (exchangeDoc.exists) {
        _exchangeData = exchangeDoc.data() as Map<String, dynamic>?;
      }

      final disputeQuery = await _db
          .collection('disputes')
          .where('exchangeId', isEqualTo: widget.exchangeId)
          .where('status', isEqualTo: 'open')
          .get();

      if (disputeQuery.docs.isNotEmpty) {
        _disputeData = disputeQuery.docs.first.data();
      }
    } catch (e) {
      debugPrint('Load dispute data error: $e');
    }
    if (mounted) setState(() => _isLoading = false);
  }

  Future<void> _resolveDispute() async {
    if (_resolutionCtrl.text.trim().isEmpty) {
      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
        content: Text('Please enter a resolution'),
        backgroundColor: AppColors.error,
      ));
      return;
    }

    setState(() => _isLoading = true);
    try {
      await FirestoreService.resolveDispute(
        disputeDocId: _disputeData?['disputeId'] ?? '',
        exchangeDocId: widget.exchangeId,
        toyDocId: _exchangeData?['toyId'] ?? '',
        resolution: _resolutionCtrl.text.trim(),
      );

      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
        content: Row(children: [
          Icon(Icons.check_circle_rounded, color: Colors.white, size: 18),
          SizedBox(width: 8),
          Expanded(child: Text('Dispute resolved successfully', style: TextStyle(fontWeight: FontWeight.w600))),
        ]),
        backgroundColor: AppColors.success,
        margin: EdgeInsets.all(14),
      ));
      Navigator.pop(context);
    } catch (e) {
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
          content: Text('Failed to resolve dispute. Please try again.'),
          backgroundColor: AppColors.error,
          margin: EdgeInsets.all(14),
        ));
      }
    }
    if (mounted) setState(() => _isLoading = false);
  }

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;

    return Scaffold(
      appBar: AppBar(
        title: const Text('Resolve Dispute'),
        backgroundColor: AppColors.error.withOpacity(0.1),
      ),
      body: _isLoading && _exchangeData == null
          ? const Center(child: CircularProgressIndicator(color: AppColors.primary))
          : SingleChildScrollView(
        padding: const EdgeInsets.all(20),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Container(
              padding: const EdgeInsets.all(16),
              decoration: BoxDecoration(
                color: AppColors.error.withOpacity(0.08),
                borderRadius: BorderRadius.circular(16),
                border: Border.all(color: AppColors.error.withOpacity(0.2)),
              ),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Row(
                    children: [
                      const Icon(Icons.warning_amber_rounded, color: AppColors.error),
                      const SizedBox(width: 8),
                      Text(
                        'Dispute Details',
                        style: TextStyle(
                          fontSize: 16,
                          fontWeight: FontWeight.w800,
                          color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
                        ),
                      ),
                    ],
                  ),
                  const SizedBox(height: 12),
                  Text(
                    'Toy: ${_exchangeData?['toyName'] ?? 'Unknown'}',
                    style: TextStyle(
                      fontSize: 14,
                      color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary,
                    ),
                  ),
                  const SizedBox(height: 4),
                  Text(
                    'Reason: ${_disputeData?['reason'] ?? 'No reason provided'}',
                    style: TextStyle(
                      fontSize: 14,
                      color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary,
                    ),
                  ),
                  const SizedBox(height: 4),
                  Text(
                    'Reported by: ${_disputeData?['reporterName'] ?? 'Unknown'}',
                    style: TextStyle(
                      fontSize: 14,
                      color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary,
                    ),
                  ),
                ],
              ),
            ),
            const SizedBox(height: 24),
            Text(
              'Resolution',
              style: TextStyle(
                fontSize: 16,
                fontWeight: FontWeight.w800,
                color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
              ),
            ),
            const SizedBox(height: 8),
            Text(
              'Describe how this dispute was resolved:',
              style: TextStyle(
                fontSize: 13,
                color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary,
              ),
            ),
            const SizedBox(height: 12),
            TextField(
              controller: _resolutionCtrl,
              maxLines: 4,
              decoration: InputDecoration(
                hintText: 'e.g., Toy was returned in good condition, issue was a misunderstanding...',
                border: OutlineInputBorder(borderRadius: BorderRadius.circular(14)),
                filled: true,
                fillColor: isDark ? AppColors.darkInputBg : const Color(0xFFF8F8FC),
              ),
            ),
            const SizedBox(height: 24),
            SizedBox(
              width: double.infinity,
              child: ElevatedButton.icon(
                onPressed: _isLoading ? null : _resolveDispute,
                icon: _isLoading
                    ? const SizedBox(
                  width: 18,
                  height: 18,
                  child: CircularProgressIndicator(
                    strokeWidth: 2,
                    color: Colors.white,
                  ),
                )
                    : const Icon(Icons.check_circle_rounded),
                label: Text(
                  _isLoading ? 'Resolving...' : 'Mark as Resolved',
                  style: const TextStyle(fontWeight: FontWeight.w800),
                ),
                style: ElevatedButton.styleFrom(
                  backgroundColor: AppColors.success,
                  foregroundColor: Colors.white,
                  padding: const EdgeInsets.symmetric(vertical: 14),
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                  elevation: 0,
                ),
              ),
            ),
            const SizedBox(height: 12),
            SizedBox(
              width: double.infinity,
              child: OutlinedButton.icon(
                onPressed: _isLoading ? null : () => Navigator.pop(context),
                icon: const Icon(Icons.close_rounded),
                label: const Text('Cancel'),
                style: OutlinedButton.styleFrom(
                  foregroundColor: AppColors.error,
                  side: BorderSide(color: AppColors.error.withOpacity(0.4)),
                  padding: const EdgeInsets.symmetric(vertical: 14),
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                ),
              ),
            ),
          ],
        ),
      ),
    );
  }
}

// ==================== BLACKLIST SCREEN ====================
// ==================== QR RECEIPT SCREEN ====================
class ToyQRReceiptScreen extends StatelessWidget {
  final Map<String, dynamic> exchangeData;
  final String exchangeId;
  const ToyQRReceiptScreen({super.key, required this.exchangeData, required this.exchangeId});

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final toyName = exchangeData['toyName'] ?? 'Unknown Toy';
    final ownerName = exchangeData['ownerName'] ?? 'Unknown';
    final holderName = exchangeData['holderName'] ?? 'Unknown';
    final returnDateStr = exchangeData['returnDate'] as String?;
    final returnDate = returnDateStr != null ? DateTime.tryParse(returnDateStr) : null;
    final status = exchangeData['status'] ?? 'active';
    final fee = exchangeData['fee'];
    final currentUid = _auth.currentUser?.uid ?? '';
    final isOwner = exchangeData['ownerId'] == currentUid;

    // QR data — encodes key exchange info
    final qrData = 'toyshare://exchange?id=$exchangeId&toy=${Uri.encodeComponent(toyName)}&owner=${Uri.encodeComponent(ownerName)}&status=$status';

    return Scaffold(
      appBar: AppBar(title: const Text('Exchange Receipt')),
      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20),
        child: Center(
          child: ConstrainedBox(
            constraints: const BoxConstraints(maxWidth: 480),
            child: Column(children: [
              // ── Receipt Card ──
              Container(
                decoration: BoxDecoration(
                  color: isDark ? AppColors.darkCardBg : Colors.white,
                  borderRadius: BorderRadius.circular(24),
                  boxShadow: AppShadows.card(context),
                  border: Border.all(color: isDark ? AppColors.darkDivider : AppColors.divider),
                ),
                child: Column(children: [
                  // Header
                  Container(
                    width: double.infinity,
                    padding: const EdgeInsets.all(20),
                    decoration: const BoxDecoration(
                      gradient: LinearGradient(
                        colors: [Color(0xFF3D32A8), Color(0xFF7B6FE8)],
                        begin: Alignment.topLeft, end: Alignment.bottomRight,
                      ),
                      borderRadius: BorderRadius.only(topLeft: Radius.circular(24), topRight: Radius.circular(24)),
                    ),
                    child: Column(children: [
                      const Icon(Icons.receipt_long_rounded, color: Colors.white, size: 36),
                      const SizedBox(height: 8),
                      const Text('Toy Exchange Receipt', style: TextStyle(color: Colors.white, fontSize: 18, fontWeight: FontWeight.w900)),
                      const SizedBox(height: 4),
                      Text(DateFormat('MMM d, yyyy · h:mm a').format(DateTime.now()),
                        style: TextStyle(color: Colors.white.withOpacity(0.75), fontSize: 12.5)),
                    ]),
                  ),
                  // Details
                  Padding(
                    padding: const EdgeInsets.all(20),
                    child: Column(children: [
                      // Toy photo if available
                      if (exchangeData['imageUrl'] != null && (exchangeData['imageUrl'] as String).isNotEmpty) ...[
                        ClipRRect(
                          borderRadius: BorderRadius.circular(12),
                          child: Image.network(exchangeData['imageUrl'] as String,
                            height: 150, width: double.infinity, fit: BoxFit.cover,
                            errorBuilder: (_, __, ___) => const SizedBox.shrink()),
                        ),
                        const SizedBox(height: 16),
                      ],
                      _receiptRow('🧸 Toy', toyName, bold: true),
                      _receiptRow('👤 Owner', ownerName),
                      _receiptRow('🤝 Borrower', holderName),
                      _receiptRow('📋 Status', status.toUpperCase()),
                      if (returnDate != null)
                        _receiptRow('📅 Return By', DateFormat('MMM d, yyyy · h:mm a').format(returnDate)),
                      if (fee != null && (fee as num) > 0)
                        _receiptRow('💰 Fee', 'PKR ${(fee as num).toStringAsFixed(0)}'),
                      _receiptRow('🔑 Exchange ID', exchangeId.substring(0, 10).toUpperCase() + '...'),
                    ]),
                  ),
                  Divider(height: 1, color: isDark ? AppColors.darkDivider : AppColors.divider),
                  // QR Code
                  Padding(
                    padding: const EdgeInsets.all(24),
                    child: Column(children: [
                      Text(
                        isOwner ? 'Owner QR — Borrower scans this' : 'Borrower QR — Owner scans this',
                        style: TextStyle(fontSize: 13, fontWeight: FontWeight.w700,
                          color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary),
                        textAlign: TextAlign.center,
                      ),
                      const SizedBox(height: 16),
                      Container(
                        padding: const EdgeInsets.all(16),
                        decoration: BoxDecoration(
                          color: Colors.white,
                          borderRadius: BorderRadius.circular(16),
                          boxShadow: [BoxShadow(color: Colors.black.withOpacity(0.08), blurRadius: 12, offset: const Offset(0, 4))],
                        ),
                        child: QrImageView(
                          data: qrData,
                          version: QrVersions.auto,
                          size: 200,
                          errorCorrectionLevel: QrErrorCorrectLevel.M,
                          eyeStyle: const QrEyeStyle(
                            eyeShape: QrEyeShape.square,
                            color: Color(0xFF3D32A8),
                          ),
                          dataModuleStyle: const QrDataModuleStyle(
                            dataModuleShape: QrDataModuleShape.square,
                            color: Color(0xFF3D32A8),
                          ),
                        ),
                      ),
                      const SizedBox(height: 12),
                      Text('Scan to verify this exchange', style: TextStyle(fontSize: 12, color: Colors.grey.shade500)),
                    ]),
                  ),
                ]),
              ),
              const SizedBox(height: 20),
              // ── Report Lost/Stolen/Damaged button ──
              SizedBox(
                width: double.infinity,
                child: OutlinedButton.icon(
                  onPressed: () => navigateTo(context, ToyIncidentReportScreen(
                    exchangeId: exchangeId,
                    toyName: toyName,
                    ownerName: ownerName,
                    holderName: holderName,
                  )),
                  icon: const Icon(Icons.warning_amber_rounded, color: AppColors.error),
                  label: const Text('Report Incident (Lost/Stolen/Damaged)', style: TextStyle(color: AppColors.error, fontWeight: FontWeight.w700)),
                  style: OutlinedButton.styleFrom(
                    side: BorderSide(color: AppColors.error.withOpacity(0.4)),
                    padding: const EdgeInsets.symmetric(vertical: 14),
                    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
                  ),
                ),
              ),
              const SizedBox(height: 30),
            ]),
          ),
        ),
      ),
    );
  }

  Widget _receiptRow(String label, String value, {bool bold = false}) {
    return Padding(
      padding: const EdgeInsets.symmetric(vertical: 7),
      child: Row(children: [
        Expanded(flex: 2, child: Text(label, style: const TextStyle(fontSize: 13, color: AppColors.textSecondary, fontWeight: FontWeight.w500))),
        Expanded(flex: 3, child: Text(value, style: TextStyle(fontSize: 13.5, fontWeight: bold ? FontWeight.w900 : FontWeight.w700,
          color: bold ? AppColors.primary : null), textAlign: TextAlign.end)),
      ]),
    );
  }
}

// ==================== QR SCANNER SCREEN ====================
class QRScannerScreen extends StatefulWidget {
  const QRScannerScreen({super.key});
  @override
  State<QRScannerScreen> createState() => _QRScannerScreenState();
}

class _QRScannerScreenState extends State<QRScannerScreen> {
  bool _scanned = false;

  void _onDetect(String rawValue) {
    if (_scanned) return;
    setState(() => _scanned = true);

    if (!rawValue.startsWith('toyshare://exchange')) {
      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
        content: Text('Invalid ToyShare QR code.'),
        backgroundColor: AppColors.error,
        margin: EdgeInsets.all(14),
      ));
      setState(() => _scanned = false);
      return;
    }

    final uri = Uri.tryParse(rawValue);
    if (uri == null) return;

    final exchangeId = uri.queryParameters['id'] ?? '';
    final toy = uri.queryParameters['toy'] ?? '';
    final owner = uri.queryParameters['owner'] ?? '';
    final status = uri.queryParameters['status'] ?? '';

    Navigator.pop(context);
    showDialog(
      context: context,
      builder: (ctx) => AlertDialog(
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
        title: Row(children: [
          Container(width: 36, height: 36,
            decoration: BoxDecoration(color: AppColors.success.withOpacity(0.1), borderRadius: BorderRadius.circular(10)),
            child: const Icon(Icons.qr_code_scanner_rounded, color: AppColors.success, size: 20)),
          const SizedBox(width: 12),
          const Flexible(child: Text('QR Verified ✓', style: TextStyle(fontWeight: FontWeight.w800, fontSize: 17))),
        ]),
        content: Column(mainAxisSize: MainAxisSize.min, crossAxisAlignment: CrossAxisAlignment.start, children: [
          _infoLine('Toy', toy),
          _infoLine('Owner', owner),
          _infoLine('Status', status.toUpperCase()),
          _infoLine('Exchange ID', exchangeId.isNotEmpty ? '${exchangeId.substring(0, 10).toUpperCase()}...' : '—'),
          const SizedBox(height: 12),
          Container(
            padding: const EdgeInsets.all(10),
            decoration: BoxDecoration(color: AppColors.success.withOpacity(0.07), borderRadius: BorderRadius.circular(10)),
            child: const Row(children: [
              Icon(Icons.verified_rounded, size: 14, color: AppColors.success),
              SizedBox(width: 8),
              Expanded(child: Text('This QR code is valid and belongs to a ToyShare exchange.', style: TextStyle(fontSize: 12, color: AppColors.success, height: 1.4))),
            ]),
          ),
        ]),
        actions: [
          ElevatedButton(
            onPressed: () => Navigator.pop(ctx),
            style: ElevatedButton.styleFrom(backgroundColor: AppColors.success, foregroundColor: Colors.white,
              elevation: 0, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
            child: const Text('Done', style: TextStyle(fontWeight: FontWeight.w700)),
          ),
        ],
      ),
    );
  }

  Widget _infoLine(String label, String value) => Padding(
    padding: const EdgeInsets.symmetric(vertical: 4),
    child: Row(children: [
      Text('$label: ', style: const TextStyle(fontSize: 13, color: AppColors.textSecondary, fontWeight: FontWeight.w500)),
      Expanded(child: Text(value, style: const TextStyle(fontSize: 13.5, fontWeight: FontWeight.w700))),
    ]),
  );

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Scan QR Code')),
      body: Stack(children: [
        MobileScanner(
          onDetect: (capture) {
            final barcode = capture.barcodes.firstOrNull;
            if (barcode?.rawValue != null) _onDetect(barcode!.rawValue!);
          },
        ),
        // Overlay
        Center(
          child: Container(
            width: 240, height: 240,
            decoration: BoxDecoration(
              border: Border.all(color: AppColors.primary, width: 3),
              borderRadius: BorderRadius.circular(20),
            ),
          ),
        ),
        Positioned(
          bottom: 40, left: 0, right: 0,
          child: Column(children: [
            Container(
              padding: const EdgeInsets.symmetric(horizontal: 20, vertical: 10),
              decoration: BoxDecoration(
                color: Colors.black.withOpacity(0.6),
                borderRadius: BorderRadius.circular(20),
              ),
              child: const Text('Point camera at a ToyShare QR code',
                style: TextStyle(color: Colors.white, fontSize: 14, fontWeight: FontWeight.w600)),
            ),
          ]),
        ),
      ]),
    );
  }
}

// ==================== TOY INCIDENT REPORT SCREEN ====================
class ToyIncidentReportScreen extends StatefulWidget {
  final String exchangeId, toyName, ownerName, holderName;
  const ToyIncidentReportScreen({super.key, required this.exchangeId, required this.toyName, required this.ownerName, required this.holderName});
  @override
  State<ToyIncidentReportScreen> createState() => _ToyIncidentReportScreenState();
}

class _ToyIncidentReportScreenState extends State<ToyIncidentReportScreen> {
  String _incidentType = 'damaged';
  final _descCtrl = TextEditingController();
  bool _submitting = false;

  final _types = [
    {'value': 'damaged', 'label': 'Damaged', 'icon': Icons.broken_image_rounded, 'color': AppColors.warning},
    {'value': 'lost', 'label': 'Lost', 'icon': Icons.search_off_rounded, 'color': AppColors.error},
    {'value': 'stolen', 'label': 'Stolen', 'icon': Icons.gpp_bad_rounded, 'color': AppColors.error},
  ];

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Scaffold(
      appBar: AppBar(title: const Text('Report Incident')),
      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20),
        child: Center(
          child: ConstrainedBox(
            constraints: const BoxConstraints(maxWidth: 480),
            child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
              // Toy info
              Container(
                padding: const EdgeInsets.all(14),
                decoration: BoxDecoration(
                  color: AppColors.error.withOpacity(0.06),
                  borderRadius: BorderRadius.circular(14),
                  border: Border.all(color: AppColors.error.withOpacity(0.2)),
                ),
                child: Row(children: [
                  const Icon(Icons.toys_rounded, color: AppColors.error, size: 20),
                  const SizedBox(width: 10),
                  Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                    Text(widget.toyName, style: const TextStyle(fontSize: 15, fontWeight: FontWeight.w800)),
                    Text('Owner: ${widget.ownerName} · Borrower: ${widget.holderName}',
                      style: TextStyle(fontSize: 12, color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary)),
                  ])),
                ]),
              ),
              const SizedBox(height: 24),
              Text('Type of Incident', style: TextStyle(fontSize: 14, fontWeight: FontWeight.w800,
                color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
              const SizedBox(height: 12),
              Row(children: _types.map((t) {
                final isSelected = _incidentType == t['value'];
                final color = t['color'] as Color;
                return Expanded(
                  child: Padding(
                    padding: const EdgeInsets.only(right: 8),
                    child: GestureDetector(
                      onTap: () => setState(() => _incidentType = t['value'] as String),
                      child: AnimatedContainer(
                        duration: const Duration(milliseconds: 180),
                        padding: const EdgeInsets.symmetric(vertical: 14),
                        decoration: BoxDecoration(
                          color: isSelected ? color.withOpacity(0.12) : (isDark ? AppColors.darkInputBg : const Color(0xFFF3F4F8)),
                          borderRadius: BorderRadius.circular(14),
                          border: Border.all(color: isSelected ? color : (isDark ? AppColors.darkBorder : AppColors.divider), width: isSelected ? 2 : 1),
                        ),
                        child: Column(children: [
                          Icon(t['icon'] as IconData, color: isSelected ? color : Colors.grey.shade400, size: 24),
                          const SizedBox(height: 6),
                          Text(t['label'] as String, style: TextStyle(fontSize: 12.5, fontWeight: FontWeight.w700,
                            color: isSelected ? color : Colors.grey.shade500)),
                        ]),
                      ),
                    ),
                  ),
                );
              }).toList()),
              const SizedBox(height: 24),
              Text('Description', style: TextStyle(fontSize: 14, fontWeight: FontWeight.w800,
                color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
              const SizedBox(height: 10),
              TextField(
                controller: _descCtrl,
                maxLines: 5,
                decoration: InputDecoration(
                  hintText: 'Describe what happened in detail...',
                  border: OutlineInputBorder(borderRadius: BorderRadius.circular(14)),
                  filled: true,
                  fillColor: isDark ? AppColors.darkInputBg : const Color(0xFFF8F8FC),
                  contentPadding: const EdgeInsets.all(14),
                ),
                style: TextStyle(fontSize: 14, color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
              ),
              const SizedBox(height: 24),
              SizedBox(
                width: double.infinity,
                child: ElevatedButton.icon(
                  onPressed: _submitting ? null : () async {
                    if (_descCtrl.text.trim().isEmpty) {
                      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
                        content: Text('Please describe the incident.'),
                        backgroundColor: AppColors.error,
                        margin: EdgeInsets.all(14),
                      ));
                      return;
                    }
                    setState(() => _submitting = true);
                    try {
                      await _db.collection('incidents').add({
                        'exchangeId': widget.exchangeId,
                        'toyName': widget.toyName,
                        'reporterId': _auth.currentUser?.uid,
                        'reporterName': _auth.currentUser?.displayName ?? 'Unknown',
                        'ownerName': widget.ownerName,
                        'holderName': widget.holderName,
                        'incidentType': _incidentType,
                        'description': _descCtrl.text.trim(),
                        'status': 'open',
                        'createdAt': FieldValue.serverTimestamp(),
                      });
                      // Also create a dispute
                      await FirestoreService.reportDispute(
                        exchangeDocId: widget.exchangeId,
                        toyDocId: '',
                        otherPartyId: '',
                        otherPartyName: widget.ownerName,
                        reason: '[$_incidentType] ${_descCtrl.text.trim()}',
                      );
                      if (!mounted) return;
                      ScaffoldMessenger.of(context).showSnackBar(SnackBar(
                        content: Row(children: [
                          const Icon(Icons.check_circle_rounded, color: Colors.white, size: 18),
                          const SizedBox(width: 8),
                          Expanded(child: Text('Incident reported. Exchange marked as disputed.', style: const TextStyle(fontWeight: FontWeight.w600))),
                        ]),
                        backgroundColor: AppColors.success,
                        margin: const EdgeInsets.all(14),
                      ));
                      Navigator.pop(context);
                    } catch (e) {
                      debugPrint('Incident report error: $e');
                      if (mounted) {
                        ScaffoldMessenger.of(context).showSnackBar(SnackBar(
                          content: Row(children: [
                            const Icon(Icons.error_outline, color: Colors.white, size: 18),
                            const SizedBox(width: 8),
                            Expanded(child: Text(
                              'Failed: ${e.toString().replaceAll('Exception:', '').trim()}',
                              style: const TextStyle(fontWeight: FontWeight.w600))),
                          ]),
                          backgroundColor: AppColors.error,
                          margin: const EdgeInsets.all(14),
                        ));
                      }
                    }
                    if (mounted) setState(() => _submitting = false);
                  },
                  icon: _submitting
                      ? const SizedBox(width: 16, height: 16, child: CircularProgressIndicator(strokeWidth: 2, color: Colors.white))
                      : const Icon(Icons.flag_rounded, size: 18),
                  label: Text(_submitting ? 'Submitting...' : 'Submit Report', style: const TextStyle(fontWeight: FontWeight.w800, fontSize: 15)),
                  style: ElevatedButton.styleFrom(
                    backgroundColor: AppColors.error, foregroundColor: Colors.white,
                    padding: const EdgeInsets.symmetric(vertical: 15),
                    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
                    elevation: 0,
                  ),
                ),
              ),
              const SizedBox(height: 30),
            ]),
          ),
        ),
      ),
    );
  }
}

// ==================== PUBLIC PROFILE SCREEN ====================
class PublicProfileScreen extends StatelessWidget {
  final String userId;
  final String userName;
  const PublicProfileScreen({super.key, required this.userId, required this.userName});

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Scaffold(
      appBar: AppBar(title: Text(userName)),
      body: StreamBuilder<QuerySnapshot>(
        stream: FirestoreService.streamUserFeedback(userId),
        builder: (ctx, snap) {
          if (snap.connectionState == ConnectionState.waiting) {
            return const Center(child: CircularProgressIndicator(color: AppColors.primary));
          }
          final docs = snap.data?.docs ?? [];
          int positive = 0;
          for (final d in docs) {
            final data = d.data() as Map<String, dynamic>;
            if ((data['isPositive'] == true) || (data['rating'] ?? 0) >= 4) positive++;
          }
          final total = docs.length;
          final pct = total > 0 ? ((positive / total) * 100).round() : 0;

          return CustomScrollView(slivers: [
            SliverToBoxAdapter(
              child: Container(
                width: double.infinity,
                padding: const EdgeInsets.fromLTRB(24, 32, 24, 28),
                decoration: const BoxDecoration(
                  gradient: LinearGradient(
                    colors: [Color(0xFF3D32A8), Color(0xFF7B6FE8)],
                    begin: Alignment.topLeft, end: Alignment.bottomRight,
                  ),
                ),
                child: Column(children: [
                  UserAvatar(name: userName, size: 72),
                  const SizedBox(height: 12),
                  Text(userName, style: const TextStyle(color: Colors.white, fontSize: 22, fontWeight: FontWeight.w900)),
                  const SizedBox(height: 6),
                  UserRatingBadge(userId: userId, lightMode: true, size: 15),
                  const SizedBox(height: 16),
                  if (total > 0)
                    Container(
                      padding: const EdgeInsets.symmetric(horizontal: 20, vertical: 12),
                      decoration: BoxDecoration(
                        color: Colors.white.withOpacity(0.12),
                        borderRadius: BorderRadius.circular(16),
                        border: Border.all(color: Colors.white.withOpacity(0.2)),
                      ),
                      child: Row(mainAxisAlignment: MainAxisAlignment.spaceEvenly, children: [
                        _statCol('$pct%', 'Rating'),
                        Container(width: 1, height: 30, color: Colors.white.withOpacity(0.3)),
                        _statCol('$positive', 'Positive'),
                        Container(width: 1, height: 30, color: Colors.white.withOpacity(0.3)),
                        _statCol('$total', 'Reviews'),
                      ]),
                    ),
                ]),
              ),
            ),
            if (docs.isEmpty)
              SliverFillRemaining(
                child: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [
                  const Icon(Icons.star_outline_rounded, size: 48, color: AppColors.textSecondary),
                  const SizedBox(height: 12),
                  Text('No reviews yet', style: TextStyle(fontSize: 16, fontWeight: FontWeight.w700,
                    color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                  const SizedBox(height: 4),
                  Text('Complete an exchange to receive reviews',
                    style: TextStyle(fontSize: 13, color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary)),
                ])),
              )
            else
              SliverPadding(
                padding: const EdgeInsets.fromLTRB(16, 16, 16, 24),
                sliver: SliverList(delegate: SliverChildBuilderDelegate((ctx, i) {
                  final d = docs[i].data() as Map<String, dynamic>;
                  final isPositive = (d['isPositive'] == true) || ((d['rating'] ?? 0) >= 4);
                  final fromName = d['fromUserName'] ?? 'Anonymous';
                  final comment = (d['comment'] ?? '').toString().trim();
                  final toyName = d['toyName'] ?? '';
                  final ts = d['createdAt'] as Timestamp?;
                  final date = ts != null ? DateFormat('MMM d, yyyy').format(ts.toDate()) : '';
                  return Container(
                    margin: const EdgeInsets.only(bottom: 10),
                    padding: const EdgeInsets.all(14),
                    decoration: BoxDecoration(
                      color: Theme.of(context).cardColor,
                      borderRadius: BorderRadius.circular(16),
                      border: Border.all(color: isDark ? AppColors.darkDivider : AppColors.divider),
                      boxShadow: AppShadows.card(context),
                    ),
                    child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                      Row(children: [
                        UserAvatar(name: fromName, size: 34, gradient: AppColors.heroGradient),
                        const SizedBox(width: 10),
                        Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                          Text(fromName, style: TextStyle(fontSize: 13.5, fontWeight: FontWeight.w800,
                            color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                          if (toyName.isNotEmpty)
                            Text('Re: $toyName', style: TextStyle(fontSize: 11.5,
                              color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary)),
                        ])),
                        Column(crossAxisAlignment: CrossAxisAlignment.end, children: [
                          Container(
                            padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 3),
                            decoration: BoxDecoration(
                              color: isPositive ? AppColors.success.withOpacity(0.1) : AppColors.error.withOpacity(0.1),
                              borderRadius: BorderRadius.circular(8),
                            ),
                            child: Row(mainAxisSize: MainAxisSize.min, children: [
                              Icon(isPositive ? Icons.thumb_up_rounded : Icons.thumb_down_rounded,
                                size: 11, color: isPositive ? AppColors.success : AppColors.error),
                              const SizedBox(width: 4),
                              Text(isPositive ? 'Positive' : 'Negative',
                                style: TextStyle(fontSize: 10.5, fontWeight: FontWeight.w700,
                                  color: isPositive ? AppColors.success : AppColors.error)),
                            ]),
                          ),
                          if (date.isNotEmpty) ...[
                            const SizedBox(height: 2),
                            Text(date, style: TextStyle(fontSize: 10.5, color: Colors.grey.shade500)),
                          ],
                        ]),
                      ]),
                      if (comment.isNotEmpty) ...[
                        const SizedBox(height: 10),
                        Container(
                          padding: const EdgeInsets.all(10),
                          decoration: BoxDecoration(
                            color: isDark ? AppColors.darkInputBg : const Color(0xFFF8F8FC),
                            borderRadius: BorderRadius.circular(10),
                          ),
                          child: Row(crossAxisAlignment: CrossAxisAlignment.start, children: [
                            Icon(Icons.format_quote_rounded, size: 14, color: Colors.grey.shade400),
                            const SizedBox(width: 6),
                            Expanded(child: Text(comment, style: TextStyle(fontSize: 13,
                              color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary, height: 1.4))),
                          ]),
                        ),
                      ],
                    ]),
                  );
                }, childCount: docs.length)),
              ),
          ]);
        },
      ),
    );
  }

  Widget _statCol(String value, String label) => Column(children: [
    Text(value, style: const TextStyle(color: Colors.white, fontSize: 20, fontWeight: FontWeight.w900)),
    const SizedBox(height: 2),
    Text(label, style: TextStyle(color: Colors.white.withOpacity(0.75), fontSize: 11.5)),
  ]);
}

// ==================== FEEDBACK / MY REVIEWS SCREEN ====================
class FeedbackScreen extends StatelessWidget {
  const FeedbackScreen({super.key});

  @override
  Widget build(BuildContext context) {
    final uid = _auth.currentUser?.uid ?? '';
    final isDark = Theme.of(context).brightness == Brightness.dark;

    return Scaffold(
      appBar: AppBar(title: const Text('My Reviews')),
      body: StreamBuilder<QuerySnapshot>(
        stream: _db
            .collection('feedback')
            .where('toUserId', isEqualTo: uid)
            .orderBy('createdAt', descending: true)
            .snapshots(),
        builder: (ctx, snap) {
          if (snap.connectionState == ConnectionState.waiting) {
            return const Center(child: CircularProgressIndicator(color: AppColors.primary));
          }
          if (snap.hasError) {
            return Center(child: Text('Error loading reviews', style: TextStyle(color: AppColors.textSecondary)));
          }
          final docs = snap.data?.docs ?? [];
          if (docs.isEmpty) {
            return Center(
              child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [
                Container(
                  width: 80, height: 80,
                  decoration: BoxDecoration(
                    color: AppColors.yellow.withOpacity(0.1),
                    shape: BoxShape.circle,
                  ),
                  child: const Icon(Icons.star_rounded, size: 40, color: AppColors.yellow),
                ),
                const SizedBox(height: 16),
                const Text('No reviews yet', style: TextStyle(fontSize: 17, fontWeight: FontWeight.w800)),
                const SizedBox(height: 6),
                Text('Complete an exchange to receive your first review!',
                  style: TextStyle(fontSize: 13, color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary),
                  textAlign: TextAlign.center),
              ]),
            );
          }

          // Calculate stats
          int positive = 0, negative = 0;
          for (final doc in docs) {
            final d = doc.data() as Map<String, dynamic>;
            if ((d['rating'] ?? 0) >= 4) positive++; else negative++;
          }
          final pct = docs.isEmpty ? 0 : ((positive / docs.length) * 100).round();

          return CustomScrollView(
            slivers: [
              SliverToBoxAdapter(
                child: Padding(
                  padding: const EdgeInsets.fromLTRB(16, 16, 16, 8),
                  child: Container(
                    padding: const EdgeInsets.all(16),
                    decoration: BoxDecoration(
                      gradient: const LinearGradient(
                        colors: [Color(0xFF3D32A8), Color(0xFF7B6FE8)],
                        begin: Alignment.topLeft,
                        end: Alignment.bottomRight,
                      ),
                      borderRadius: BorderRadius.circular(18),
                    ),
                    child: Row(children: [
                      Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                        Text('$pct%', style: const TextStyle(color: Colors.white, fontSize: 36, fontWeight: FontWeight.w900)),
                        Text('Positive rating', style: TextStyle(color: Colors.white.withOpacity(0.8), fontSize: 13)),
                      ])),
                      Column(crossAxisAlignment: CrossAxisAlignment.end, children: [
                        Row(children: [
                          const Icon(Icons.thumb_up_rounded, color: Colors.greenAccent, size: 16),
                          const SizedBox(width: 6),
                          Text('$positive positive', style: const TextStyle(color: Colors.white, fontSize: 13, fontWeight: FontWeight.w600)),
                        ]),
                        const SizedBox(height: 4),
                        Row(children: [
                          const Icon(Icons.thumb_down_rounded, color: Colors.redAccent, size: 16),
                          const SizedBox(width: 6),
                          Text('$negative negative', style: const TextStyle(color: Colors.white, fontSize: 13, fontWeight: FontWeight.w600)),
                        ]),
                        const SizedBox(height: 4),
                        Text('${docs.length} total', style: TextStyle(color: Colors.white.withOpacity(0.7), fontSize: 12)),
                      ]),
                    ]),
                  ),
                ),
              ),
              SliverPadding(
                padding: const EdgeInsets.fromLTRB(16, 8, 16, 24),
                sliver: SliverList(
                  delegate: SliverChildBuilderDelegate((ctx, i) {
                    final d = docs[i].data() as Map<String, dynamic>;
                    final rating = (d['rating'] ?? 0) as int;
                    final isPositive = rating >= 4;
                    final fromName = d['fromUserName'] ?? 'Anonymous';
                    final comment = d['comment'] ?? '';
                    final toyName = d['toyName'] ?? '';
                    final ts = d['createdAt'] as Timestamp?;
                    final date = ts != null ? DateFormat('MMM d, yyyy').format(ts.toDate()) : '';
                    return Container(
                      margin: const EdgeInsets.only(bottom: 10),
                      padding: const EdgeInsets.all(14),
                      decoration: BoxDecoration(
                        color: Theme.of(context).cardColor,
                        borderRadius: BorderRadius.circular(16),
                        border: Border.all(color: isDark ? AppColors.darkDivider : AppColors.divider),
                        boxShadow: AppShadows.card(context),
                      ),
                      child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                        Row(children: [
                          UserAvatar(name: fromName, size: 36, gradient: AppColors.heroGradient),
                          const SizedBox(width: 10),
                          Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                            Text(fromName, style: TextStyle(fontSize: 14, fontWeight: FontWeight.w800,
                              color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary)),
                            if (toyName.isNotEmpty)
                              Text('Re: $toyName', style: TextStyle(fontSize: 12, color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary)),
                          ])),
                          Column(crossAxisAlignment: CrossAxisAlignment.end, children: [
                            Container(
                              padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
                              decoration: BoxDecoration(
                                color: isPositive ? AppColors.success.withOpacity(0.1) : AppColors.error.withOpacity(0.1),
                                borderRadius: BorderRadius.circular(8),
                              ),
                              child: Row(mainAxisSize: MainAxisSize.min, children: [
                                Icon(isPositive ? Icons.thumb_up_rounded : Icons.thumb_down_rounded,
                                  size: 12, color: isPositive ? AppColors.success : AppColors.error),
                                const SizedBox(width: 4),
                                Text(isPositive ? 'Positive' : 'Negative',
                                  style: TextStyle(fontSize: 11, fontWeight: FontWeight.w700,
                                    color: isPositive ? AppColors.success : AppColors.error)),
                              ]),
                            ),
                            if (date.isNotEmpty) ...[
                              const SizedBox(height: 3),
                              Text(date, style: TextStyle(fontSize: 11, color: Colors.grey.shade500)),
                            ],
                          ]),
                        ]),
                        if (comment.isNotEmpty) ...[
                          const SizedBox(height: 10),
                          Container(
                            padding: const EdgeInsets.all(10),
                            decoration: BoxDecoration(
                              color: isDark ? AppColors.darkInputBg : const Color(0xFFF8F8FC),
                              borderRadius: BorderRadius.circular(10),
                            ),
                            child: Row(crossAxisAlignment: CrossAxisAlignment.start, children: [
                              Icon(Icons.format_quote_rounded, size: 16, color: Colors.grey.shade400),
                              const SizedBox(width: 6),
                              Expanded(child: Text(comment, style: TextStyle(fontSize: 13, color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary, height: 1.4))),
                            ]),
                          ),
                        ],
                      ]),
                    );
                  }, childCount: docs.length),
                ),
              ),
            ],
          );
        },
      ),
    );
  }
}

// ==================== BLACKLIST SCREEN ====================
class BlacklistScreen extends StatefulWidget {
  const BlacklistScreen({super.key});
  @override
  State<BlacklistScreen> createState() => _BlacklistScreenState();
}

class _BlacklistScreenState extends State<BlacklistScreen> {
  List<DocumentSnapshot> _users = [];
  bool _isLoading = true;
  String? _error;

  @override
  void initState() {
    super.initState();
    _load();
  }

  Future<void> _load() async {
    if (mounted) setState(() { _isLoading = true; _error = null; });
    try {
      final users = await FirestoreService.getBlacklistedUsers();
      if (mounted) setState(() => _users = users);
    } catch (e) {
      if (mounted) setState(() => _error = e.toString());
    }
    if (mounted) setState(() => _isLoading = false);
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Blocked Users'),
        actions: [
          IconButton(
              icon: const Icon(Icons.refresh_rounded), onPressed: _load),
        ],
      ),
      body: Center(
        child: ConstrainedBox(
          constraints: const BoxConstraints(maxWidth: 600),
          child: _isLoading
              ? const Center(child: CircularProgressIndicator(color: AppColors.primary))
              : _error != null
              ? errorState(message: 'Failed to load blocked users.', onRetry: _load)
              : _users.isEmpty
              ? emptyState(
              icon: Icons.shield_rounded,
              title: 'No blocked users',
              subtitle: 'Users you block will appear here',
              color: AppColors.success)
              : ListView.separated(
            padding: const EdgeInsets.all(16),
            itemCount: _users.length,
            separatorBuilder: (_, __) => const SizedBox(height: 8),
            itemBuilder: (_, i) {
              final data = _users[i].data() as Map<String, dynamic>;
              final username = data['blockedUsername'] ?? 'Unknown';
              final blockedId = data['blockedUserId'] ?? '';
              return Container(
                decoration: BoxDecoration(
                    color: Theme.of(context).cardColor,
                    borderRadius: BorderRadius.circular(16),
                    boxShadow: AppShadows.card(context)),
                child: ListTile(
                  contentPadding: const EdgeInsets.symmetric(
                      horizontal: 16, vertical: 8),
                  leading: UserAvatar(
                    name: username,
                    size: 46,
                    gradient: [
                      AppColors.error.withOpacity(0.7),
                      AppColors.error,
                    ],
                  ),
                  title: Text(username,
                      style: const TextStyle(
                          fontWeight: FontWeight.w800, fontSize: 15)),
                  subtitle: Padding(
                    padding: const EdgeInsets.only(top: 4),
                    child: Text('Blocked user',
                        style: TextStyle(
                            fontSize: 12, color: Colors.grey.shade500)),
                  ),
                  trailing: OutlinedButton(
                    onPressed: () async {
                      try {
                        await FirestoreService.removeFromBlacklist(blockedId);
                        await _load();
                        if (mounted) {
                          ScaffoldMessenger.of(context).showSnackBar(SnackBar(
                            content: Text('$username unblocked'),
                            margin: const EdgeInsets.all(14),
                          ));
                        }
                      } catch (e) {
                        if (mounted) {
                          ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
                            content: Text('Failed to unblock user.'),
                            backgroundColor: AppColors.error,
                            margin: EdgeInsets.all(14),
                          ));
                        }
                      }
                    },
                    style: OutlinedButton.styleFrom(
                      foregroundColor: AppColors.primary,
                      side: BorderSide(
                          color: AppColors.primary.withOpacity(0.4)),
                      shape: RoundedRectangleBorder(
                          borderRadius: BorderRadius.circular(10)),
                      padding: const EdgeInsets.symmetric(
                          horizontal: 14, vertical: 8),
                    ),
                    child: const Text('Unblock',
                        style: TextStyle(fontWeight: FontWeight.w700)),
                  ),
                ),
              );
            },
          ),
        ),
      ),
    );
  }
}

// ==================== PROFILE SCREEN ====================
class ProfileScreen extends StatefulWidget {
  const ProfileScreen({super.key});
  @override
  State<ProfileScreen> createState() => _ProfileScreenState();
}

class _ProfileScreenState extends State<ProfileScreen>
    with SingleTickerProviderStateMixin {
  bool _isLoading = true;
  late AnimationController _fadeCtrl;
  late Animation<double> _fadeAnim;
  String? _username, _email;
  Map<String, int> _stats = {'toys': 0, 'exchanges': 0, 'feedback': 0};

  @override
  void initState() {
    super.initState();
    _fadeCtrl =
        AnimationController(vsync: this, duration: const Duration(milliseconds: 500));
    _fadeAnim = CurvedAnimation(parent: _fadeCtrl, curve: Curves.easeOut);
    _load();
  }

  @override
  void dispose() {
    _fadeCtrl.dispose();
    super.dispose();
  }

  Future<void> _load() async {
    if (mounted) setState(() => _isLoading = true);
    try {
      final user = _auth.currentUser;
      if (user != null) {
        _username = user.displayName ?? user.email?.split('@').first ?? 'User';
        _email = user.email;
        final stats = await FirestoreService.getProfileStats();
        if (mounted) _stats = stats;
      }
    } catch (e) {
      debugPrint('Profile load error: $e');
    }
    if (mounted) {
      setState(() => _isLoading = false);
      _fadeCtrl.forward(from: 0);
    }
  }

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Scaffold(
      appBar: AppBar(
        title: const Text('My Profile'),
        automaticallyImplyLeading: false,
        actions: [
          IconButton(
            icon: const Icon(Icons.settings_rounded),
            onPressed: _showEditDialog,
            tooltip: 'Edit Profile',
          ),
        ],
      ),
      body: _isLoading
          ? const Center(child: CircularProgressIndicator(color: AppColors.primary))
          : FadeTransition(
        opacity: _fadeAnim,
        child: RefreshIndicator(
          onRefresh: _load,
          color: AppColors.primary,
          child: SingleChildScrollView(
            physics: const AlwaysScrollableScrollPhysics(),
            child: Column(children: [
              // ── Hero Header ──
              Container(
                width: double.infinity,
                padding: const EdgeInsets.fromLTRB(24, 36, 24, 32),
                decoration: const BoxDecoration(
                  gradient: LinearGradient(
                    colors: [Color(0xFF3D32A8), Color(0xFF5B4FCF), Color(0xFF7B6FE8)],
                    begin: Alignment.topLeft,
                    end: Alignment.bottomRight,
                  ),
                ),
                child: Stack(children: [
                  Positioned(right: -20, top: -20,
                    child: Container(width: 120, height: 120,
                      decoration: BoxDecoration(color: Colors.white.withOpacity(0.05), shape: BoxShape.circle))),
                  Positioned(left: -30, bottom: -30,
                    child: Container(width: 100, height: 100,
                      decoration: BoxDecoration(color: Colors.white.withOpacity(0.04), shape: BoxShape.circle))),
                  Column(children: [
                    Stack(children: [
                      UserAvatar(name: _username ?? '?', size: 90),
                      Positioned(
                        right: 0, bottom: 0,
                        child: GestureDetector(
                          onTap: _showEditDialog,
                          child: Container(
                            width: 28, height: 28,
                            decoration: BoxDecoration(
                              color: AppColors.accent,
                              shape: BoxShape.circle,
                              border: Border.all(color: Colors.white, width: 2),
                            ),
                            child: const Icon(Icons.edit_rounded, size: 14, color: Colors.white),
                          ),
                        ),
                      ),
                    ]),
                    const SizedBox(height: 14),
                    Text(_username ?? '',
                      style: const TextStyle(color: Colors.white, fontSize: 24, fontWeight: FontWeight.w900, letterSpacing: -0.5)),
                    const SizedBox(height: 4),
                    Text(_email ?? '',
                      style: TextStyle(color: Colors.white.withOpacity(0.7), fontSize: 13.5)),
                    const SizedBox(height: 6),
                    UserRatingBadge(userId: _auth.currentUser?.uid ?? '', lightMode: true),
                    const SizedBox(height: 20),
                    // Stats pills
                    Container(
                      padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 12),
                      decoration: BoxDecoration(
                        color: Colors.white.withOpacity(0.1),
                        borderRadius: BorderRadius.circular(18),
                        border: Border.all(color: Colors.white.withOpacity(0.15)),
                      ),
                      child: Row(mainAxisAlignment: MainAxisAlignment.spaceEvenly, children: [
                        _statPill('${_stats['toys']}', 'Toys Listed'),
                        Container(width: 1, height: 36, color: Colors.white.withOpacity(0.2)),
                        _statPill('${_stats['exchanges']}', 'Exchanges'),
                        Container(width: 1, height: 36, color: Colors.white.withOpacity(0.2)),
                        _statPill('${_stats['feedback']}', 'Reviews'),
                      ]),
                    ),
                  ]),
                ]),
              ),
              // ── Info & Actions ──
              Padding(
                padding: EdgeInsets.symmetric(
                  horizontal: Responsive.isMobile(context) ? 16 : 24,
                  vertical: 20,
                ),
                child: Center(
                  child: ConstrainedBox(
                    constraints: const BoxConstraints(maxWidth: 500),
                    child: Column(children: [
                      // Info card
                      Container(
                        decoration: BoxDecoration(
                            color: Theme.of(context).cardColor,
                            borderRadius: BorderRadius.circular(18),
                            boxShadow: AppShadows.card(context)),
                        child: Padding(
                          padding: const EdgeInsets.all(16),
                          child: Column(children: [
                            _infoRow(Icons.person_rounded, 'Username', _username ?? ''),
                            Divider(height: 20, color: isDark ? AppColors.darkDivider : AppColors.divider),
                            _infoRow(Icons.mail_rounded, 'Email', _email ?? ''),
                          ]),
                        ),
                      ),
                      const SizedBox(height: 16),
                      // Quick action tiles
                      _actionTile(
                        icon: Icons.edit_rounded,
                        color: AppColors.primary,
                        label: 'Edit Username',
                        subtitle: 'Change how others see you',
                        onTap: _showEditDialog,
                      ),
                      const SizedBox(height: 10),
                      _actionTile(
                        icon: isDark ? Icons.light_mode_rounded : Icons.dark_mode_rounded,
                        color: isDark ? AppColors.yellow : AppColors.primaryDark,
                        label: isDark ? 'Switch to Light Mode' : 'Switch to Dark Mode',
                        subtitle: isDark ? 'Easier on the eyes in bright light' : 'Easier on the eyes at night',
                        onTap: () => ThemeManager().toggleTheme(),
                      ),
                      const SizedBox(height: 10),
                      _actionTile(
                        icon: Icons.lock_reset_rounded,
                        color: AppColors.warning,
                        label: 'Change Password',
                        subtitle: 'Update your account password',
                        onTap: _showChangePasswordDialog,
                      ),
                      const SizedBox(height: 10),
                      _actionTile(
                        icon: Icons.history_rounded,
                        color: AppColors.accent,
                        label: 'My Activities',
                        subtitle: 'View full lending & borrowing history',
                        onTap: () => navigateTo(context, const UserDashboardScreen()),
                      ),
                      const SizedBox(height: 10),
                      _actionTile(
                        icon: Icons.toys_rounded,
                        color: AppColors.coral,
                        label: 'My Toy Listings',
                        subtitle: 'View, edit or remove your toys',
                        onTap: () => navigateTo(context, const MyToysScreen()),
                      ),
                      const SizedBox(height: 10),
                      _actionTile(
                        icon: Icons.star_rounded,
                        color: AppColors.yellow,
                        label: 'My Reviews',
                        subtitle: 'See feedback others left for you',
                        onTap: () => navigateTo(context, const FeedbackScreen()),
                      ),
                      const SizedBox(height: 10),
                      _actionTile(
                        icon: Icons.block_rounded,
                        color: Colors.grey.shade600,
                        label: 'Blocked Users',
                        subtitle: 'Manage who you have blocked',
                        onTap: () => navigateTo(context, const BlacklistScreen()),
                      ),
                      const SizedBox(height: 24),
                      // App version footer
                      Text('ToyShare v1.0.0', style: TextStyle(fontSize: 12, color: Colors.grey.shade400, fontWeight: FontWeight.w500)),
                      const SizedBox(height: 4),
                      Text('Made with ❤️ for sharing joy', style: TextStyle(fontSize: 11, color: Colors.grey.shade400)),
                      const SizedBox(height: 10),
                      const SizedBox(height: 10),
                      _actionTile(
                        icon: Icons.logout_rounded,
                        color: AppColors.error,
                        label: 'Sign Out',
                        subtitle: 'See you next time!',
                        onTap: () async {
                          final confirm = await showDialog<bool>(
                            context: context,
                            builder: (ctx) => AlertDialog(
                              shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
                              title: const Text('Sign Out?', style: TextStyle(fontWeight: FontWeight.w800)),
                              content: const Text('Are you sure you want to sign out?'),
                              actions: [
                                TextButton(onPressed: () => Navigator.pop(ctx, false),
                                  child: const Text('Cancel')),
                                ElevatedButton(
                                  onPressed: () => Navigator.pop(ctx, true),
                                  style: ElevatedButton.styleFrom(
                                    backgroundColor: AppColors.error, foregroundColor: Colors.white, elevation: 0,
                                    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
                                  child: const Text('Sign Out', style: TextStyle(fontWeight: FontWeight.w700)),
                                ),
                              ],
                            ),
                          );
                          if (confirm == true) await _auth.signOut();
                        },
                        isDestructive: true,
                      ),
                      const SizedBox(height: 30),
                    ]),
                  ),
                ),
              ),
            ]),
          ),
        ),
      ),
    );
  }

  Widget _actionTile({
    required IconData icon,
    required Color color,
    required String label,
    required String subtitle,
    required VoidCallback onTap,
    bool isDestructive = false,
  }) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Material(
      color: Theme.of(context).cardColor,
      borderRadius: BorderRadius.circular(16),
      child: InkWell(
        onTap: onTap,
        borderRadius: BorderRadius.circular(16),
        child: Container(
          padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 14),
          decoration: BoxDecoration(
            borderRadius: BorderRadius.circular(16),
            boxShadow: AppShadows.card(context),
          ),
          child: Row(children: [
            Container(
              width: 42, height: 42,
              decoration: BoxDecoration(
                color: color.withOpacity(0.1),
                borderRadius: BorderRadius.circular(12),
              ),
              child: Icon(icon, color: color, size: 20),
            ),
            const SizedBox(width: 14),
            Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
              Text(label, style: TextStyle(
                fontSize: 14.5, fontWeight: FontWeight.w700,
                color: isDestructive ? AppColors.error : (isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
              )),
              Text(subtitle, style: TextStyle(
                fontSize: 12, color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary,
              )),
            ])),
            Icon(Icons.chevron_right_rounded, color: Colors.grey.shade400, size: 20),
          ]),
        ),
      ),
    );
  }

  void _showChangePasswordDialog() {
    final emailCtrl = TextEditingController(text: _email ?? '');
    showDialog(
      context: context,
      builder: (ctx) {
        final isDark = Theme.of(ctx).brightness == Brightness.dark;
        return AlertDialog(
          backgroundColor: isDark ? AppColors.darkCardBg : Colors.white,
          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
          insetPadding: const EdgeInsets.symmetric(horizontal: 20, vertical: 24),
          title: Row(children: [
            Container(width: 36, height: 36,
              decoration: BoxDecoration(color: AppColors.warning.withOpacity(0.1), borderRadius: BorderRadius.circular(10)),
              child: const Icon(Icons.lock_reset_rounded, color: AppColors.warning, size: 20)),
            const SizedBox(width: 12),
            const Flexible(child: Text('Reset Password', style: TextStyle(fontWeight: FontWeight.w800, fontSize: 17))),
          ]),
          content: Column(mainAxisSize: MainAxisSize.min, crossAxisAlignment: CrossAxisAlignment.start, children: [
            const Text('We\'ll send a password reset link to your email address.', style: TextStyle(fontSize: 13.5, height: 1.5)),
            const SizedBox(height: 12),
            Container(
              padding: const EdgeInsets.all(12),
              decoration: BoxDecoration(
                color: AppColors.primary.withOpacity(0.07),
                borderRadius: BorderRadius.circular(12),
              ),
              child: Row(children: [
                const Icon(Icons.mail_rounded, size: 16, color: AppColors.primary),
                const SizedBox(width: 8),
                Expanded(child: Text(_email ?? '', style: const TextStyle(fontSize: 13.5, fontWeight: FontWeight.w700, color: AppColors.primary))),
              ]),
            ),
          ]),
          actions: [
            Row(children: [
              Expanded(child: TextButton(
                onPressed: () => Navigator.pop(ctx),
                child: const Text('Cancel', style: TextStyle(color: AppColors.textSecondary)),
              )),
              const SizedBox(width: 8),
              Expanded(flex: 2, child: ElevatedButton.icon(
                onPressed: () async {
                  Navigator.pop(ctx);
                  try {
                    await _auth.sendPasswordResetEmail(email: _email ?? '');
                    if (mounted) {
                      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
                        content: Row(children: [
                          Icon(Icons.check_circle_rounded, color: Colors.white, size: 18),
                          SizedBox(width: 8),
                          Expanded(child: Text('Password reset email sent! Check your inbox.', style: TextStyle(fontWeight: FontWeight.w600))),
                        ]),
                        backgroundColor: AppColors.success,
                        margin: EdgeInsets.all(14),
                        duration: Duration(seconds: 4),
                      ));
                    }
                  } catch (e) {
                    if (mounted) {
                      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
                        content: Text('Failed to send reset email. Please try again.'),
                        backgroundColor: AppColors.error,
                        margin: EdgeInsets.all(14),
                      ));
                    }
                  }
                },
                icon: const Icon(Icons.send_rounded, size: 15),
                label: const Text('Send Reset Link', style: TextStyle(fontWeight: FontWeight.w700, fontSize: 13)),
                style: ElevatedButton.styleFrom(
                  backgroundColor: AppColors.warning,
                  foregroundColor: Colors.white,
                  elevation: 0,
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                ),
              )),
            ]),
          ],
        );
      },
    );
  }

  void _showEditDialog() {
    final ctrl = TextEditingController(text: _username ?? '');
    showDialog(
      context: context,
      builder: (ctx) {
        final isDark = Theme.of(ctx).brightness == Brightness.dark;
        return AlertDialog(
          backgroundColor: isDark ? AppColors.darkCardBg : Colors.white,
          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
          title: Text('Edit Profile',
              style: TextStyle(
                fontWeight: FontWeight.w800,
                fontSize: 18,
                color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
              )),
          content: Column(mainAxisSize: MainAxisSize.min, children: [
            TextField(
              controller: ctrl,
              style: TextStyle(
                fontSize: 14.5,
                color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary,
              ),
              decoration: InputDecoration(
                labelText: 'Username',
                labelStyle: TextStyle(
                  color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary,
                ),
                prefixIcon: Icon(Icons.person_rounded,
                    size: 20, color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary),
                border: OutlineInputBorder(borderRadius: BorderRadius.circular(14)),
                enabledBorder: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(14),
                  borderSide: BorderSide(color: isDark ? AppColors.darkBorder : AppColors.divider),
                ),
                focusedBorder: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(14),
                  borderSide: const BorderSide(color: AppColors.primary, width: 2),
                ),
                filled: true,
                fillColor: isDark ? AppColors.darkInputBg : const Color(0xFFF8F8FC),
              ),
            ),
          ]),
          actions: [
            TextButton(
                onPressed: () => Navigator.pop(ctx),
                child: const Text('Cancel',
                    style: TextStyle(color: AppColors.textSecondary))),
            ElevatedButton(
              onPressed: () async {
                final newName = ctrl.text.trim();
                if (newName.isEmpty) return;
                Navigator.pop(ctx);
                try {
                  await _auth.currentUser?.updateDisplayName(newName);
                  await _auth.currentUser?.reload();
                  if (mounted) {
                    setState(() => _username = newName);
                    ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
                      content: Row(children: [
                        Icon(Icons.check_circle_rounded, color: Colors.white, size: 18),
                        SizedBox(width: 8),
                        Expanded(child: Text('Profile updated!',
                            style: TextStyle(fontWeight: FontWeight.w600))),
                      ]),
                      backgroundColor: AppColors.success,
                      margin: EdgeInsets.all(14),
                    ));
                  }
                } catch (e) {
                  if (mounted) {
                    ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
                      content: Text('Failed to update profile.'),
                      backgroundColor: AppColors.error,
                      margin: EdgeInsets.all(14),
                    ));
                  }
                }
              },
              style: ElevatedButton.styleFrom(
                backgroundColor: AppColors.primary,
                foregroundColor: Colors.white,
                elevation: 0,
                shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
              ),
              child: const Text('Save', style: TextStyle(fontWeight: FontWeight.w700)),
            ),
          ],
        );
      },
    );
  }

  Widget _statPill(String value, String label) => Column(children: [
    Text(value,
        style: const TextStyle(
            color: Colors.white, fontSize: 20, fontWeight: FontWeight.w900)),
    const SizedBox(height: 2),
    Text(label,
        style: TextStyle(color: Colors.white.withOpacity(0.7), fontSize: 11.5)),
  ]);

  Widget _divider() => Container(
      width: 1, height: 32, color: Colors.white.withOpacity(0.3));

  Widget _infoRow(IconData icon, String label, String value) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    return Row(children: [
      Container(
        width: 40,
        height: 40,
        decoration: BoxDecoration(
            color: AppColors.primary.withOpacity(0.08),
            borderRadius: BorderRadius.circular(12)),
        child: Icon(icon, size: 20, color: AppColors.primary),
      ),
      const SizedBox(width: 14),
      Expanded(
        child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
          Text(label,
              style: TextStyle(
                  fontSize: 11.5,
                  color: isDark ? AppColors.darkTextSecondary : AppColors.textSecondary,
                  fontWeight: FontWeight.w600,
                  letterSpacing: 0.3)),
          const SizedBox(height: 2),
          Text(value,
              style: TextStyle(
                  fontSize: 15,
                  fontWeight: FontWeight.w800,
                  color: isDark ? AppColors.darkTextPrimary : AppColors.textPrimary),
              overflow: TextOverflow.ellipsis),
        ]),
      ),
    ]);
  }
}